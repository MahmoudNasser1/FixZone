# 📚 وثائق استيراد البيانات من النظام القديم إلى FixZone

## 📋 نظرة عامة

تم إعداد نظام كامل ومتكامل لاستيراد البيانات من النظام القديم (`u539485933_maintain`) إلى نظام FixZone الجديد مع الحفاظ على:
- ✅ **الترابط بين الجداول المختلفة**
- ✅ **سلامة البيانات**
- ✅ **إمكانية التتبع** (عبر ملفات mapping)
- ✅ **القدرة على التدقيق** (عبر سجلات الأخطاء والتقارير)

---

## 🎯 الأهداف المحققة

### 1. **استيراد العملاء**
- ✅ من جدول `clients` إلى `Customer`
- ✅ تنظيف أرقام الهواتف
- ✅ التعامل مع الأسماء الكاملة
- ✅ الحفاظ على البيانات الإضافية في `customFields`
- ✅ تخطي المكررات

**الناتج:** ~960 عميل

### 2. **استيراد الخدمات**
- ✅ استخراج الخدمات الفريدة من `invoice_services`
- ✅ حساب متوسط الأسعار
- ✅ إنشاء وصف تلقائي
- ✅ تجنب التكرار

**الناتج:** ~50-100 خدمة فريدة

### 3. **استيراد الأجهزة**
- ✅ من `invoices` إلى `Device`
- ✅ دمج الأجهزة المكررة (نفس SN + نفس العميل)
- ✅ الحفاظ على المواصفات والملحقات

**الناتج:** ~1200 جهاز

### 4. **استيراد طلبات الإصلاح**
- ✅ من `invoices` إلى `RepairRequest`
- ✅ تحويل الحالات من العربية للإنجليزية
- ✅ ربط الخدمات بطلبات الإصلاح
- ✅ الحفاظ على تقارير الفنيين

**الناتج:** 1414 طلب إصلاح

### 5. **استيراد الفواتير**
- ✅ من `invoices` إلى `Invoice` + `InvoiceItem` + `Payment`
- ✅ حساب حالة الفاتورة تلقائياً
- ✅ إنشاء سجلات الدفع
- ✅ ربط عناصر الفاتورة بالخدمات

**الناتج:** 1414 فاتورة + 1854 عنصر فاتورة

---

## 📊 البيانات المستوردة

### إحصائيات متوقعة:

| الجدول | النظام القديم | النظام الجديد | الملاحظات |
|--------|---------------|---------------|-----------|
| **العملاء** | 965 | ~960 | بعد حذف المكررات |
| **الخدمات** | 1854 سجل | ~100 | خدمات فريدة فقط |
| **الأجهزة** | - | ~1200 | من بيانات الفواتير |
| **طلبات الإصلاح** | 1414 | 1414 | مباشر |
| **الفواتير** | 1414 | 1414 | مباشر |
| **عناصر الفاتورة** | 1854 | 1854 | من الخدمات |
| **سجلات الدفع** | - | ~1200 | الفواتير المدفوعة |

---

## 🗺️ خريطة التحويل

### العملاء (clients → Customer)

```
النظام القديم              →    النظام الجديد
───────────────────────────────────────────────────
clients.id                  →    mapping file
clients.name                →    Customer.name
clients.mobile              →    Customer.phone (منظف)
clients.address             →    Customer.address
clients.note                →    Customer.customFields.importNote
clients.credit_limit        →    Customer.customFields.creditLimit
clients.open_balance        →    Customer.customFields.openBalance
clients.id_number           →    Customer.customFields.idNumber
```

### الأجهزة (invoices → Device)

```
النظام القديم              →    النظام الجديد
───────────────────────────────────────────────────
invoices.device_type        →    Device.deviceType
invoices.brand              →    Device.brand
invoices.device_model       →    Device.model
invoices.device_sn          →    Device.serialNumber
invoices.client_id          →    Device.customerId (من mapping)
invoices.accessories        →    Device.customFields.accessories
invoices.specifcations      →    Device.customFields.specifications
```

### طلبات الإصلاح (invoices → RepairRequest)

```
النظام القديم              →    النظام الجديد
───────────────────────────────────────────────────
invoices.id                 →    customFields.oldInvoiceId
invoices.client_id          →    RepairRequest.customerId
Device (منشأ)               →    RepairRequest.deviceId
invoices.problem_description →   RepairRequest.reportedProblem
invoices.examination        →    RepairRequest.technicianReport
invoices.status_id          →    RepairRequest.status (محول)
invoices.note               →    RepairRequest.notes
invoices.entery_at          →    RepairRequest.createdAt
```

### الفواتير (invoices → Invoice)

```
النظام القديم              →    النظام الجديد
───────────────────────────────────────────────────
invoices.total              →    Invoice.totalAmount
invoices.paid               →    Invoice.amountPaid
invoices.tax                →    Invoice.taxAmount
حساب تلقائي                →    Invoice.status
RepairRequest (منشأ)       →    Invoice.repairRequestId
invoices.payment            →    customFields.paymentMethod
```

---

## 📁 الملفات المنشأة

### في مجلد `backend/scripts/migration/`:

```
migration/
├── 📄 config.js                      # إعدادات الاتصال والدوال المساعدة
├── 📄 import-customers.js            # استيراد العملاء (965 عميل)
├── 📄 import-services.js             # استيراد الخدمات (~100 خدمة)
├── 📄 import-repairs.js              # استيراد الأجهزة وطلبات الإصلاح (1414)
├── 📄 import-invoices.js             # استيراد الفواتير (1414)
├── 📄 master-import.js               # السكربت الرئيسي
├── 📄 DATA_IMPORT_PLAN.md           # خطة الاستيراد التفصيلية
├── 📄 README.md                      # دليل الاستخدام
├── 📄 .env.example                   # مثال لإعدادات البيئة
│
├── 📁 mappings/                      # ملفات الربط
│   ├── customer-mapping.json         # {old_id: new_id}
│   ├── service-mapping.json          # {service_name: service_id}
│   ├── device-mapping.json           # {old_invoice_id: device_id}
│   ├── repair-request-mapping.json   # {old_invoice_id: repair_id}
│   └── invoice-mapping.json          # {old_invoice_id: new_invoice_id}
│
├── 📁 logs/                          # سجلات الأخطاء
│   └── *.log / *.json
│
└── 📁 output/                        # المخرجات
    ├── backup_*.sql
    └── import-report-*.md
```

---

## 🔧 الدوال والميزات الرئيسية

### 1. **تنظيف البيانات**
```javascript
cleanPhoneNumber(phone)      // إزالة الأحرف الخاصة من أرقام الهواتف
splitFullName(fullName)      // تقسيم الاسم إلى firstName و lastName
```

### 2. **التحويلات**
```javascript
convertInvoiceStatus(paid, total)    // حساب حالة الفاتورة (paid/partial/unpaid)
convertRepairStatus(oldStatusId)     // تحويل حالة الإصلاح من العربية للإنجليزية
```

### 3. **Mapping والتتبع**
```javascript
saveMapping(filename, data)   // حفظ mapping إلى ملف JSON
loadMapping(filename)         // قراءة mapping من ملف JSON
```

### 4. **التسجيل والتقارير**
```javascript
saveLog(filename, data)       // حفظ الأخطاء والسجلات
displayStats(title, stats)    // عرض الإحصائيات بشكل منظم
```

---

## 🚀 كيفية الاستخدام

### الطريقة السريعة:
```bash
cd /opt/lampp/htdocs/FixZone/backend/scripts/migration
npm install mysql2 dotenv
node master-import.js
```

### الطريقة المفصلة:
راجع `backend/scripts/migration/README.md` للتفاصيل الكاملة.

---

## ⚙️ الإعدادات المتقدمة

### في ملف `config.js`:

```javascript
const importConfig = {
  batchSize: 100,              // عدد السجلات في كل دفعة
  defaultUserId: 1,            // المستخدم الافتراضي
  defaultBranchId: 1,          // الفرع الافتراضي
  skipDeletedRecords: true,    // تخطي السجلات المحذوفة
  logErrors: true,             // تسجيل الأخطاء
  outputDir: './output',
  logsDir: './logs',
  mappingsDir: './mappings'
};
```

---

## 🛡️ الأمان وسلامة البيانات

### 1. **النسخ الاحتياطي التلقائي**
- يتم إنشاء نسخة احتياطية تلقائياً قبل البدء
- الموقع: `output/backup_before_import_*.sql`

### 2. **التحقق من المتطلبات**
- التحقق من الاتصال بقواعد البيانات
- التحقق من وجود المستخدم والفرع الافتراضي
- التحقق من متغيرات البيئة

### 3. **معالجة الأخطاء**
- حفظ السجلات الفاشلة في ملفات منفصلة
- تسجيل جميع الأخطاء مع stack traces
- إمكانية المتابعة حتى مع وجود أخطاء جزئية

### 4. **الترابط والتناسق**
- تنفيذ المراحل بالتسلسل الصحيح
- التحقق من وجود السجلات المرتبطة قبل الإدراج
- استخدام transactions عند الحاجة

---

## 📊 خريطة تحويل الحالات

### حالات طلب الإصلاح:

| الحالة (قديم) | الحالة (جديد) | الوصف |
|---------------|--------------|--------|
| جديد/مستلم | `RECEIVED` | تم استلام الجهاز |
| قيد الفحص | `INSPECTION` | تحت الفحص |
| قيد الإصلاح/جاري | `UNDER_REPAIR` | تحت الإصلاح |
| جاهز للتسليم | `READY_FOR_DELIVERY` | جاهز للتسليم |
| تم التسليم | `DELIVERED` | تم تسليم الجهاز |
| ملغي | `REJECTED` | طلب ملغي |
| بانتظار القطع | `WAITING_PARTS` | بانتظار قطع الغيار |
| معلق | `ON_HOLD` | معلق |

### حالات الفاتورة:

| الشرط | الحالة |
|-------|--------|
| `paid == total` | `paid` (مدفوعة بالكامل) |
| `0 < paid < total` | `partial` (مدفوعة جزئياً) |
| `paid == 0` | `unpaid` (غير مدفوعة) |

---

## 🔍 التحقق من النتائج

### استعلامات SQL للتحقق:

```sql
-- 1. عد السجلات المستوردة
SELECT 
  'Customers' as table_name, 
  COUNT(*) as count 
FROM Customer 
WHERE JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
UNION ALL
SELECT 'Devices', COUNT(*) FROM Device 
WHERE JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
UNION ALL
SELECT 'RepairRequests', COUNT(*) FROM RepairRequest 
WHERE JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
UNION ALL
SELECT 'Invoices', COUNT(*) FROM Invoice 
WHERE JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system';

-- 2. التحقق من الترابط
SELECT 
  'RepairRequests without Customer' as issue,
  COUNT(*) as count
FROM RepairRequest 
WHERE customerId IS NULL 
  AND JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
UNION ALL
SELECT 
  'RepairRequests without Device',
  COUNT(*)
FROM RepairRequest 
WHERE deviceId IS NULL 
  AND JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
UNION ALL
SELECT 
  'Invoices without RepairRequest',
  COUNT(*)
FROM Invoice 
WHERE repairRequestId IS NULL 
  AND JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system';

-- 3. توزيع حالات الفواتير
SELECT 
  status,
  COUNT(*) as count,
  SUM(totalAmount) as total_amount,
  SUM(amountPaid) as paid_amount
FROM Invoice
WHERE JSON_EXTRACT(customFields, '$.importedFrom') = 'old_system'
GROUP BY status;

-- 4. أكثر 10 عملاء نشاطاً
SELECT 
  c.name,
  c.phone,
  COUNT(rr.id) as repair_count,
  SUM(i.totalAmount) as total_spent
FROM Customer c
LEFT JOIN RepairRequest rr ON c.id = rr.customerId
LEFT JOIN Invoice i ON rr.invoiceId = i.id
WHERE JSON_EXTRACT(c.customFields, '$.importedFrom') = 'old_system'
GROUP BY c.id
ORDER BY repair_count DESC
LIMIT 10;
```

---

## 🎓 الدروس المستفادة والتوصيات

### ✅ نقاط القوة:
1. **التصميم المودولي**: كل مرحلة في ملف منفصل
2. **إمكانية التتبع**: ملفات mapping لجميع المراحل
3. **معالجة الأخطاء الشاملة**: حفظ جميع الأخطاء والسجلات الفاشلة
4. **المرونة**: إمكانية تشغيل المراحل بشكل منفصل أو شامل
5. **التوثيق الكامل**: ملفات README و DATA_IMPORT_PLAN

### 💡 التوصيات للمستقبل:
1. **النسخ الاحتياطي الدوري**: أنشئ نسخ احتياطية قبل أي عملية كبيرة
2. **الاختبار على بيئة staging**: لا تختبر على الإنتاج مباشرة
3. **مراجعة السجلات الفاشلة**: تحقق من الـ logs بعد كل استيراد
4. **الحفاظ على mappings**: لا تحذف ملفات الـ mapping
5. **التحديثات التدريجية**: استورد البيانات على دفعات إذا كانت كبيرة جداً

---

## 📞 المساعدة والدعم

### إذا واجهت مشكلة:

1. **راجع ملفات الـ logs**:
   ```bash
   cat logs/*-errors.log
   cat logs/*-failed.json
   ```

2. **راجع التقرير الشامل**:
   ```bash
   cat output/import-report-*.md
   ```

3. **راجع الوثائق**:
   - `backend/scripts/migration/README.md`
   - `backend/scripts/migration/DATA_IMPORT_PLAN.md`

---

## ✅ Checklist النهائي

### قبل الاستيراد:
- [ ] النسخ الاحتياطي من قاعدة البيانات الجديدة
- [ ] التأكد من وجود قاعدة البيانات القديمة
- [ ] التأكد من وجود مستخدم افتراضي (id: 1)
- [ ] التأكد من وجود فرع افتراضي (id: 1)
- [ ] إعداد ملف `.env`
- [ ] تثبيت التبعيات (`npm install`)

### أثناء الاستيراد:
- [ ] مراقبة سير العملية
- [ ] عدم إيقاف العملية في المنتصف
- [ ] مراقبة استهلاك الموارد

### بعد الاستيراد:
- [ ] التحقق من عدد السجلات
- [ ] مراجعة السجلات الفاشلة
- [ ] التحقق من الترابط بين الجداول
- [ ] اختبار النظام الجديد
- [ ] حفظ ملفات الـ mapping
- [ ] مراجعة التقرير الشامل
- [ ] توثيق أي ملاحظات

---

## 📈 الإحصائيات المتوقعة

### الوقت:
- **الإعداد:** 5-10 دقائق
- **التنفيذ:** 5-15 دقيقة
- **التحقق:** 10-15 دقيقة
- **الإجمالي:** 20-40 دقيقة

### الموارد:
- **المساحة:** ~500 MB (بما فيها النسخة الاحتياطية)
- **الذاكرة:** ~200-500 MB
- **المعالج:** استخدام معتدل

### معدل النجاح المتوقع:
- **العملاء:** 95-98%
- **الخدمات:** 98-100%
- **الأجهزة:** 95-98%
- **طلبات الإصلاح:** 95-98%
- **الفواتير:** 95-98%

---

## 🎯 الخلاصة

تم إنشاء نظام متكامل وموثوق لاستيراد البيانات من النظام القديم إلى FixZone مع:

✅ **الحفاظ على سلامة البيانات**
✅ **الترابط الكامل بين الجداول**
✅ **إمكانية التتبع والتدقيق**
✅ **معالجة شاملة للأخطاء**
✅ **توثيق كامل**
✅ **سهولة الاستخدام**

النظام جاهز للاستخدام ويمكن تشغيله بأمان.

---

**تم الإعداد بواسطة:** FixZone Development Team
**التاريخ:** 2025-11-20
**الإصدار:** 1.0
**الحالة:** ✅ جاهز للإنتاج


