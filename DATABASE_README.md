# Fix Zone ERP - دليل قاعدة البيانات

## ملف قاعدة البيانات الموحد

### 📁 الملفات المهمة

#### ✅ الملفات الأساسية (يجب الاحتفاظ بها)
- **`FIXZONE_DATABASE_COMPLETE.sql`** - الملف الموحد الكامل لقاعدة البيانات
- **`migrations/fixzone_erp_full_schema.sql`** - الهيكل الأساسي (للمرجع)
- **`migrations/seed_data_complete.sql`** - البيانات التجريبية (للمرجع)
- **`database/seed_data.sql`** - البيانات الأساسية (للمرجع)
- **`backend/database/migrations/create_activity_log_table.sql`** - جدول سجل الأنشطة

### 🚀 كيفية الاستخدام

#### للتطبيق الأول (قاعدة بيانات جديدة):
```bash
mysql -u root -p < FIXZONE_DATABASE_COMPLETE.sql
```

#### للتحديث (قاعدة بيانات موجودة):
احرص على عمل نسخة احتياطية أولاً:
```bash
mysqldump -u root -p FZ > backup_$(date +%Y%m%d_%H%M%S).sql
mysql -u root -p < FIXZONE_DATABASE_COMPLETE.sql
```

### 📊 محتويات قاعدة البيانات

#### الجداول الأساسية:
- **إدارة المستخدمين**: `User`, `Role`, `UserLoginLog`, `activity_log`
- **إدارة العملاء**: `Customer`, `Company`
- **إدارة الطلبات**: `RepairRequest`, `RepairRequestService`, `RepairRequestAccessory`
- **إدارة المخزون**: `InventoryItem`, `Warehouse`, `StockLevel`, `StockMovement`
- **إدارة الفواتير**: `Invoice`, `InvoiceItem`, `Payment`
- **إدارة الخدمات**: `Service`, `PartsUsed`
- **النظام العام**: `SystemSetting`, `City`, `Branch`

#### البيانات التجريبية:
- 4 مستخدمين (admin, manager, technician, receptionist)
- 3 شركات عملاء
- 4 عملاء (فرديين وشركات)
- 10 مدن مصرية
- 3 فروع
- عناصر مخزون أساسية
- 5 خدمات إصلاح
- إعدادات النظام الأساسية

### ⚙️ الإعدادات المهمة

#### العملة الافتراضية:
- **الجنيه المصري (EGP)** كعملة افتراضية
- معدل الضريبة: 14%
- فترة الضمان: 30 يوم

#### كلمات المرور الافتراضية:
جميع المستخدمين: `password123`
- admin@fixzone.com
- manager1@fixzone.com  
- tech1@fixzone.com
- reception1@fixzone.com

### 🔧 الصيانة والتحديث

#### تحديث الملف الموحد:
عند إجراء تغييرات على قاعدة البيانات، يجب تحديث الملف الموحد:

1. تصدير الهيكل الحالي:
```bash
mysqldump -u root -p --no-data --routines --triggers FZ > schema_new.sql
```

2. تصدير البيانات الأساسية:
```bash
mysqldump -u root -p --no-create-info --where="1" FZ > data_new.sql
```

3. دمج التغييرات في `FIXZONE_DATABASE_COMPLETE.sql`

#### النسخ الاحتياطية:
```bash
# نسخة احتياطية كاملة
mysqldump -u root -p FZ > backup_complete_$(date +%Y%m%d_%H%M%S).sql

# نسخة احتياطية للبيانات فقط
mysqldump -u root -p --no-create-info FZ > backup_data_$(date +%Y%m%d_%H%M%S).sql
```

### 📈 الأداء والفهرسة

تم إضافة فهارس محسنة للأداء:
- فهارس على المفاتيح الأجنبية
- فهارس على الحقول المستخدمة في البحث
- فهارس على التواريخ والحالات

### 🔐 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- سجل تدقيق شامل (AuditLog)
- سجل أنشطة المستخدمين (activity_log)
- Soft delete للبيانات المهمة

### 📞 الدعم

للمساعدة أو الاستفسارات:
- راجع الكود المصدري في `/backend/controllers/`
- تحقق من ملفات الـ API في `/backend/routes/`
- راجع واجهة المستخدم في `/frontend/react-app/src/`

---
**تاريخ آخر تحديث**: 12 سبتمبر 2025  
**الإصدار**: 1.0 Production Ready
