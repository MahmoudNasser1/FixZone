# 🧪 **دليل الاختبار اليدوي الشامل**

## 🚀 **تشغيل السيرفرات**

### **1. Backend Server**
```bash
cd /opt/lampp/htdocs/FixZone/backend
node server.js
```
**المنفذ:** http://localhost:4000  
**Health Check:** http://localhost:4000/health

### **2. Frontend Server**
```bash
cd /opt/lampp/htdocs/FixZone/frontend/react-app
PORT=3000 npm start
```
**المنفذ:** http://localhost:3000

---

## 📋 **خطة الاختبار اليدوي - 12 صفحة**

### **✅ الصفحة 1: InventoryPageEnhanced**
**الرابط:** http://localhost:3000/inventory

**الاختبارات:**
1. ✅ **Stats Dashboard:**
   - تحقق من ظهور إجمالي الأصناف
   - تحقق من قيمة المخزون
   - تحقق من التنبيهات (Low Stock, Out of Stock)

2. ✅ **قائمة الأصناف:**
   - عرض جميع الأصناف
   - البحث بالاسم
   - التصفية بالفئة
   - الترتيب

3. ✅ **إضافة صنف جديد:**
   - اضغط "إضافة صنف جديد"
   - املأ الحقول:
     * الاسم: "صنف اختبار"
     * رمز الصنف: "TEST001"
     * سعر الشراء: 100
     * سعر البيع: 150
     * الحد الأدنى: 10
     * الحد الأقصى: 100
   - اضغط "حفظ"
   - تحقق من الإشعار الناجح
   - تحقق من ظهور الصنف في القائمة

4. ✅ **تعديل صنف:**
   - اضغط على أيقونة التعديل
   - غير السعر
   - احفظ
   - تحقق من التحديث

5. ✅ **حذف صنف:**
   - اضغط على أيقونة الحذف
   - أكد الحذف
   - تحقق من الإشعار

---

### **✅ الصفحة 2: StockMovementPage**
**الرابط:** http://localhost:3000/stock-movement

**الاختبارات:**
1. ✅ **عرض الحركات:**
   - تحقق من ظهور قائمة الحركات
   - تحقق من التواريخ
   - تحقق من الأنواع (in, out, transfer, adjustment)

2. ✅ **تصفية الحركات:**
   - صفي حسب النوع
   - صفي حسب التاريخ
   - صفي حسب المخزن

3. ✅ **إضافة حركة جديدة:**
   - اضغط "إضافة حركة"
   - اختر النوع: "in"
   - اختر الصنف
   - أدخل الكمية: 50
   - اختر المخزن
   - احفظ
   - تحقق من الإشعار

4. ✅ **الرسوم البيانية:**
   - تحقق من ظهور Chart للحركات

---

### **✅ الصفحة 3: StockCountPage**
**الرابط:** http://localhost:3000/stock-count

**الاختبارات:**
1. ✅ **عرض الجردات:**
   - تحقق من قائمة الجردات
   - تحقق من الحالات (draft, in_progress, completed, approved)

2. ✅ **إنشاء جرد جديد:**
   - اضغط "جرد جديد"
   - اختر المخزن
   - اختر التاريخ
   - اختر النوع: "full"
   - أدخل ملاحظات
   - احفظ
   - تحقق من الإشعار

3. ✅ **إضافة أصناف للجرد:**
   - افتح الجرد
   - اضغط "إضافة صنف"
   - اختر الصنف
   - أدخل الكمية المعدودة
   - احفظ

4. ✅ **تغيير حالة الجرد:**
   - اضغط "بدء الجرد" → يتحول لـ in_progress
   - اضغط "إكمال" → يتحول لـ completed
   - اضغط "موافقة" → يتحول لـ approved
   - تحقق من تحديث المخزون

5. ✅ **الإحصائيات:**
   - تحقق من Stats Cards
   - إجمالي الجردات
   - الفروقات

---

### **✅ الصفحة 4: StockTransferPage**
**الرابط:** http://localhost:3000/stock-transfer

**الاختبارات:**
1. ✅ **عرض النقلات:**
   - تحقق من قائمة النقلات
   - تحقق من الحالات (pending, approved, in_transit, completed)

2. ✅ **إنشاء نقل جديد:**
   - اضغط "نقل جديد"
   - اختر المخزن المرسل
   - اختر المخزن المستقبل
   - اختر التاريخ
   - أضف أصناف:
     * اختر الصنف
     * أدخل الكمية
   - احفظ
   - تحقق من الإشعار

3. ✅ **تغيير حالة النقل:**
   - اضغط "موافقة" → approved
   - اضغط "شحن" → in_transit
   - اضغط "استلام" → completed
   - تحقق من تحديث المخزون في كلا المخزنين

4. ✅ **حذف نقل:**
   - اضغط حذف على نقل في حالة pending
   - أكد الحذف
   - تحقق من الإشعار

---

### **✅ الصفحة 5: StockAlertsPageEnhanced**
**الرابط:** http://localhost:3000/stock-alerts

**الاختبارات:**
1. ✅ **Stats Cards:**
   - تحقق من إجمالي التنبيهات
   - تحقق من Low Stock
   - تحقق من Out of Stock
   - تحقق من Critical Alerts

2. ✅ **Tabs:**
   - اضغط "الكل" - عرض جميع التنبيهات
   - اضغط "حرج" - فقط Critical
   - اضغط "منخفض" - فقط Low Stock
   - اضغط "منتهي" - فقط Out of Stock

3. ✅ **قائمة التنبيهات:**
   - تحقق من ظهور الأصناف
   - تحقق من الكميات الحالية
   - تحقق من الحد الأدنى
   - تحقق من مستوى الخطر

4. ✅ **إجراءات سريعة:**
   - اضغط "طلب" لطلب المزيد
   - اضغط "عرض" لعرض تفاصيل الصنف

---

### **✅ الصفحة 6: PartsUsageReportPage**
**الرابط:** http://localhost:3000/parts-usage-report

**الاختبارات:**
1. ✅ **Stats Cards:**
   - إجمالي القطع المستخدمة
   - إجمالي الكمية
   - إجمالي القيمة

2. ✅ **Recharts:**
   - تحقق من ظهور Bar Chart
   - تحقق من البيانات
   - تحقق من الألوان

3. ✅ **التصفية:**
   - صفي حسب تاريخ البداية
   - صفي حسب تاريخ النهاية
   - اضغط "بحث"
   - تحقق من تحديث البيانات

4. ✅ **قائمة القطع:**
   - تحقق من عرض القطع المستخدمة
   - تحقق من الكميات
   - تحقق من التكلفة

---

### **✅ الصفحة 7: WarehousesPage**
**الرابط:** http://localhost:3000/warehouses

**الاختبارات:**
1. ✅ **عرض المخازن:**
   - تحقق من قائمة المخازن
   - تحقق من الأسماء والعناوين

2. ✅ **إضافة مخزن:**
   - اضغط "مخزن جديد"
   - أدخل الاسم
   - أدخل العنوان
   - احفظ
   - تحقق من الإشعار

3. ✅ **تعديل مخزن:**
   - اضغط تعديل
   - غير البيانات
   - احفظ

---

### **✅ الصفحة 8: VendorsPage**
**الرابط:** http://localhost:3000/vendors

**الاختبارات:**
1. ✅ **عرض الموردين:**
   - تحقق من قائمة الموردين
   - تحقق من البيانات

2. ✅ **إضافة مورد:**
   - اضغط "مورد جديد"
   - أدخل الاسم
   - أدخل البريد والهاتف
   - احفظ

3. ✅ **ربط مورد بصنف:**
   - افتح صفحة صنف
   - اضغط "إدارة الموردين"
   - أضف مورد
   - حدد السعر المخصص
   - احفظ

---

### **✅ الصفحة 9: CategoriesPage**
**الرابط:** مُدمجة في النظام الأساسي

**الاختبارات:**
1. ✅ **API Test:**
   ```bash
   curl http://localhost:4000/api/inventory-enhanced/categories
   ```
   - تحقق من عودة 6 فئات

---

### **🆕 الصفحة 10: BarcodeScannerPage**
**الرابط:** http://localhost:3000/barcode-scanner

**الاختبارات:**
1. ✅ **Stats Cards:**
   - إجمالي المسح
   - المسح الناجح
   - غير موجود
   - أصناف فريدة

2. ✅ **مسح باركود:**
   - أدخل باركود يدوياً (استخدم SKU موجود)
   - اختر نوع المسح: "lookup"
   - اضغط "مسح"
   - تحقق من ظهور النتيجة:
     * اسم الصنف
     * رمز الصنف
     * الكمية المتاحة
     * السعر

3. ✅ **مسح باركود غير موجود:**
   - أدخل باركود عشوائي
   - اضغط "مسح"
   - تحقق من رسالة "الصنف غير موجود"

4. ✅ **سجل المسح:**
   - اضغط "السجل"
   - تحقق من ظهور آخر عمليات المسح
   - تحقق من التواريخ والنتائج

5. ✅ **إحصائيات:**
   - تحقق من تحديث الإحصائيات بعد كل مسح

---

### **🆕 الصفحة 11: ImportExportPage**
**الرابط:** http://localhost:3000/import-export

**الاختبارات:**

#### **Tab 1: استيراد**
1. ✅ **تحميل قالب:**
   - اضغط "تحميل قالب Excel"
   - تحقق من تحميل ملف `inventory_template.xlsx`
   - افتح الملف وتحقق من الأعمدة

2. ✅ **رفع ملف:**
   - عدل القالب وأضف 3 أصناف جديدة
   - احفظ الملف
   - اضغط "اختر ملف"
   - اختر الملف
   - تحقق من معاينة البيانات (أول 10 صفوف)

3. ✅ **التحقق من البيانات:**
   - رفع ملف بدون حقل "name"
   - تحقق من ظهور أخطاء التحقق
   - تحقق من رسالة الخطأ التفصيلية

4. ✅ **استيراد البيانات:**
   - رفع ملف صحيح
   - اضغط "استيراد البيانات"
   - انتظر الـ Progress Bar
   - تحقق من رسالة النجاح
   - تحقق من عدد الأصناف المستوردة
   - افتح صفحة المخزون وتحقق من الأصناف الجديدة

#### **Tab 2: تصدير**
1. ✅ **تصدير البيانات:**
   - اضغط Tab "تصدير"
   - اضغط "تصدير جميع الأصناف"
   - انتظر الـ Progress Bar
   - تحقق من تحميل ملف Excel
   - افتح الملف وتحقق من البيانات
   - تحقق من جميع الأعمدة

---

### **🆕 الصفحة 12: Batch Operations (Component)**
**الموقع:** داخل InventoryPageEnhanced

**الاختبارات:**
1. ✅ **تفعيل Multi-select:**
   - اذهب إلى صفحة المخزون
   - حدد 3 أصناف من القائمة (Checkbox)
   - اضغط "عمليات جماعية"

2. ✅ **تحديث الأسعار:**
   - اختر العملية: "تحديث الأسعار"
   - أدخل السعر الجديد: 200
   - اضغط "تنفيذ"
   - تحقق من تحديث جميع الأصناف المحددة

3. ✅ **تغيير الفئة:**
   - حدد 2 صنف
   - اختر "تغيير الفئة"
   - أدخل الفئة الجديدة: "إلكترونيات"
   - اضغط "تنفيذ"
   - تحقق من التحديث

4. ✅ **تفعيل جماعي:**
   - حدد أصناف
   - اختر "تفعيل"
   - اضغط "تنفيذ"
   - تحقق من تفعيل جميع الأصناف

5. ✅ **حذف جماعي:**
   - حدد أصناف اختبارية
   - اختر "حذف"
   - تحقق من رسالة التحذير
   - أكد الحذف
   - تحقق من حذف الأصناف

---

## 🔌 **اختبار Backend APIs مباشرة**

### **1. Inventory APIs**
```bash
# Stats
curl http://localhost:4000/api/inventory-enhanced/stats | jq '.'

# List Items
curl http://localhost:4000/api/inventory-enhanced/items | jq '.data.items | length'

# Categories
curl http://localhost:4000/api/inventory-enhanced/categories | jq '.'
```

### **2. Stock Count APIs**
```bash
# Stats
curl http://localhost:4000/api/stock-count/stats | jq '.'

# List
curl http://localhost:4000/api/stock-count | jq '.data.stockCounts | length'
```

### **3. Stock Transfer APIs**
```bash
# Stats
curl http://localhost:4000/api/stock-transfer/stats | jq '.'

# List
curl http://localhost:4000/api/stock-transfer | jq '.data.transfers | length'
```

### **4. Stock Alerts APIs**
```bash
# All Alerts
curl http://localhost:4000/api/stock-alerts | jq '. | length'

# Low Stock
curl http://localhost:4000/api/stock-alerts/low | jq '.totalAlerts'

# Reorder Suggestions
curl http://localhost:4000/api/stock-alerts/reorder-suggestions | jq '.totalItems'
```

### **5. Barcode APIs** 🆕
```bash
# Stats
curl http://localhost:4000/api/barcode/stats | jq '.'

# Scan (استخدم SKU موجود)
curl -X POST http://localhost:4000/api/barcode/scan \
  -H "Content-Type: application/json" \
  -d '{"barcode":"ITEM001","scanType":"lookup"}' | jq '.'

# History
curl http://localhost:4000/api/barcode/history | jq '.data.scans | length'
```

### **6. Parts Used APIs**
```bash
# List
curl http://localhost:4000/api/partsused | jq '. | length'

# Consumption Report
curl http://localhost:4000/api/partsused/reports/consumption | jq '.'
```

---

## ✅ **Checklist الاختبار الشامل**

### **Database (14 جدول)**
- [ ] InventoryItem
- [ ] InventoryItemCategory
- [ ] InventoryItemVendor
- [ ] Vendor
- [ ] Warehouse
- [ ] StockLevel
- [ ] StockMovement
- [ ] StockAlert
- [ ] StockCount
- [ ] StockCountItem
- [ ] StockTransfer
- [ ] StockTransferItem
- [ ] PartsUsed
- [ ] BarcodeScan

### **Backend APIs (53 API)**
- [ ] Inventory Core (15)
- [ ] Stock Count (7)
- [ ] Stock Transfer (7)
- [ ] Stock Alerts (6)
- [ ] Barcode (6)
- [ ] Parts Used (4)
- [ ] Others (8)

### **Frontend Pages (12 صفحة)**
- [ ] InventoryPageEnhanced
- [ ] StockMovementPage
- [ ] StockCountPage
- [ ] StockTransferPage
- [ ] StockAlertsPageEnhanced
- [ ] PartsUsageReportPage
- [ ] WarehousesPage
- [ ] VendorsPage
- [ ] CategoriesPage
- [ ] BarcodeScannerPage 🆕
- [ ] ImportExportPage 🆕
- [ ] Batch Operations 🆕

---

## 🎯 **نصائح الاختبار**

### **1. بيانات الدخول:**
```
Email: admin@fixzone.com
Password: admin123
```

### **2. ترتيب الاختبار المُوصى به:**
1. ابدأ بـ InventoryPageEnhanced (أضف بيانات)
2. ثم StockMovementPage
3. ثم StockCountPage
4. ثم StockTransferPage
5. ثم الصفحات المتقدمة

### **3. نقاط التحقق المهمة:**
- ✅ الإشعارات تظهر بعد كل عملية
- ✅ البيانات تتحدث فوراً
- ✅ Loading Spinners تظهر أثناء التحميل
- ✅ رسائل الخطأ واضحة
- ✅ الـ Charts تعمل
- ✅ التصفية والبحث يعملان
- ✅ التحديثات تنعكس على المخزون

---

## 📊 **النتيجة المتوقعة**

```
✅ جميع الصفحات تعمل: 12/12
✅ جميع APIs تستجيب: 53/53
✅ جميع الميزات تعمل: 100%
✅ النظام مستقر وسريع
✅ جاهز للإنتاج
```

---

**تم إعداد الدليل:** 9 أكتوبر 2025 - 01:20 AM  
**الحالة:** ✅ جاهز للاختبار

**🎉 بالتوفيق في الاختبار! 🎉**

