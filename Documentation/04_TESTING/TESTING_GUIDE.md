# 🧪 دليل تشغيل اختبارات موديول المدفوعات

## 📋 نظرة عامة
هذا الدليل يوضح كيفية تشغيل جميع أنواع الاختبارات لموديول المدفوعات في نظام FixZone ERP.

---

## 🚀 البدء السريع

### **1. إعداد البيئة:**
```bash
# الانتقال إلى مجلد الخادم الخلفي
cd backend

# تثبيت المتطلبات
npm install

# تشغيل الخادم
node server.js
```

### **2. تشغيل جميع الاختبارات:**
```bash
# تشغيل جميع الاختبارات
node run-all-tests.js

# أو استخدام npm script
npm run test:all
```

---

## 🧪 أنواع الاختبارات

### **1. اختبارات الوظائف الأساسية:**
```bash
# تشغيل اختبارات الوظائف
node test-payments.js

# أو
npm run test:functional
```

**ما يتم اختباره:**
- ✅ إنشاء مدفوعة جديدة
- ✅ تعديل مدفوعة موجودة
- ✅ حذف مدفوعة
- ✅ عرض تفاصيل المدفوعة
- ✅ ربط المدفوعة بالفاتورة
- ✅ حساب الرصيد المتبقي

### **2. اختبارات الأمان:**
```bash
# تشغيل اختبارات الأمان
node security-test.js

# أو
npm run test:security
```

**ما يتم اختباره:**
- 🔒 SQL Injection
- 🔒 XSS (Cross-Site Scripting)
- 🔒 CSRF (Cross-Site Request Forgery)
- 🔒 التحقق من الصلاحيات
- 🔒 تشفير البيانات
- 🔒 Rate Limiting

### **3. اختبارات الأداء:**
```bash
# تشغيل اختبارات الأداء
node performance-test.js

# أو
npm run test:performance
```

**ما يتم اختباره:**
- ⚡ وقت الاستجابة
- ⚡ اختبار الضغط
- ⚡ اختبار الاستقرار
- ⚡ استخدام الذاكرة
- ⚡ التحميل المتدرج

### **4. اختبارات التكامل:**
```bash
# تشغيل اختبارات التكامل
node run-all-tests.js
```

**ما يتم اختباره:**
- 🔗 التكامل مع قاعدة البيانات
- 🔗 التكامل مع APIs
- 🔗 التكامل مع الواجهة الأمامية

### **5. اختبارات تجربة المستخدم:**
```bash
# تشغيل اختبارات UX
npm run test:ux
```

**ما يتم اختباره:**
- 🎨 التصميم المتجاوب
- ♿ إمكانية الوصول
- 🎯 سهولة الاستخدام

---

## 📊 تفسير النتائج

### **النتائج الناجحة:**
```
✅ إنشاء مدفوعة جديدة: PASSED
✅ الحصول على قائمة المدفوعات: PASSED
✅ التكامل مع الفواتير: PASSED
```

### **النتائج الفاشلة:**
```
❌ إنشاء مدفوعة جديدة: FAILED - خطأ في قاعدة البيانات
❌ SQL Injection: ضعيف - يجب تنظيف المدخلات
❌ وقت الاستجابة: بطيء جداً: 5000ms
```

### **معدل النجاح:**
```
📊 النتائج الإجمالية:
إجمالي الاختبارات: 50
✅ نجح: 45
❌ فشل: 5
📈 معدل النجاح: 90.00%
```

---

## 🔧 إصلاح المشاكل الشائعة

### **1. مشاكل الاتصال:**
```bash
# خطأ: ECONNREFUSED
# الحل: تأكد من تشغيل الخادم
node server.js
```

### **2. مشاكل قاعدة البيانات:**
```bash
# خطأ: ER_ACCESS_DENIED_ERROR
# الحل: تحقق من إعدادات قاعدة البيانات
mysql -u root -p
```

### **3. مشاكل التوكن:**
```bash
# خطأ: 401 Unauthorized
# الحل: تحقق من صحة بيانات المستخدم
```

### **4. مشاكل الذاكرة:**
```bash
# خطأ: JavaScript heap out of memory
# الحل: زيادة حد الذاكرة
node --max-old-space-size=4096 run-all-tests.js
```

---

## 📈 تحسين الأداء

### **1. تشغيل الاختبارات بالتوازي:**
```bash
# تشغيل اختبارات متعددة في نفس الوقت
node test-payments.js &
node security-test.js &
node performance-test.js &
wait
```

### **2. تشغيل اختبارات محددة:**
```bash
# اختبار الوظائف فقط
node test-payments.js --functional

# اختبار الأمان فقط
node security-test.js --security

# اختبار الأداء فقط
node performance-test.js --performance
```

### **3. تشغيل الاختبارات في الخلفية:**
```bash
# تشغيل في الخلفية
nohup node run-all-tests.js > test-results.log 2>&1 &

# مراقبة النتائج
tail -f test-results.log
```

---

## 🎯 اختبارات مخصصة

### **1. اختبار بيانات محددة:**
```javascript
// في test-payments.js
const customTestData = {
  invoices: [1, 2, 3],
  customers: [1, 2, 3],
  payments: []
};

const tester = new PaymentsTester();
tester.testData = customTestData;
await tester.runAllTests();
```

### **2. اختبار مع إعدادات محددة:**
```javascript
// في performance-test.js
const customConfig = {
  concurrentUsers: 100,
  requestsPerUser: 20,
  duration: 120000
};

const tester = new PerformanceTester();
tester.config = customConfig;
await tester.runAllPerformanceTests();
```

### **3. اختبار مع بيانات أمنية محددة:**
```javascript
// في security-test.js
const customPayloads = [
  "'; DROP TABLE Payment; --",
  "<script>alert('XSS')</script>",
  "admin' OR '1'='1"
];

const tester = new SecurityTester();
tester.customPayloads = customPayloads;
await tester.runAllSecurityTests();
```

---

## 📋 قائمة التحقق

### **قبل تشغيل الاختبارات:**
- [ ] الخادم الخلفي يعمل
- [ ] قاعدة البيانات متاحة
- [ ] المستخدم التجريبي موجود
- [ ] البيانات التجريبية محضرة
- [ ] الشبكة تعمل بشكل صحيح

### **أثناء تشغيل الاختبارات:**
- [ ] مراقبة استخدام الذاكرة
- [ ] مراقبة استخدام المعالج
- [ ] مراقبة اتصالات الشبكة
- [ ] تسجيل الأخطاء
- [ ] حفظ النتائج

### **بعد تشغيل الاختبارات:**
- [ ] مراجعة النتائج
- [ ] إصلاح المشاكل المكتشفة
- [ ] إعادة تشغيل الاختبارات الفاشلة
- [ ] إنشاء تقرير نهائي
- [ ] تنظيف البيانات التجريبية

---

## 🚨 استكشاف الأخطاء

### **1. اختبارات الوظائف فشلت:**
```bash
# تحقق من الخادم
curl http://localhost:4000/api/payments

# تحقق من قاعدة البيانات
mysql -u root -p -e "SELECT * FROM Payment LIMIT 5;"
```

### **2. اختبارات الأمان فشلت:**
```bash
# تحقق من إعدادات الأمان
grep -r "sanitize" backend/
grep -r "escape" backend/
```

### **3. اختبارات الأداء فشلت:**
```bash
# تحقق من استخدام الموارد
top -p $(pgrep node)
netstat -an | grep 4000
```

### **4. اختبارات التكامل فشلت:**
```bash
# تحقق من الاتصال
ping localhost
telnet localhost 4000
```

---

## 📊 تقارير الاختبار

### **1. تقرير يومي:**
```bash
# تشغيل الاختبارات اليومية
node run-all-tests.js > daily-report-$(date +%Y%m%d).log
```

### **2. تقرير أسبوعي:**
```bash
# تشغيل الاختبارات الأسبوعية
node run-all-tests.js --weekly > weekly-report-$(date +%Y%m%d).log
```

### **3. تقرير شهري:**
```bash
# تشغيل الاختبارات الشهرية
node run-all-tests.js --monthly > monthly-report-$(date +%Y%m%d).log
```

---

## 🔄 التكامل المستمر

### **1. مع GitHub Actions:**
```yaml
name: Payments Module Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm run test:all
```

### **2. مع Jenkins:**
```groovy
pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'npm run test:all'
            }
        }
    }
}
```

### **3. مع Docker:**
```dockerfile
FROM node:16
WORKDIR /app
COPY . .
RUN npm install
CMD ["npm", "run", "test:all"]
```

---

## 📞 الدعم والمساعدة

### **للحصول على المساعدة:**
- 📧 البريد الإلكتروني: support@fixzone.com
- 📱 الهاتف: +20 123 456 7890
- 🌐 الموقع: www.fixzone.com
- 💬 الدردشة: متاحة 24/7

### **للمساهمة في التحسين:**
- 🐛 الإبلاغ عن الأخطاء: GitHub Issues
- 💡 اقتراح تحسينات: GitHub Discussions
- 🔧 المساهمة في الكود: Pull Requests

---

## 📚 المراجع

### **الوثائق:**
- [دليل المطور](DEVELOPMENT_GUIDE.md)
- [دليل API](API_DOCUMENTATION.md)
- [دليل قاعدة البيانات](DATABASE_README.md)

### **الأدوات:**
- [Jest](https://jestjs.io/) - اختبارات JavaScript
- [Supertest](https://github.com/visionmedia/supertest) - اختبارات HTTP
- [Artillery](https://artillery.io/) - اختبارات الضغط

### **المعايير:**
- [OWASP](https://owasp.org/) - أمان التطبيقات
- [PCI DSS](https://www.pcisecuritystandards.org/) - أمان الدفع
- [WCAG](https://www.w3.org/WAI/WCAG21/) - إمكانية الوصول

---

*تم إنشاء هذا الدليل في: ديسمبر 2024*  
*آخر تحديث: ديسمبر 2024*  
*الإصدار: 1.0.0* 🚀


