# ✅ UI/UX Roadmap & TODOs – FixZone ERP

هذه الخطة العملية تغطي أولويات التنفيذ القادمة لواجهات FixZone، مع تقسيم واضح للمهام، القبول، والروابط ذات الصلة.

---

## 🔝 الأولويات (Roadmap Priorities)

1) الأداء وقابلية التوسع (High)
- Pagination/Infinite Loading في DataView لصفحة RepairsPage.
- Server-side params: page, pageSize, sort, filters.
- Caching بسيط لتقليل نداءات الشبكة.

2) فلترة متقدمة وبحث فوري (High)
- Faceted Filters: الحالة، الأولوية، الفني، التاريخ.
- حفظ الفلاتر في URL (query params) + استرجاع تلقائي.

3) صفحة تفاصيل طلب الإصلاح (High)
- تبويبات: الحالة/السجل/المرفقات/الفواتير/التعليقات.
- طباعة إيصال، ملصق/QR، إجراءات سريعة (تحديث حالة/إسناد فني).

4) توحيد الإجراءات المجمّعة (Medium)
- واجهة موحدة عبر Repairs/Inventory/Invoices + إشعارات متناسقة.

5) تحسينات الحركة (Medium)
- اعتماد Framer Motion لانتقالات Dropdown/Modal المعقدة عند الحاجة.

6) تعميم DataView (Medium)
- نقل المزايا الجديدة إلى Inventory وInvoices.

7) الطباعة والتقارير (Medium)
- قوالب PDF/Excel للفحص/التسليم/الفواتير.

8) تحسين المحمول والتجربة اللمسية (Medium)
- مسافات ولمس أفضل وتثبيت شريط الإجراءات.

9) الإتاحة (Ongoing)
- مراجعة ARIA/Keyboard/Announcements للقوائم والإشعارات.

---

## 🎯 المرحلة 1 — Pagination + URL Params + Sorting (RepairsPage)

- المهام
  - [x] (FE) إضافة حالة page/pageSize في RepairsPage متزامنة مع URL.
  - [x] (FE) تمرير المعلمات إلى apiService.getRepairs(params) – دون كسر التوافق.
  - [ ] (BE) دعم `GET /api/repairs?page=&pageSize=&status=&sort=&order=&q=` (إن لم يكن مدعومًا، مؤقتًا Client-side paginate).
  - [x] (FE) عناصر تحكم للصفحات: أزرار السابق/التالي + اختيار عدد الصفوف/صفحة.
  - [x] (FE) Skeleton/Loading أثناء الجلب والانتقال بين الصفحات.
  - [x] (FE) إضافة الفرز Sort مع مزامنة URL وتمرير المعلمات (`sort`,`order`) إلى API.
  - [x] (FE) دعم استجابة الخادم `{ items, total }` مع عرض العدد الإجمالي الصحيح في التذييل.
  - [ ] (QA) اختبار مزامنة URL والتنقل المباشر + حالات الفرز.
- القبول
  - تغيير الصفحة/الحجم يحدّث URL ويعيد تحميل البيانات بسلاسة.
  - الرجوع/التحديث يحافظ على الحالة نفسها.

## 🎯 المرحلة 2 — Faceted Filters + بحث فوري

- المهام
  - [ ] (FE) إضافة فلاتر (الأولوية، الفني، التاريخ) مع Dropdowns عملية.
  - [ ] (FE) مزامنة كل فلتر مع URL + LocalStorage fallback.
  - [x] (FE) البحث الفوري debounced + مزامنة `q` في URL وتمريره للخادم.
  - [ ] (BE) دعم المعلمات المقابلة.
  - [ ] (QA) اختبار الدمج مع Pagination.
- القبول
  - كل فلتر يعكس في URL ويسترجع تلقائيًا.
  - النتائج تتحدث فورًا بدون وميض.

## 🎯 المرحلة 3 — صفحة تفاصيل طلب الإصلاح

- المهام
  - [ ] (FE) إنشاء صفحة `RepairDetailsPage` مع تبويبات:
    - [ ] الحالة (Status): عرض الحالة الحالية + زر تغيير الحالة مع تأكيد.
    - [ ] السجل (Timeline/Logs): استخدام `RepairTimeline` لعرض الأحداث.
    - [ ] المرفقات (Attachments): رفع/عرض/حذف ملفات؛ سحب وإفلات + معاينة.
    - [ ] الفواتير (Invoices): ربط/عرض فواتير الطلب + إنشاء/طباعة.
    - [ ] التعليقات/الملاحظات (Notes): قائمة ملاحظات مع إضافة/حذف.
  - [ ] (FE) رأس الصفحة (Header): معلومات أساسية + أزرار سريعة (تحديث حالة، إسناد فني، طباعة إيصال/QR، حذف).
  - [x] (FE) ربط أولي بالـ API: جلب التفاصيل/السجل/المرفقات مع Fallback للبيانات التجريبية.
  - [ ] (FE) دعم RTL بالكامل + Skeleton للحمل الأولي لكل تبويب.
  - [ ] (FE) إشعارات نجاح/خطأ متسقة عبر نظام الإشعارات الحالي.
  - [x] (FE) توسيع `apiService`: سجلات، إسناد فني، مرفقات (قائمة/رفع/حذف)، فواتير الطلب (قائمة/إنشاء)، طباعة.
  - [ ] (FE) تبويبات فعلية + أزرار سريعة مع حوارات تأكيد.
  - [ ] (BE) Endpoints مساندة:
    - [ ] `GET /api/repairs/:id` تفاصيل الطلب.
    - [ ] `GET /api/repairs/:id/logs` السجل الزمني.
    - [ ] `POST /api/repairs/:id/status` تغيير الحالة.
    - [ ] `POST /api/repairs/:id/assign` تعيين/تغيير الفني.
    - [ ] `GET/POST/DELETE /api/repairs/:id/attachments` إدارة المرفقات.
    - [ ] `GET /api/repairs/:id/invoices` + `POST /api/repairs/:id/invoices`.
    - [ ] `GET /api/repairs/:id/print/(receipt|sticker)` توليد الإيصال/الملصق.
  - [ ] (QA) تدفقات كاملة من/إلى `RepairsPage`:
    - [ ] فتح التفاصيل من جميع أنماط العرض في `DataView`.
    - [ ] التحقق من الحفظ/التراجع/التأكيد للممارسات الحساسة.
    - [ ] تغطية حالات فارغة وأخطاء الشبكة.
- القبول
  - تبويبات سلسة وسريعة مع Skeleton وتحميل كسول.
  - كل إجراء حساس (تغيير حالة/إسناد/حذف) يتطلب تأكيد ويُظهر إشعارًا.
  - المرفقات تدعم سحب-وإفلات ومعاينة آمنة.
  - الطباعة تعمل وتولّد ملفات صحيحة مع بيانات الطلب.

## 🎯 المرحلة 4 — تعميم DataView + الإجراءات المجمّعة

- المهام
  - [ ] (FE) نقل `DataView` المحسّن إلى:
    - [ ] Inventory: دعم أنماط العرض الأربعة + إخفاء/إظهار الأعمدة + البحث والفلاتر الأساسية.
    - [ ] Invoices: دعم أنماط العرض + فلاتر الحالة (مسودة/مرسلة/مدفوعة) + البحث.
  - [ ] (FE) توحيد شريط الإجراءات المجمّعة ضمن مكون `BulkActions`:
    - [ ] Inventory: تفعيل/إلغاء تفعيل/تعديل مخزون/حذف/تصدير.
    - [ ] Invoices: إرسال/طباعة/تصدير/حذف.
  - [ ] (FE) حفظ التفضيلات (وضع العرض/الأعمدة المرئية) لكل صفحة في `localStorage` بمفاتيح خاصة بالمسار.
  - [ ] (FE) تحسين الإتاحة: تنقل لوحة المفاتيح، ARIA roles، إعلانات عند تغيير الفلاتر/العرض.
  - [ ] (BE) دعم معلمات الترقيم/الفرز/البحث عبر `GET /api/inventory` و`GET /api/invoices` مع `{ items, total }`.
  - [ ] (QA) تحقق من التناسق:
    - [ ] نفس أنماط العرض والاختصارات وسلوك القوائم المنسدلة في كل الصفحات.
    - [ ] استرجاع التفضيلات بعد إعادة التحميل.
- القبول
  - تجربة موحدة عبر كل القوائم مع سلوك متسق للفلاتر، الفرز، الإجراءات المجمّعة.
  - المقاييس والأزرار تتجاوب بسرعة وتغلق القوائم عند النقر خارجها.

---

## 🔗 روابط ومستندات
- UI_UX_Plan.md — الخطة العامة والتوثيق.
- erp_fix_zone_project_details.md — تفاصيل الموديولات والأهداف.

---

ملاحظة: سنحدّث هذا الملف دوريًا مع الحالة والتقدم والروابط إلى PRs/Commits.
