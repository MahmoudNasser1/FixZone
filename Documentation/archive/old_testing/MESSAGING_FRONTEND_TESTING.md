# دليل اختبار Frontend - نظام المراسلة

## 📋 خطوات الاختبار التفاعلي

### ✅ 1. إصلاح الأخطاء
- ✅ تم إصلاح خطأ في `RepairDetailsPage.js` (شرط مكرر)

---

### 🧪 2. اختبار صفحة مركز الرسائل

**الرابط:** `http://localhost:3000/messaging`

**الخطوات:**
1. سجل الدخول أولاً
2. افتح `/messaging`
3. ✅ تحقق من ظهور الإحصائيات (إجمالي، مرسلة، فاشلة، معدل النجاح)
4. ✅ جرب الفلترة (نوع الكيان، القناة، الحالة)
5. ✅ تحقق من ظهور قائمة الرسائل
6. ✅ اضغط على رسالة لعرض التفاصيل
7. ✅ جرب إعادة المحاولة (إذا كانت هناك رسالة فاشلة)

---

### 🧪 3. اختبار صفحة الفاتورة

**الرابط:** `http://localhost:3000/invoices/:id` (استبدل :id برقم فاتورة حقيقي)

**الخطوات:**
1. افتح صفحة فاتورة
2. ✅ تحقق من ظهور زر الإرسال في Header (مع ChannelSelector)
3. ✅ تحقق من ظهور زر الإرسال في Quick Actions (بدون ChannelSelector)
4. ✅ اضغط على زر الإرسال
5. ✅ اختر WhatsApp
6. ✅ اضغط "إرسال"
7. ✅ يجب أن يفتح WhatsApp Web مع الرسالة
8. ✅ تحقق من ظهور MessageLogViewer في نهاية الصفحة
9. ✅ تحقق من ظهور الرسالة في السجل

---

### 🧪 4. اختبار صفحة طلب الإصلاح

**الرابط:** `http://localhost:3000/repairs/:id`

**الخطوات:**
1. افتح صفحة طلب إصلاح
2. ✅ تحقق من ظهور زر الإرسال
3. ✅ اضغط على زر الإرسال
4. ✅ يجب أن يفتح WhatsApp Web مع رسالة "تم الاستلام"
5. ✅ تحقق من ظهور MessageLogViewer
6. ✅ تحقق من ظهور الرسالة في السجل

---

### 🧪 5. اختبار صفحة الدفعة

**الرابط:** `http://localhost:3000/payments/:id`

**الخطوات:**
1. افتح صفحة دفعة
2. ✅ تحقق من ظهور زر الإرسال
3. ✅ اضغط على زر الإرسال
4. ✅ يجب أن يفتح WhatsApp Web مع تذكير دفع
5. ✅ تحقق من ظهور MessageLogViewer

---

### 🧪 6. اختبار صفحة المدفوعات المتأخرة

**الرابط:** `http://localhost:3000/payments/overdue`

**الخطوات:**
1. افتح صفحة المدفوعات المتأخرة
2. ✅ تحقق من ظهور زر التذكير لكل دفعة
3. ✅ اضغط على زر التذكير
4. ✅ يجب أن يفتح WhatsApp Web مع تذكير

---

## 📱 7. اختبار إرسال فعلي

**الرقم المستخدم:** `01113511940`

**الخطوات:**
1. افتح صفحة فاتورة أو طلب إصلاح
2. اضغط على زر الإرسال
3. اختر WhatsApp
4. اضغط "إرسال"
5. ✅ يجب أن يفتح WhatsApp Web في نافذة جديدة
6. ✅ يجب أن تكون الرسالة جاهزة للإرسال
7. ✅ اضغط "إرسال" في WhatsApp Web
8. ✅ تحقق من وصول الرسالة للرقم `01113511940`

---

## ✅ قائمة التحقق

### المكونات
- [ ] MessageStatusBadge يظهر بشكل صحيح
- [ ] ChannelSelector يعمل (اختيار قناة)
- [ ] SendButton يعمل في جميع الصفحات
- [ ] MessageLogViewer يعرض السجل بشكل صحيح

### الصفحات
- [ ] صفحة مركز الرسائل (`/messaging`)
- [ ] صفحة الفاتورة (`/invoices/:id`)
- [ ] صفحة طلب الإصلاح (`/repairs/:id`)
- [ ] صفحة الدفعة (`/payments/:id`)
- [ ] صفحة المدفوعات المتأخرة (`/payments/overdue`)

### الوظائف
- [ ] إرسال فاتورة عبر WhatsApp
- [ ] إرسال إشعار طلب إصلاح
- [ ] إرسال تذكير دفع
- [ ] عرض سجل المراسلات
- [ ] إعادة محاولة رسالة فاشلة
- [ ] فلترة في صفحة مركز الرسائل

---

## 🐛 المشاكل المحتملة

### 1. خطأ في Build
- ✅ تم إصلاح خطأ في `RepairDetailsPage.js`
- إذا ظهرت أخطاء أخرى، تحقق من:
  - الأخطاء في Console
  - الأخطاء في Terminal (React Dev Server)

### 2. زر الإرسال لا يظهر
- تحقق من وجود رقم هاتف للعميل
- تحقق من تفعيل WhatsApp في الإعدادات

### 3. WhatsApp Web لا يفتح
- تحقق من أن WhatsApp Web مفتوح في المتصفح
- تحقق من أن الرقم صحيح

---

**تاريخ الاختبار:** 2025-01-12
**الحالة:** ✅ جاهز للاختبار اليدوي

