# 📘 توثيق قاعدة بيانات نظام Fix Zone ERP

## 🚀 التحسينات المنطقية والممارسات الفضلى (Logical Enhancements & Best Practices)

تم اقتراح التحسينات التالية لتعزيز مرونة النظام، الأمان، وقابلية التوسع:

- **الحذف المنطقي (Soft Deletes):** إضافة حقل `deletedAt` في الجداول الأساسية (مثل User, Customer, RepairRequest, InventoryItem) لدعم حذف البيانات بشكل منطقي واستعادتها لاحقًا.
- **التدقيق (Auditability):** التأكد من تسجيل جميع العمليات الحرجة (خاصة المالية والمخزون) في سجل التدقيق AuditLog مع تفاصيل كافية (القيم قبل/بعد، معرفات الكيانات المرتبطة).
- **توحيد الطوابع الزمنية:** التأكد من وجود حقول `createdAt` و`updatedAt` في جميع الجداول.
- **تاريخ تسجيل الدخول للمستخدم (User Login History):** إضافة جدول `UserLoginLog` لتتبع عمليات دخول المستخدمين وعناوين IP والأجهزة.
- **جدول الأجهزة (Device Table):** فصل تفاصيل الجهاز في جدول مستقل `Device` إذا كان الجهاز قد يخضع لأكثر من طلب صيانة.
- **دعم المرفقات:** إضافة دعم للمرفقات (صور، مستندات) في طلبات الإصلاح وتقارير الفحص.
- **تنبيهات المخزون:** إضافة منطق أو حقل لحالة التنبيه عند انخفاض المخزون.
- **سجل استيراد دفعات الأجهزة:** إضافة سجل للأخطاء أو المشاكل أثناء استيراد دفعات الأجهزة.
- **دعم الحقول المخصصة (Custom Fields):** السماح بإضافة حقول ديناميكية لبعض الكيانات (مثل العميل، الجهاز، طلب الإصلاح).
- **دعم القنوات المتعددة للإشعارات:** إضافة حقل `channel` في جدول Notification (مثل: EMAIL, SMS, IN_APP).
- **قوالب الإشعارات:** إضافة جدول `NotificationTemplate` لتخزين قوالب الإشعارات المتكررة.
- **دعم العملات المتعددة:** التأكد من وجود حقل `currency` في جميع الجداول المالية.
- **تاريخ الموافقة على أوامر الشراء:** إضافة حقول لحالة الموافقة، من وافق، وتاريخ الموافقة في PurchaseOrder.
- **دعم الضرائب:** إضافة حقول للضريبة في الفواتير وعروض الأسعار إذا لزم الأمر.
- **تاريخ دفعة الأجهزة (DeviceBatch Status):** إضافة حقل حالة للدفعة (PENDING, COMPLETED).
- **تاريخ الفحص بالصور:** دعم رفع صور لكل مكون أثناء الفحص.
- **تسجيل الدخول المتعدد العوامل (MFA):** إضافة جداول لدعم المصادقة الثنائية إذا لزم الأمر.

---

## مقدمة

هذا المستند يوضح تصميم وهيكل قاعدة بيانات نظام Fix Zone ERP. تم بناء المخطط باستخدام Prisma وهو مصمم للعمل مع PostgreSQL. الهدف هو توفير نظام مرن وقابل للتطوير لإدارة عمليات مركز صيانة وإصلاح.

---

## مخطط علاقات الكيانات (ERD)

(سيتم إضافة رسم تخطيطي هنا لاحقًا لتوضيح العلاقات بشكل مرئي)

---

## قائمة الـ Models (الجداول)

### 1. الوحدات الأساسية (Core Entities)

#### `City` (المدن)
- **الغرض:** تعريف المدن التي تتواجد بها الفروع.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم المدينة (فريد).
  - `branches`: علاقة واحد إلى متعدد مع الفروع.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `Role` (الأدوار)
- **الغرض:** تحديد صلاحيات المستخدمين في النظام.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم الدور (e.g., "Admin", "Technician", "Receptionist").
  - `permissions`: كائن JSON لتخزين صلاحيات مفصلة ومرنة.
  - `users`: علاقة واحد إلى متعدد مع المستخدمين.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `parentRoleId`: لدعم التسلسل الهرمي للأدوار (Enhancement).
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `User` (المستخدمون)
- **الغرض:** تخزين معلومات حسابات المستخدمين والموظفين.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم المستخدم.
  - `email`: البريد الإلكتروني (فريد).
  - `password`: كلمة المرور (يجب تشفيرها).
  - `phone`: رقم الهاتف (فريد).
  - `isActive`: لتحديد ما إذا كان الحساب نشطًا.
  - `roleId`: مفتاح يربط المستخدم بدوره (`Role`).
  - `role`: علاقة متعدد إلى واحد مع Role.
  - `repairRequests`: علاقة واحد إلى متعدد مع RepairRequest (كفني).
  - `statusUpdateLogs`: علاقة واحد إلى متعدد مع StatusUpdateLog.
  - `notifications`: علاقة واحد إلى متعدد مع Notification.
  - `payments`: علاقة واحد إلى متعدد مع Payment.
  - `expenses`: علاقة واحد إلى متعدد مع Expense.
  - `auditLogs`: علاقة واحد إلى متعدد مع AuditLog.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `UserLoginLog` (سجل دخول المستخدم) (Enhancement)
- **الغرض:** تتبع عمليات دخول المستخدمين للنظام.
- **الحقول:**
  - `id`: معرف فريد.
  - `userId`: معرف المستخدم.
  - `loginAt`: تاريخ ووقت الدخول.
  - `ipAddress`: عنوان IP.
  - `deviceInfo`: معلومات الجهاز/المتصفح.

#### `Customer` (العملاء)
- **الغرض:** تخزين بيانات العملاء.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`, `phone`, `email`, `address`.
  - `repairRequests`: علاقة واحد إلى متعدد مع RepairRequest.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).
  - `customFields`: حقول مخصصة (JSON) (Enhancement).

#### `Branch` (الفروع)
- **الغرض:** إدارة فروع مركز الصيانة المختلفة.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`, `address`, `phone`.
  - `city`: علاقة متعدد إلى واحد مع City.
  - `repairRequests`: علاقة واحد إلى متعدد مع RepairRequest.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `Device` (الجهاز) (Enhancement)
- **الغرض:** تخزين بيانات كل جهاز بشكل مستقل عن طلبات الإصلاح.
- **الحقول:**
  - `id`: معرف فريد.
  - `customerId`: معرف العميل.
  - `deviceType`, `brand`, `model`, `serialNumber`: تفاصيل الجهاز.
  - `customFields`: حقول مخصصة (JSON).
  - `createdAt`, `updatedAt`, `deletedAt`.
  - **العلاقات:**
    - `repairRequests`: علاقة واحد إلى متعدد مع RepairRequest.

### 2. إدارة الإصلاحات (Repair Management)

#### `RepairRequest` (طلبات الإصلاح)
- **الغرض:** تتبع كل عملية إصلاح من البداية إلى النهاية.
- **الحقول:**
  - `id`: معرف فريد.
  - `deviceType`, `brand`, `model`, `serialNumber`: تفاصيل الجهاز.
  - `reportedProblem`, `technicianReport`: وصف المشكلة وتقرير الفني.
  - `status`: حالة الإصلاح الحالية (من `RepairStatus` enum).
  - `customerId`, `branchId`, `technicianId`: مفاتيح الربط بالعميل والفرع والفني.
  - `customer`: علاقة متعدد إلى واحد مع Customer.
  - `branch`: علاقة متعدد إلى واحد مع Branch.
  - `technician`: علاقة متعدد إلى واحد مع User.
  - `quotation`: علاقة واحد إلى واحد مع Quotation.
  - `statusUpdateLogs`: علاقة واحد إلى متعدد مع StatusUpdateLog.
  - `partsUsed`: علاقة واحد إلى متعدد مع PartsUsed.
  - `invoice`: علاقة واحد إلى واحد مع Invoice.
                                                                                                                                                                - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deviceId`: معرف الجهاز (Enhancement).
  - `deviceBatchId`: معرف دفعة الأجهزة (اختياري).
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).
  - `attachments`: مرفقات (صور/مستندات) (Enhancement).
  - `customFields`: حقول مخصصة (JSON) (Enhancement).

#### `StatusUpdateLog` (سجل تحديثات الحالة)
- **الغرض:** تسجيل كل تغيير يطرأ على حالة طلب الإصلاح للتدقيق.
- **الحقول:**
  - `fromStatus`, `toStatus`: الحالة قبل وبعد التغيير.
  - `notes`: ملاحظات حول التغيير.
  - `repairRequestId`, `changedById`: مفاتيح الربط بطلب الإصلاح والمستخدم الذي قام بالتغيير.
  - `repairRequest`: علاقة متعدد إلى واحد مع RepairRequest.
  - `changedBy`: علاقة متعدد إلى واحد مع User.
  - `createdAt`

### 3. عروض الأسعار (Quotations)

#### `Quotation` (عرض السعر)
- **الغرض:** إنشاء وإدارة عروض الأسعار المقدمة للعملاء قبل بدء الإصلاح.
- **الحقول:**
  - `id`: معرف فريد.
  - `status`: حالة عرض السعر (`PENDING`, `SENT`, `APPROVED`, `REJECTED`).
  - `totalAmount`: المبلغ الإجمالي لعرض السعر.
  - `notes`, `sentAt`, `responseAt`: تفاصيل إضافية.
  - `repairRequestId`: مفتاح يربط عرض السعر بطلب إصلاح معين (علاقة رأس برأس).
  - `repairRequest`: علاقة واحد إلى واحد مع RepairRequest.
  - `quotationItems`: علاقة واحد إلى متعدد مع QuotationItem.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `taxAmount`: مبلغ الضريبة (Enhancement).
  - `currency`: العملة (Enhancement).

#### `QuotationItem` (بنود عرض السعر)
- **الغرض:** تفصيل البنود الموجودة في عرض السعر (قطع غيار، أجور عمل، إلخ).
- **الحقول:**
  - `description`, `quantity`, `unitPrice`, `totalPrice`.
  - `quotationId`: مفتاح يربط البند بعرض السعر الخاص به.
  - `quotation`: علاقة متعدد إلى واحد مع Quotation.
  - `createdAt`, `updatedAt`

### 4. المخزون والمشتريات (Inventory & Purchasing)

#### `Warehouse` (المستودع)
- **الغرض:** تعريف أماكن تخزين قطع الغيار.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`.
  - `stockLevels`: علاقة واحد إلى متعدد مع StockLevel.
  - `stockMovementsFrom`: علاقة واحد إلى متعدد مع StockMovement (من المخزن).
  - `stockMovementsTo`: علاقة واحد إلى متعدد مع StockMovement (إلى المخزن).
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `InventoryItem` (عنصر المخزون)
- **الغرض:** تعريف كل قطعة غيار أو منتج في المخزون.
- **الحقول:**
  - `sku`: رقم تعريف فريد للمنتج (SKU).
  - `purchasePrice`, `sellingPrice`: أسعار الشراء والبيع.
  - `stockLevels`: علاقة واحد إلى متعدد مع StockLevel.
  - `stockMovements`: علاقة واحد إلى متعدد مع StockMovement.
  - `partsUsed`: علاقة واحد إلى متعدد مع PartsUsed.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `serialNumber`: رقم تسلسلي فريد (اختياري) (Enhancement).
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `StockLevel` (مستوى المخزون)
- **الغرض:** تتبع كمية كل `InventoryItem` في كل `Warehouse`.
- **الحقول:**
  - `quantity`, `minLevel`: كمية المخزون ومستوى التنبيه.
  - `warehouse`: علاقة متعدد إلى واحد مع Warehouse.
  - `inventoryItem`: علاقة متعدد إلى واحد مع InventoryItem.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `isLowStock`: حالة التنبيه (Enhancement).

#### `StockMovement` (حركة المخزون)
- **الغرض:** تسجيل كل حركة دخول أو خروج أو نقل للمخزون.
- **الحقول:**
  - `type`: نوع الحركة (IN, OUT, TRANSFER).
  - `quantity`: كمية الحركة.
  - `inventoryItem`: علاقة متعدد إلى واحد مع InventoryItem.
  - `fromWarehouse`: علاقة متعدد إلى واحد مع Warehouse.
  - `toWarehouse`: علاقة متعدد إلى واحد مع Warehouse.
  - `user`: علاقة متعدد إلى واحد مع User.
  - `createdAt`, `updatedAt`

#### `Vendor` (المورد)
- **الغرض:** تخزين بيانات موردي قطع الغيار.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`, `email`, `phone`.
  - `purchaseOrders`: علاقة واحد إلى متعدد مع PurchaseOrder.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `PurchaseOrder` (أمر الشراء)
- **الغرض:** إدارة طلبات الشراء من الموردين.
- **الحقول:**
  - `id`: معرف فريد.
  - `status`: حالة الطلب.
  - `vendorId`: مفتاح يربط طلب الشراء بالمورد.
  - `purchaseOrderItems`: علاقة واحد إلى متعدد مع PurchaseOrderItem.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `approvalStatus`: حالة الموافقة (PENDING, APPROVED, REJECTED) (Enhancement).
  - `approvedById`: معرف من وافق (Enhancement).
  - `approvalDate`: تاريخ الموافقة (Enhancement).

#### `PurchaseOrderItem` (بنود أمر الشراء)
- **الغرض:** تفصيل المنتجات والكميات في كل أمر شراء.
- **الحقول:**
  - `id`: معرف فريد.
  - `quantity`: كمية المنتج.
  - `unitPrice`: سعر الوحدة.
  - `totalPrice`: المبلغ الإجمالي للبند.
  - `purchaseOrderId`: مفتاح يربط البند بطلب الشراء الخاص به.
  - `inventoryItemId`: مفتاح يربط البند بالصنف الموجود في المخزون.
  - `purchaseOrder`: علاقة متعدد إلى واحد مع PurchaseOrder.
  - `inventoryItem`: علاقة متعدد إلى واحد مع InventoryItem.
  - `createdAt`, `updatedAt`

#### `PartsUsed` (القطع المستخدمة)
- **الغرض:** تسجيل قطع الغيار التي تم استخدامها في طلب إصلاح معين.
- **الحقول:**
  - `id`: معرف فريد.
  - `quantity`: كمية القطعة المستخدمة.
  - `repairRequestId`: مفتاح يربط القطعة المستخدمة بطلب الإصلاح المعين.
  - `inventoryItemId`: مفتاح يربط القطعة المستخدمة بالصنف الموجود في المخزون.
  - `invoiceItemId`: مفتاح يربط القطعة المستخدمة ببند الفاتورة الموجود في الفاتورة.
  - `createdAt`, `updatedAt`
  - `repairRequest`: علاقة متعدد إلى واحد مع RepairRequest.
  - `inventoryItem`: علاقة متعدد إلى واحد مع InventoryItem.
  - `invoiceItem`: علاقة واحد إلى واحد مع InvoiceItem (اختياري).

### 5. الوحدات المالية (Financial Entities)

#### `Invoice` (الفاتورة)
- **الغرض:** إنشاء الفواتير النهائية للعملاء بعد اكتمال الإصلاح.
- **الحقول:**
  - `id`: معرف فريد.
  - `totalAmount`: المبلغ الإجمالي للفاتورة.
  - `amountPaid`: المبلغ المدفوع.
  - `status`: حالة الفاتورة.
  - `repairRequestId`: مفتاح يربط الفاتورة بطلب الإصلاح المعين.
  - `invoiceItems`: علاقة واحد إلى متعدد مع InvoiceItem.
  - `payments`: علاقة واحد إلى متعدد مع Payment.
  - `currency`: العملة.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `taxAmount`: مبلغ الضريبة (Enhancement).
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `InvoiceItem` (بنود الفاتورة)
- **الغرض:** تفصيل بنود الفاتورة (أجور، قطع غيار).
- **الحقول:**
  - `id`: معرف فريد.
  - `quantity`: كمية المنتج.
  - `unitPrice`: سعر الوحدة.
  - `totalPrice`: المبلغ الإجمالي للبند.
  - `invoiceId`: مفتاح يربط البند بالفاتورة المعينة.
  - `partsUsedId`: مفتاح يربط البند بالقطعة المستخدمة المعينة.
  - `partsUsed`: علاقة واحد إلى واحد مع PartsUsed (اختياري).
  - `createdAt`, `updatedAt`

#### `Payment` (الدفعة)
- **الغرض:** تسجيل الدفعات المستلمة من العملاء.
- **الحقول:**
  - `id`: معرف فريد.
  - `amount`: مبلغ الدفع.
  - `paymentMethod`: طريقة الدفع.
  - `invoiceId`: مفتاح يربط الدفعة بالفاتورة المعينة.
  - `userId`: مفتاح يربط الدفعة بالمستخدم المعين.
  - `currency`: العملة.
  - `createdAt`, `updatedAt`

#### `ExpenseCategory` (فئة المصروفات)
- **الغرض:** تصنيف المصروفات (إيجار، رواتب، فواتير).
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم الفئة.
  - `expenses`: علاقة واحد إلى متعدد مع Expense.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).

#### `Expense` (المصروف)
- **الغرض:** تسجيل أي مصروفات إدارية أو تشغيلية.
- **الحقول:**
  - `id`: معرف فريد.
  - `description`: وصف المصروف.
  - `amount`: مبلغ المصروف.
  - `expenseDate`: تاريخ المصروف.
  - `categoryId`: مفتاح يربط المصروف بفئته المعينة.
  - `userId`: مفتاح يربط المصروف بالمستخدم المعين.
  - `currency`: العملة.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).
  - `category`: علاقة متعدد إلى واحد مع ExpenseCategory.
  - `user`: علاقة متعدد إلى واحد مع User.

### 6. وحدات مساعدة (Utility Models)

#### `Notification` (الإشعارات)
- **الغرض:** إرسال إشعارات للمستخدمين داخل النظام حول أحداث معينة (تغيير حالة، تنبيه مخزون، إلخ).
- **الحقول:**
  - `id`: معرف فريد.
  - `type`: نوع الإشعار (SYSTEM, USER, WARNING, INFO, ...).
  - `message`: نص الإشعار.
  - `isRead`: هل تم قراءة الإشعار.
  - `userId`: المستخدم المستلم (اختياري إذا كان الإشعار عام).
  - `repairRequestId`: (اختياري) ربط الإشعار بطلب صيانة معين.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `channel`: قناة الإشعار (EMAIL, SMS, IN_APP, ...) (Enhancement).

#### `NotificationTemplate` (قالب الإشعار) (Enhancement)
- **الغرض:** تخزين قوالب نصية للإشعارات المتكررة.
- **الحقول:**
  - `id`: معرف فريد.
  - `type`: نوع الإشعار.
  - `template`: نص القالب.
  - `createdAt`, `updatedAt`.

#### `SystemSetting` (إعدادات النظام)
- **الغرض:** تخزين إعدادات عامة للنظام يمكن التحكم بها من لوحة الإدارة.
- **الحقول:**
  - `id`: معرف فريد.
  - `key`: اسم الإعداد (فريد).
  - `value`: قيمة الإعداد (String).
  - `type`: نوع القيمة (STRING, NUMBER, BOOLEAN, JSON).
  - `description`: وصف للإعداد.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.

#### `AuditLog` (سجل التدقيق)
- **الغرض:** تسجيل الإجراءات الهامة التي تتم في النظام لزيادة الأمان والمساءلة.
- **الحقول:**
  - `id`: معرف فريد.
  - `action`: وصف الإجراء (مثال: "تعديل فاتورة").
  - `actionType`: نوع العملية (CREATE, UPDATE, DELETE, LOGIN, ...).
  - `details`: تفاصيل إضافية (JSON أو نص).
  - `entityType`: نوع الكيان المرتبط (User, Invoice, RepairRequest, ...).
  - `entityId`: معرف الكيان المرتبط (اختياري).
  - `userId`: المستخدم الذي قام بالإجراء.
  - `ipAddress`: عنوان الـ IP للجهاز المستخدم (اختياري).
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `beforeValue`: القيمة السابقة (JSON) (Enhancement).
  - `afterValue`: القيمة اللاحقة (JSON) (Enhancement).
  - **العلاقات:**
    - `user`: علاقة متعدد إلى واحد مع User.

### 7. الخدمات (Services)

#### `Service` (الخدمة)
- **الغرض:** تعريف جميع الخدمات المقدمة (صيانة دورية، إصلاح شاشة، تغيير بطارية، ...).
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم الخدمة (فريد، مثال: "صيانة دورية").
  - `description`: وصف مختصر للخدمة.
  - `basePrice`: السعر الأساسي للخدمة.
  - `isActive`: هل الخدمة متاحة.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).
  - **العلاقات:**
    - `repairRequestServices`: علاقة واحد إلى متعدد مع RepairRequestService.

#### `RepairRequestService` (خدمات الطلب)
- **الغرض:** ربط كل طلب صيانة بالخدمات المنفذة (يدعم أكثر من خدمة في نفس الطلب).
- **الحقول:**
  - `id`: معرف فريد.
  - `repairRequestId`: الطلب المرتبط.
  - `serviceId`: الخدمة المقدمة.
  - `technicianId`: الفني المنفذ.
  - `price`: السعر النهائي للخدمة (لو فيه خصم/زيادة).
  - `notes`: ملاحظات إضافية.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - **العلاقات:**
    - `repairRequest`: علاقة متعدد إلى واحد مع RepairRequest.
    - `service`: علاقة متعدد إلى واحد مع Service.
    - `technician`: علاقة متعدد إلى واحد مع User.

### 8. تقارير الفحص (Inspection Reports)

#### `InspectionType` (نوع الفحص)
- **الغرض:** تعريف أنواع الفحوصات بشكل ديناميكي وقابل للتخصيص من الإعدادات.
- **الحقول:**
  - `id`: معرف فريد.
  - `name`: اسم نوع الفحص (مثال: فحص مبدئي).
  - `description`: وصف مختصر (اختياري).
  - `isActive`: هل النوع متاح.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `deletedAt`: تاريخ الحذف المنطقي (Enhancement).
  - **العلاقات:**
    - `reports`: علاقة واحد إلى متعدد مع InspectionReport.

#### `InspectionReport` (تقرير الفحص)
- **الغرض:** توثيق كل عمليات الفحص (مبدئي، شامل، نهائي...) المرتبطة بطلبات الصيانة.
- **الحقول:**
  - `id`: معرف فريد.
  - `repairRequestId`: الطلب المرتبط.
  - `inspectionTypeId`: نوع الفحص (مفتاح خارجي يربط بجدول InspectionType).
  - `technicianId`: الفني المنفذ.
  - `summary`: ملخص الفحص.
  - `result`: نتيجة الفحص النهائية.
  - `recommendations`: التوصيات.
  - `notes`: ملاحظات إضافية.
  - `reportDate`: تاريخ الفحص.
  - `branchId`: الفرع المنفذ فيه الفحص.
  - `invoiceLink`: رابط التقرير/الفاتورة (اختياري).
  - `qrCode`: نص أو رابط QR (اختياري).
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `attachments`: مرفقات (صور/مستندات) (Enhancement).
  - **العلاقات:**
    - `repairRequest`: علاقة متعدد إلى واحد مع RepairRequest.
    - `technician`: علاقة متعدد إلى واحد مع User.
    - `branch`: علاقة متعدد إلى واحد مع Branch.
    - `inspectionType`: علاقة متعدد إلى واحد مع InspectionType.
    - `components`: علاقة واحد إلى متعدد مع InspectionComponent.

#### `InspectionComponent` (مكونات الفحص)
- **الغرض:** توثيق حالة كل مكون في الجهاز أثناء الفحص.
- **الحقول:**
  - `id`: معرف فريد.
  - `inspectionReportId`: التقرير المرتبط.
  - `name`: اسم المكون (مثال: SSD, battery, ...).
  - `status`: حالة المكون (WORKING, PARTIAL, DEFECTIVE, NOT_PRESENT).
  - `notes`: ملاحظات لكل مكون (مثال: Health 92%).
  - `priority`: أولوية الإصلاح (HIGH, MEDIUM, LOW, NONE).
  - `createdAt`: تواريخ الإنشاء.
  - `photo`: صورة للمكون (Enhancement).
  - **العلاقات:**
    - `inspectionReport`: علاقة متعدد إلى واحد مع InspectionReport.

#### Enums
- `ComponentStatus`: WORKING, PARTIAL, DEFECTIVE, NOT_PRESENT
- `ComponentPriority`: HIGH, MEDIUM, LOW, NONE

### 9. تسجيل دفعة أجهزة (Bulk Device Registration)

#### منطق العمل:
- في بعض الحالات (خاصة مع الشركات أو العملاء الكبار)، يتم استلام عدة أجهزة دفعة واحدة.
- النظام يجب أن يدعم تسجيل مجموعة أجهزة في عملية واحدة، مع إمكانية ربط كل جهاز بنفس العميل/الشركة ونفس طلب الاستلام أو الدفعة.
- يمكن إضافة بيانات مشتركة (مثل جهة الاستلام، ملاحظات عامة، تاريخ الدفعة) لكل المجموعة.
- بعد التسجيل، يتم إنشاء سجل منفصل لكل جهاز مع ربطه بالدفعة.

#### التعديلات المقترحة على الجداول:

##### `DeviceBatch` (دفعة أجهزة)
- **الغرض:** توثيق بيانات الدفعة/المجموعة عند استلام عدة أجهزة دفعة واحدة.
- **الحقول:**
  - `id`: معرف فريد.
  - `clientId`: العميل/الشركة المالكة للأجهزة.
  - `receivedById`: الموظف المستلم.
  - `batchDate`: تاريخ الاستلام.
  - `notes`: ملاحظات عامة.
  - `createdAt`, `updatedAt`: تواريخ الإنشاء والتحديث.
  - `status`: حالة الدفعة (PENDING, COMPLETED) (Enhancement).
  - `importLog`: سجل استيراد الأخطاء (Enhancement).
  - **العلاقات:**
    - `devices`: علاقة واحد إلى متعدد مع Device (أو RepairRequest حسب التصميم).

##### تعديل جدول Device (أو RepairRequest)
- إضافة حقل `deviceBatchId` (اختياري) لربط الجهاز بالدفعة.
- علاقة: deviceBatch: DeviceBatch @relation(fields: [deviceBatchId], references: [id])

#### منطق الواجهة:
- عند تسجيل دفعة أجهزة جديدة، يتم إدخال بيانات الدفعة أولاً، ثم إدخال بيانات كل جهاز بسرعة (أو رفع ملف Excel/CSV).
- بعد الحفظ، تظهر قائمة بكل الأجهزة المسجلة في الدفعة مع إمكانية إدارة كل جهاز على حدة.

---

## أمثلة عملية (Sample Scenarios) - دفعة أجهزة

### مثال: استلام دفعة أجهزة من شركة
- شركة ترسل 10 أجهزة لصيانة دورية.
- يتم إنشاء DeviceBatch باسم الشركة وتاريخ الاستلام.
- يتم إدخال بيانات كل جهاز (أو رفعها دفعة واحدة).
- كل جهاز يُربط بنفس الدفعة ونفس العميل.
- يمكن متابعة حالة كل جهاز على حدة، مع الاحتفاظ بسجل الدفعة.

---

> تم إضافة منطق تسجيل دفعة أجهزة وتعديل الجداول لدعم ذلك، مع أمثلة عملية.

### 10. بوابة العميل (Client Portal) وخدمات العملاء

#### منطق ربط العميل بالأجهزة والتقارير والفواتير
- كل عميل (Customer) يمكنه استعراض جميع أجهزته التي دخلت المركز.
- لكل جهاز: يمكنه استعراض كل تقارير الفحص المرتبطة به (InspectionReports).
- لكل جهاز: يمكنه استعراض وتحميل كل الفواتير والمدفوعات المرتبطة به (Invoices, Payments).
- كل هذه البيانات متاحة من خلال حساب العميل (Client Portal).

#### تقديم طلب صيانة أو استشارة
- العميل يمكنه من خلال حسابه:
  - تقديم طلب صيانة جديد لأي جهاز أو جهاز جديد.
  - حجز استشارة فنية مع أحد الفنيين أو خدمة العملاء.
- كل طلب يتم ربطه بالعميل ويظهر في حسابه مع إمكانية متابعة حالته.

#### الخدمات المقترحة للبدء بها (Service Master Data)
- صيانة دورية
- فحص مبدئي
- فحص نهائي/قبل التسليم
- إصلاح شاشة
- تغيير بطارية
- إصلاح لوحة مفاتيح
- إصلاح منافذ USB
- استعادة بيانات
- استشارة فنية
- تركيب نظام تشغيل
- تحديث BIOS/Drivers
- إصلاح منافذ شحن
- إصلاح صوت/سماعات

> يمكن تخصيص هذه الخدمات لاحقًا حسب احتياج السوق والعملاء.

---

### 11. واجهات النظام الرئيسية (System Main Interfaces)

#### 1. واجهة الأدمن (Admin Panel)
- إدارة جميع بيانات النظام (المستخدمين، الصلاحيات، الخدمات، التقارير، الإعدادات...)
- صلاحيات كاملة على كل الموديولات.

#### 2. واجهة الموظفين (Team Panel)
- لكل موظف صلاحياته حسب الدور (ريسيبشن، فني، محاسب، ...)
- استقبال وإدارة طلبات الصيانة، تسجيل الفحوصات، إصدار الفواتير، متابعة المهام اليومية.
- كل دور يرى فقط ما يخصه (مثلاً: الفني يرى مهامه وتقاريره فقط).

#### 3. واجهة العملاء (Client Portal)
- استعراض كل الأجهزة وسجل الصيانة والتقارير والفواتير.
- تقديم طلب صيانة أو استشارة.
- متابعة حالة الطلبات وتقييم الخدمة.
- استقبال إشعارات بالحالة أو العروض.

---

> تم توثيق منطق ربط العميل بكل أجهزته وتقاريره وفواتيره، وإمكانية حجز طلب صيانة أو استشارة، مع اقتراح الخدمات الأساسية وتوضيح الواجهات الرئيسية للنظام وصلاحيات كل منها.

---

> تم تحديث التوثيق ليشمل التحسينات المنطقية المقترحة والممارسات الفضلى لدعم المرونة، الأمان، وقابلية التوسع.
