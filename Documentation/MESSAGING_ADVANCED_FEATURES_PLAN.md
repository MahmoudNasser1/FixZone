# 🚀 خطة الميزات المتقدمة لنظام المراسلة

## الميزات المطلوبة

### 1. إضافة قوالب مخصصة
- إمكانية إضافة قوالب جديدة
- ربط القالب بحالة معينة (Repair Status, Invoice Status, etc.)
- حفظ القوالب في الإعدادات

### 2. توسيع إعدادات الأتمتة
- إعدادات أكثر تفصيلاً لكل نوع إشعار
- تحديد أوقات الإرسال
- إعدادات للحد من تكرار الإشعارات
- إعدادات للفلترة

### 3. جدولة تذكيرات الدفع
- بدلاً من كل يوم، إمكانية اختيار أيام محددة
- تحديد وقت محدد
- استخدام cron expression أو واجهة بسيطة

---

## التصميم المقترح

### 1. إضافة قوالب مخصصة

#### الواجهة:
```
[+ إضافة قالب جديد] ← زر في تبويب القوالب

Modal:
- اسم القالب (مطلوب)
- نوع الكيان (Invoice, Repair, Quotation, Payment)
- الحالة المرتبطة (اختياري - dropdown)
- القالب (textarea)
- المتغيرات المتاحة (display)
- [حفظ] [إلغاء]
```

#### التخزين:
```javascript
customTemplates: [
  {
    id: 'custom_1',
    name: 'رسالة مخصصة للإصلاح',
    entityType: 'repair',
    status: 'UNDER_REPAIR',
    template: '...',
    createdAt: '...'
  }
]
```

---

### 2. توسيع إعدادات الأتمتة

#### إعدادات لكل نوع إشعار:
```javascript
automation: {
  repair: {
    notifyOnReceived: {
      enabled: true,
      channels: ['whatsapp', 'email'],
      sendTime: '09:00', // وقت الإرسال
      minInterval: 0, // الحد الأدنى بين الإشعارات (بالساعات)
      conditions: {
        onlyImportant: false, // فقط للعملاء المهمين
        minAmount: null, // الحد الأدنى للمبلغ
      }
    },
    // ... باقي الحالات
  }
}
```

---

### 3. جدولة تذكيرات الدفع

#### الواجهة:
```
تذكيرات الدفع المتأخرة:
☑ تفعيل
📅 الجدولة:
  ○ كل يوم (9:00 صباحاً)
  ○ أيام محددة:
    ☑ السبت ☑ الأحد ☑ الاثنين ☑ الثلاثاء ☑ الأربعاء ☑ الخميس ☑ الجمعة
    الوقت: [09:00]
  ○ Cron Expression (متقدم)
    [0 9 * * *]
```

#### التخزين:
```javascript
overdueReminders: {
  enabled: true,
  schedule: {
    type: 'daily', // 'daily', 'weekly', 'cron'
    time: '09:00',
    days: [1, 2, 3, 4, 5], // 1=السبت, 2=الأحد, etc.
    cronExpression: '0 9 * * *' // إذا type = 'cron'
  },
  minDaysBetweenReminders: 1
}
```

---

## خطوات التنفيذ

### Phase 1: إضافة قوالب مخصصة
1. إضافة زر "إضافة قالب جديد" في تبويب القوالب
2. إنشاء Modal لإضافة قالب
3. حفظ القوالب في الإعدادات
4. عرض القوالب المخصصة في Accordion منفصل

### Phase 2: توسيع إعدادات الأتمتة
1. تحديث بنية الإعدادات
2. إضافة حقول جديدة في واجهة الأتمتة
3. تحديث backend service لدعم الإعدادات الجديدة

### Phase 3: جدولة تذكيرات الدفع
1. إضافة واجهة اختيار الجدولة
2. تحديث backend cron jobs لدعم الجدولة المخصصة
3. اختبار الجدولة

---

## الملفات المطلوب تعديلها

### Frontend:
- `MessagingSettingsPage.js` - إضافة الواجهات الجديدة
- `Accordion.js` - دعم القوالب المخصصة

### Backend:
- `automation.service.js` - دعم الإعدادات الجديدة
- `messagingCronJobs.js` - دعم الجدولة المخصصة
- `template.service.js` - دعم القوالب المخصصة

---

## ملاحظات
- يجب الحفاظ على التوافق مع الإعدادات القديمة
- استخدام migration script لتحديث الإعدادات الموجودة
- إضافة validation للقوالب والجدولة



