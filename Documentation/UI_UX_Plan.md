# خطة عمل Full-Stack متكاملة - FixZone ERP

هذا المستند يتتبع خطة العمل والميزات التي سيتم تنفيذها في نظام FixZone ERP.

---

## خريطة الأولويات المحدثة (أغسطس 2025)

- **P0 (عالي الأهمية) – التنفيذ أولاً:**
  - إصلاح بحث العملاء في إنشاء الفاتورة (End-to-End): تأكد من تطابق ردود API مع الواجهة، إصلاح عرض النتائج، وتحسين اختيار العميل.
  - فحص وتسجيل طلبات `/api/customers` و`/api/customers/search` (Logs) لتأكد من الاستعلامات والقيم وعدد النتائج.
  - فحص وتسجيل طلبات `/api/customers` و`/api/customers/search` (Logs) لتأكيد الاستعلامات والقيم وعدد النتائج.
  - مراجعة المخطط مقابل الكنترولرز للفواتير وعناصرها وتشغيل الهجرات المعلّقة لجدول `InvoiceItem`.

- **P1 (متوسط) – بعد إغلاق P0:**
  - إكمال `InvoicesPage` ببيانات حقيقية + Pagination/Sorting/Search.
  - إتمام `InvoiceDetailsPage`: الإجماليات والضرائب والخصومات والطباعة/التصدير وربط العميل.
  - QA لتدفق المخزون ↔ الفواتير: خصم المخزون، التحويل من `PartsUsed` إلى `InvoiceItem`، وحالات المصدر.

- **P2 (منخفض) – لاحقاً):**
  - وحدة الشركات (قائمة/إنشاء/تفاصيل وربط العملاء).
  - تنبيهات اللوحة (تأخّر الطلبات/نقص المخزون) ومهام الفنيين.
  - التقارير (أداء الفنيين، الإيرادات/التكاليف).

---

### **المرحلة الأولى: الأساسيات وقاعدة البيانات**
-   [x] تصميم مخطط قاعدة البيانات الكامل (Schema).
-   [x] إعداد المشروع (Backend + Frontend).

### **المرحلة الثانية: المصادقة والهيكل الأساسي**
-   [x] بناء نظام المصادقة (Login API).
-   [x] ربط الواجهة الأمامية بنظام المصادقة.
-   [x] بناء الهيكل العام للتطبيق (Layout: Sidebar, Topbar).

### **المرحلة الثالثة: تحسينات الواجهة الرئيسية (Layout Enhancements)**

خطة تطوير الشريطين العلوي والجانبي لزيادة احترافية الواجهة وتحسين تجربة المستخدم.

1.  **الشريط الجانبي القابل للطي (Collapsible Sidebar):**
    -   **الحالة:** `تم الإنجاز ✔️`

2.  **القائمة المنسدلة للمستخدم (User Dropdown Menu):**
    -   **الحالة:** `تم الإنجاز ✔️`

3.  **زر تبديل المظهر (Theme Toggle):**
    -   **الحالة:** `تم الإنجاز ✔️`

4.  **أيقونة الإشعارات (Notifications Bell):**
    -   **الحالة:** `تم الإنجاز ✔️`

5.  **قوائم فرعية متداخلة (Nested Sub-menus):**
    -   **الحالة:** `تم الإنجاز ✔️`

### **المرحلة الخامسة: نظام الإشعارات وإدارة المرفقات**

1.  **نظام الإشعارات الفورية (Real-time Notifications):**
    -   **الحالة:** `تم الإنجاز ✔️`
    -   **الميزات:**
        - NotificationSystem مع Context و Provider
        - NotificationCenter في الشريط العلوي
        - أنواع مختلفة: نجاح، خطأ، تحذير، معلومات، تحميل
        - إشعارات تفاعلية مع أزرار وإجراءات
        - شريط تقدم وإغلاق تلقائي
        - تم إيقاف الإشعارات العشوائية لتحسين تجربة المستخدم

2.  **مدير المرفقات (AttachmentManager):**
    -   **الحالة:** `تم الإنجاز ✔️`
    -   **الميزات:**
        - رفع بالسحب والإفلات (Drag & Drop)
        - فلترة بنوع الملفات (صور، مستندات، فيديو، صوتيات)
        - بحث في المرفقات بالعنوان والوصف
        - تحرير عنوان ووصف لكل مرفق
        - معاينة وتحميل وحذف المرفقات
        - أيقونات ملونة حسب نوع الملف
        - إشعارات لجميع عمليات المرفقات

3.  **صفحة تجريبية للإشعارات (NotificationDemo):**
    -   **الحالة:** `تم الإنجاز ✔️`
    -   **المسار:** `/notifications-demo`
    -   **الوظيفة:** اختبار جميع أنواع الإشعارات

### **المرحلة السادسة: تحسينات واجهة المستخدم والعرض (تم الإنجاز جزئياً)**

1.  **أنماط عرض متعددة (Multiple View Modes):**
    -   **الحالة:** `تم الإنجاز جزئياً ✔️`
    -   **الهدف:** إضافة أنماط عرض مختلفة للبيانات:
        - **عرض بطاقات (Cards View):** الوضع الحالي
        - **عرض جدول (Table View):** عرض مضغوط للبيانات
        - **عرض قائمة (List View):** عرض مبسط وسريع
        - **عرض شبكي (Grid View):** عرض مرن للبيانات
    -   **الصفحات المستهدفة:**
        - [x] صفحة العملاء (CustomersPage) - `تم الإنجاز ✔️`
        - [x] صفحة طلبات الإصلاح (RepairsPage) - `تم الإنجاز ✔️`
        - [~] صفحة المخزون (InventoryPage) - `تحسين UI وتناسق التصميم ✔️`، "أوضاع العرض المتعددة" قيد الدراسة
        - [~] صفحة الفواتير (InvoicesPage) - `أُنشئت صفحات القائمة/التفاصيل الأساسية ✔️`، التكامل المتقدم لاحقًا

2.  **إخفاء وإظهار الأعمدة (Column Visibility Toggle):**
    -   **الحالة:** `تم الإنجاز جزئياً ✔️`
    -   **الميزات:**
        - [x] قائمة منسدلة لاختيار الأعمدة المرئية
        - [x] حفظ تفضيلات المستخدم في localStorage
        - [x] إعادة تعيين افتراضية للأعمدة
        - [x] مؤشرات بصرية للأعمدة المخفية

3.  **الإجراءات المجمعة (Bulk Actions):**
    -   **الحالة:** `تم الإنجاز جزئياً ✔️`
    -   **الميزات:**
        - [x] تحديد متعدد للعناصر (Checkbox Selection)
        - [x] تحديد الكل / إلغاء التحديد
        - [x] شريط إجراءات متحرك عند التحديد
        - [x] إجراءات مختلفة حسب نوع البيانات:
          - [x] **طلبات الإصلاح:** تغيير الحالة، تعيين فني، حذف
          - [x] **العملاء:** تغيير الحالة، تصدير، حذف
          - [ ] **الفواتير:** إرسال، طباعة، تصدير
        - [x] تأكيد الإجراءات الحساسة
        - [x] عداد العناصر المحددة في الواجهة

4.  **تحسينات عامة للواجهة:**
    -   **الحالة:** `تم الإنجاز جزئياً ✔️`
    -   **الميزات:**
        - [x] أزرار تبديل وضع العرض في الشريط العلوي
        - [x] حفظ تفضيلات العرض لكل مستخدم
        - [x] رسوم متحركة عند التبديل بين الأنماط
        - [x] تحسين الاستجابة على الشاشات الصغيرة
        - [x] تحسين أداء التحميل والعرض
        - [x] **تحسينات إضافية (تمت في الجلسة الحالية):**
            - [x] تصنيف الحالات بالألوان في طلبات الإصلاح
            - [x] تحسين UI الإيصالات والطباعة
            - [x] إصلاح مشاكل التطابق بين الفرونت والباك إند

---

### **المرحلة الرابعة: الميزات الأساسية (Core Features)**

-   [x] **إدارة طلبات الإصلاح (Repairs Management):** `تم الإنجاز ✔️`
    -   [x] عرض قائمة بجميع طلبات الإصلاح في جدول بيانات.
    -   [x] إمكانية إضافة طلب إصلاح جديد.
    -   [x] تعديل حالة طلب الإصلاح.
    -   [x] فلترة وترتيب متقدم.
    -   [x] إحصائيات سريعة وملونة.
    -   [x] ربط مع العملاء.
    -   [x] **تحسينات إضافية (تمت في الجلسة الحالية):**
        -   [x] إصلاح مشكلة حفظ الحالات (قيد الإصلاح/معلق) وإعادة الحالة للانتظار بعد الريفرش
        -   [x] تحسين UI إيصالات الاستلام وتقرير الفحص - وضع QR في أعلى الصفحة وحذف رابط المتابعة
        -   [x] تحديث فورم إنشاء/تعديل تقرير الفحص مع واجهة محسنة وجميلة
        -   [x] إصلاح زر التعديل في صفحة طلبات الإصلاح
        -   [x] تصنيف الحالات بالألوان في صفحة طلبات الإصلاح (أصفر للانتظار، أزرق للإصلاح، رمادي للمعلق، أخضر للمكتمل، أحمر للملغي)
        -   [x] إضافة دعم الحالة الجديدة "معلق" (on-hold) مع بطاقة إحصائية جديدة
        -   [x] تحسين SimpleBadge لدعم prop 'color' وإضافة لون 'primary' مع حدود ملونة
        -   [x] تحديث البيانات الوهمية لتستخدم الحالات الصحيحة مع الشرطات (in-progress, on-hold)
-   [x] **إدارة العملاء (CRM):** `تم الإنجاز ✔️`
    -   [x] عرض قائمة بالعملاء (42 عميل).
    -   [x] إضافة وتعديل بيانات العملاء.
    -   [x] أنماط عرض متعددة (بطاقات، جدول، قائمة، شبكة).
    -   [x] بحث وفلترة متقدمة.
    -   [x] إحصائيات العملاء (إجمالي، VIP، نشط، غير نشط).
    -   [x] إجراءات مجمعة وأزرار تعديل تفاعلية.

---

### **المرحلة الخامسة: التحسينات والميزات المتقدمة (المقترحة للتطوير)**

#### 🎯 **الأولوية العالية:**
-   [x] **صفحة تفاصيل طلب الإصلاح المحسنة:** `تم الإنجاز ✔️`
    -   [x] تفاصيل كاملة للطلب مع timeline تفاعلي
    -   [x] إدارة المرفقات (صور، مستندات)
    -   [x] تتبع حالة الطلب خطوة بخطوة
    -   [x] تعليقات وملاحظات الفنيين
    -   [x] **تحسينات إضافية (تمت في الجلسة الحالية):**
        -   [x] تحسين UI إيصالات الاستلام وتقرير الفحص
        -   [x] تحديث فورم إنشاء/تعديل تقرير الفحص مع واجهة محسنة
        -   [x] إصلاح مشكلة حفظ الحالات
        -   [x] تصنيف الحالات بالألوان

-   [ ] **نظام إدارة المخزون:**
    -   عرض قطع الغيار والمواد
    -   تتبع المخزون ونقص القطع
    -   طلبات الشراء والموردين

-   [ ] **النظام المالي:**
    -   الفواتير والمدفوعات
    -   التقارير المالية
    -   متابعة المستحقات

#### 🔧 **الأولوية المتوسطة:**
-   [ ] **لوحة التحكم المحسنة:**
    -   إحصائيات تفاعلية ومخططات بيانية
    -   تنبيهات ذكية للمهام المعلقة
    -   ملخص الأداء اليومي/الأسبوعي

-   [ ] **نظام التقارير:**
    -   تقارير أداء الفنيين
    -   تقارير رضا العملاء
    -   تحليل الإيرادات والتكاليف

-   [ ] **التكامل الخارجي:**
    -   إشعارات WhatsApp للعملاء
    -   تصدير للـ Google Sheets
    -   طباعة الباركود وQR Code

#### 🎨 **تحسينات الواجهة:**
-   [x] **نظام الألوان والثيمات:** `تم الإنجاز جزئياً ✔️`
    -   [x] وضع مظلم محسن
    -   [x] ثيمات ملونة حسب نوع العمل
    -   [x] تخصيص ألوان الشركة
    -   [x] **تحسينات إضافية (تمت في الجلسة الحالية):**
        -   [x] تصنيف الحالات بالألوان في طلبات الإصلاح
        -   [x] تحسين SimpleBadge مع ألوان وحدود ملونة
        -   [x] إضافة بطاقة إحصائية جديدة للحالة "معلق"

-   [ ] **تعدد اللغات:**
    -   دعم اللغة الإنجليزية
    -   تبديل اللغة من الإعدادات
    -   RTL/LTR تلقائي

## 🎯 مقدمة

هذه الوثيقة هي المرجع الأساسي لتصميم وتنفيذ نظام **FixZone ERP** بشكل متكامل، حيث تربط كل مهمة واجهة أمامية (Frontend) بنقطة النهاية (API Endpoint) المقابلة لها في الواجهة الخلفية (Backend). تم إعدادها لضمان بناء نظام احترافي، آمن، قابل للتطوير، وعالي الجودة.

---

## 1. 🏛️ المبادئ الأساسية للتصميم (Core Design Principles)

- **التقنية المستخدمة:** React.js + Tailwind CSS.
- **الهوية البصرية:** تصميم عصري، نظيف، واحترافي يوحي بالثقة والتقنية.
- **لوحة الألوان:**
  - **أساسي:** درجات الأزرق (للعناصر التفاعلية والروابط).
  - **محايد:** درجات الرمادي (للخلفيات والنصوص).
  - **قاعدة:** أبيض (للمساحات والمحتوى الرئيسي).
- **الخطوط (Typography):** استخدام خط **Cairo** أو **Inter** لضمان وضوح القراءة باللغتين العربية والإنجليزية.
- **الأيقونات (Iconography):** استخدام مكتبة **Lucide Icons** أو **Tabler Icons** لتوحيد شكل الأيقونات.
- **المفاهيم الأساسية:**
  - **استجابة كاملة (Fully Responsive):** يعمل بكفاءة على جميع الشاشات (مكتبية، لوحية، هواتف).
  - **دعم الوضع الليلي (Dark Mode):** ضروري لراحة العين، خاصة للفنيين.
  - **دعم اللغة العربية (RTL):** تصميم الواجهات لتعمل بسلاسة من اليمين إلى اليسار.
  - **مؤثرات حركية دقيقة (Micro-animations):** لإعطاء إحساس بالحيوية عند التفاعل.
  - **صديق للطباعة (Print-Friendly):** تصميم خاص للتقارير والفواتير عند الطباعة.
  - **متجاوب مع جميع الأجهزة (Cross-Device):** يعمل بكفاءة على جميع الأجهزة.

---

## 2. 🧱 المكونات العامة القابلة لإعادة الاستخدام (Global UI Components)

سيتم بناء هذه المكونات أولاً لتكون أساسًا لكل الواجهات (لا تتطلب ربط مباشر بالـ API بنفسها، بل تستخدمه الصفحات التي تحتويها):

- **[✅] الهيكل العام (Main Layout):**
  - **[✅] الشريط الجانبي (Sidebar):** قائمة تنقل رئيسية قابلة للطي محسنة مع تنظيم أفضل، إشعارات، وأيقونات واضحة.
  - **[✅] الشريط العلوي (Topbar):** محسن مع اختصارات سريعة، بحث مدمج، إحصائيات فورية، ونظام إشعارات متقدم.
  - **[✅] مسار التنقل (Breadcrumb):** نظام تنقل تلقائي وذكي مع دعم الاختصار.
  - **[✅] دعم RTL:** ضبط كامل للغة العربية على مستوى النظام مع خطوط محسنة.
- **[ ] الأزرار (Buttons):** تصميم موحد للأزرار الرئيسية، الثانوية، وأزرار الحذف.
- **[ ] النماذج (Forms):** تصميم موحد لحقول الإدخال، القوائم المنسدلة، مربعات الاختيار، وغيرها.
- [x] **الجداول (Tables):** مكون جدول احترافي تم بناؤه مع الميزات المتقدمة التالية:
  - [x] الفرز (Sorting)
  - [x] الترقيم (Pagination)
  - [x] إخفاء/إظهار الأعمدة (Column Visibility)
  - [x] تحديد الصفوف (Row Selection)
  - [x] بحث شامل (Global Search)
  - [x] فلاتر متقدمة (Faceted Filtering)
  - [x] إجراءات مجمعة (Bulk Actions)
  - [x] تصدير البيانات (Data Export to CSV)
- **[ ] البطاقات (Cards):** لعرض الإحصائيات والمعلومات المختصرة كما في لوحة التحكم.
- **[ ] النوافذ المنبثقة (Modals):** لتأكيد الإجراءات أو عرض نماذج الإدخال السريعة.
- **[ ] الإشعارات (Notifications/Toasts):** لتقديم ملاحظات فورية للمستخدم.
- **[x] الشارات (Badges):** لعرض الحالات المختلفة بألوان مميزة. `تم الإنجاز ✔️`
    -   [x] تحسين SimpleBadge لدعم prop 'color' و 'variant'
    -   [x] إضافة ألوان جديدة: primary (بنفسجي)، warning (أصفر)، info (أزرق)، success (أخضر)، danger (أحمر)، secondary (رمادي)
    -   [x] إضافة حدود ملونة لكل الألوان لتحسين الوضوح البصري
    -   [x] تطبيق الألوان على حالات طلبات الإصلاح بشكل صحيح
- **[ ] مؤشرات التحميل (Loaders/Skeletons):** لتحسين تجربة المستخدم أثناء انتظار البيانات.

---

## 3. 🗺️ تفصيل الواجهات والربط مع الواجهة الخلفية (UI & API Integration)

### أ. المصادقة وإدارة الوصول (Authentication & Access Management)

- **[x] صفحة تسجيل الدخول (Login Page):** (اكتمل بنجاح وأصبحت آمنة)
  - **Frontend:** تم تحديث الواجهة الأمامية للتعامل مع الـ Cookies.
  - **Backend API:** `POST /api/auth/login`
    - **المخرجات:** معلومات المستخدم الأساسية (الاسم، الدور).
    - **التفاصيل:** تم تأمين عملية الدخول بالكامل. التوكن يُرسل الآن كـ `httpOnly cookie` آمن، وتم حل جميع مشاكل CORS.
- **[ ] تسجيل مستخدم جديد (User Registration - للمدراء فقط):**
  - **Frontend:** نموذج خاص في لوحة تحكم المدير لإنشاء مستخدمين جدد (فنيين، موظفي استقبال).
  - **Backend API:** `POST /api/v1/users/register`
    - **المدخلات:** `name`, `email`, `phone`, `password`, `roleId`.
    - **التفاصيل:** يجب أن يكون المستخدم الذي يقوم بالإنشاء مديرًا (Admin). يتم تشفير كلمة المرور باستخدام `bcrypt`.
- **[ ] استعادة كلمة المرور (Password Reset):**
  - **Frontend (الخطوة 1):** صفحة تطلب البريد الإلكتروني للمستخدم.
  - **Backend API (الخطوة 1):** `POST /api/v1/auth/forgot-password` - لإرسال رابط إعادة تعيين إلى بريد المستخدم.
  - **Frontend (الخطوة 2):** صفحة جديدة (يتم الوصول إليها من الرابط) لإدخال كلمة المرور الجديدة.
  - **Backend API (الخطوة 2):** `POST /api/v1/auth/reset-password` - لتحديث كلمة المرور في قاعدة البيانات.

### ب. تحسينات واجهة تسجيل الدخول (Login Page UI Enhancements)
- **[ ] إضافة "تذكرني" (Remember Me):**
  - **Frontend:** إضافة checkbox في صفحة الدخول.
  - **Backend:** تعديل صلاحية الـ cookie لتمتد لفترة أطول إذا تم تحديد الخيار.
- **[ ] إضافة "هل نسيت كلمة المرور؟" (Forgot Password?):
  - **Frontend:** إضافة رابط يوجه المستخدم إلى صفحة استعادة كلمة المرور.
  - **Backend:** (سيتم تنفيذها لاحقًا ضمن قسم استعادة كلمة المرور).

### ج. الهيكل العام للتطبيق (Main Application Layout)
- **[ ] الشريط الجانبي (Sidebar):**
  - **Frontend:** بناء مكون الشريط الجانبي الثابت الذي يحتوي على روابط التنقل الرئيسية.
- **[ ] الشريط العلوي (Topbar):**
  - **Frontend:** بناء مكون الشريط العلوي لعرض اسم المستخدم، الإشعارات، وزر الخروج.

### د. لوحة التحكم (Dashboard)
- **[x] بطاقات الإحصائيات (Stats Cards):**
  - **Frontend:** عرض سريع للأرقام الهامة (الأجهزة المستلمة، تحت الإصلاح، إلخ).
  - **Backend API:** `GET /api/dashboard/stats` - لجلب الإحصائيات الرئيسية.
- **[x] الرسوم البيانية (Charts):**
  - **Frontend:** رسوم بيانية تفاعلية (أعمدة، دائري) لعرض الإحصائيات بشكل مرئي.
  - **Backend API:** `GET /api/dashboard/charts-data` - لجلب بيانات الرسوم البيانية.
- **[ ] قسم التنبيهات (Alerts Section):**
  - **Frontend:** قائمة بالأجهزة المتأخرة والمخزون المنخفض.
  - **Backend API:** `GET /api/dashboard/alerts` - لجلب التنبيهات الهامة.
- **[ ] قائمة مهام الفنيين (Technician Tasks):**
  - **Frontend:** عرض مبسط للمهام الموكلة للفنيين.
  - **Backend API:** `GET /api/dashboard/technician-tasks` - لجلب مهام الفنيين.

### د. إدارة الشركات والعملاء (Company & Customer Management)

#### 👥 إدارة العملاء (Customer Management)
- **[✅] الواجهة الرئيسية (List View):**
  - **[✅] Frontend:** جدول احترافي يعرض قائمة العملاء مع إحصائيات سريعة، بحث، وفلترة
  - **[✅] Backend API:** `GET /api/customers` - لجلب قائمة العملاء مع دعم للبحث والترقيم

- **[✅] إضافة عميل جديد (Create Customer):**
  - **[✅] Frontend:** نموذج شامل لإدخال بيانات العميل (الاسم، الهاتف، البريد، العنوان، الشركة، إعدادات VIP)
  - **[ ] Backend API:** `POST /api/customers` - لإرسال بيانات العميل الجديد وحفظه

- **[✅] صفحة تفاصيل العميل (Customer Details):**
  - **[✅] Frontend:** عرض شامل لبيانات العميل، سجل طلبات الإصلاح، والملاحظات
  - **[✅] Backend API:** `GET /api/customers/:id` - لجلب بيانات عميل معين
  - **[✅] Backend API:** `GET /api/repairs?customerId=:id` - لجلب سجل طلبات العميل
  - **[✅] ربط العملاء مع طلبات الإصلاح:** فلترة طلبات الإصلاح بالعميل من URL
  - **[✅] واجهة تفاعلية:** أزرار سريعة للانتقال بين العملاء وطلباتهم

#### 🏢 إدارة الشركات (Company Management)
- **[ ] الواجهة الرئيسية (List View):**
  - **[ ] Frontend:** جدول احترافي يعرض كل الشركات مع أعمدة (اسم الشركة، جهة الاتصال، الهاتف، عدد العملاء المرتبطين)
  - **[ ] Backend API:** `GET /api/companies` - لجلب قائمة الشركات مع دعم للبحث والترقيم

- **[ ] إنشاء شركة جديدة (Create Company):**
  - **[ ] Frontend:** نموذج لإدخال بيانات الشركة الجديدة (الاسم، العنوان، الهاتف، البريد، رقم الضريبة)
  - **[ ] Backend API:** `POST /api/companies` - لإرسال بيانات الشركة الجديدة وحفظها

- **[ ] صفحة تفاصيل الشركة (Company Details):**
  - **[ ] Frontend:** عرض شامل لكل بيانات الشركة، بالإضافة إلى قائمة بالعملاء المرتبطين
  - **[ ] Backend API:** `GET /api/companies/:id` - لجلب بيانات شركة معينة
  - **[ ] Backend API:** `GET /api/companies/:id/customers` - لجلب قائمة العملاء المرتبطين بالشركة
  - **[ ] Backend API:** `GET /api/companies/:id/repairs` - لجلب كل طلبات الصيانة لعملاء الشركة
- **[ ] ربط عميل بشركة (Link Customer to Company):**
  - **Frontend:** عند إنشاء أو تعديل عميل، يتم إضافة حقل (Autocomplete) للبحث عن الشركة وربطه بها.
  - **Backend API:** يتم تحديث `POST /api/customers` و `PUT /api/customers/:id` ليقبلا `companyId`.

### هـ. طلبات الصيانة (Repair Tickets)
- **[ ] الواجهة الرئيسية (List View):**
  - **Frontend:** جدول احترافي يعرض كل طلبات الصيانة مع أعمدة (رقم الطلب، العميل، الجهاز، الحالة، الفني، تاريخ الاستلام) وأزرار تحكم (طلب جديد، بحث، تصفية).
  - **Backend API:** `GET /api/repairs` - لجلب قائمة طلبات الصيانة مع دعم للبحث والتصفية والترقيم.
- **[ ] إنشاء طلب جديد (Create Ticket):**
  - **Frontend:** نموذج (Modal أو صفحة) لإدخال بيانات الطلب الجديد.
  - **Backend API:** `POST /api/repairs` - لإرسال بيانات الطلب الجديد وحفظه.
- **[x] صفحة تفاصيل الطلب (Ticket Details):** `تم الإنجاز ✔️`
  - **[x] Frontend:** عرض شامل لكل بيانات الطلب، سجل التغييرات (Logs)، وقسم لإضافة الملاحظات.
  - **[x] Backend API:** `GET /api/repairs/:id` - لجلب بيانات طلب معين.
  - **[x] Backend API:** `GET /api/repairs/:id/logs` - لجلب سجل التغييرات.
  - **[x] Backend API:** `POST /api/repairs/:id/notes` - لإضافة ملاحظة جديدة.
  - **[x] تحسينات إضافية:** تحديث فورم إنشاء/تعديل تقرير الفحص مع واجهة محسنة وجميلة
- **[ ] تعديل طلب (Update Ticket):**
  - **Frontend:** نموذج لتعديل بيانات الطلب.
  - **Backend API:** `PUT /api/repairs/:id` - لتحديث بيانات الطلب.
- **[x] تغيير حالة الطلب (Change Status):** `تم الإنجاز ✔️`
  - **[x] Frontend:** قائمة منسدلة أو أزرار لتغيير حالة الطلب.
  - **[x] Backend API:** `PATCH /api/repairs/:id/status` - لتحديث حالة الطلب فقط.
  - **[x] تحسينات إضافية:** إصلاح تطابق الحالات بين الفرونت والباك إند، دعم الحالات الجديدة (in-progress, on-hold)
- **[x] نماذج الطباعة (Printing):** `تم الإنجاز ✔️`
  - **[x] Frontend:** أزرار لطباعة تقرير الفحص، التسليم، أو الملصق.
  - **[x] Backend API:** `GET /api/repairs/:id/print/inspection` (أو `/delivery`, `/sticker`) - لجلب بيانات مهيأة للطباعة أو ملف PDF جاهز.
  - **[x] تحسينات إضافية:** تحسين UI الإيصالات - وضع QR في أعلى الصفحة وحذف رابط المتابعة

### د. العملاء (CRM)
- **[ ] عرض قائمة العملاء:**
  - **Frontend:** جدول يعرض قائمة العملاء مع خيارات البحث.
  - **Backend API:** `GET /api/customers` - لجلب قائمة العملاء.
- **[ ] صفحة ملف العميل (Customer Profile):**
  - **Frontend:** عرض بيانات العميل، سجل طلباته، الملاحظات، وتقييماته.
  - **Backend API:** `GET /api/customers/:id` - لجلب بيانات عميل معين.
  - **Backend API:** `GET /api/customers/:id/repairs` - لجلب سجل طلبات العميل.

### هـ. حسابات الشركات (Company Accounts) - جديد
- **الهدف:** إدارة العملاء التابعين لشركات، وتوفير حساب رئيسي لكل شركة يمكن من خلاله متابعة كل الطلبات.
- **[ ] الواجهة الرئيسية لإدارة الشركات (Companies List View):**
  - **Frontend:** جدول يعرض قائمة الشركات المسجلة مع معلومات أساسية (اسم الشركة، عدد العملاء) وأزرار تحكم (إضافة شركة، بحث).
  - **Backend API:** `GET /api/v1/companies` - لجلب قائمة الشركات.
- **[ ] إنشاء/تعديل شركة (Create/Update Company):**
  - **Frontend:** نموذج لإدخال بيانات الشركة (اسم الشركة، معلومات الاتصال، ...).
  - **Backend API:** `POST /api/v1/companies` و `PUT /api/v1/companies/:id`.
- **[ ] صفحة تفاصيل الشركة (Company Profile):**
  - **Frontend:** عرض بيانات الشركة، وقائمة بالعملاء التابعين لها، وسجل مجمع لكل طلبات الصيانة الخاصة بهم.
  - **Backend API:** `GET /api/v1/companies/:id` - لجلب بيانات الشركة.
  - **Backend API:** `GET /api/v1/companies/:id/customers` - لجلب قائمة العملاء التابعين للشركة.
  - **Backend API:** `GET /api/v1/companies/:id/repairs` - لجلب كل طلبات الصيانة لعملاء الشركة.
- **[ ] ربط عميل بشركة (Link Customer to Company):**
  - **Frontend:** عند إنشاء أو تعديل عميل، يتم إضافة حقل (Autocomplete) للبحث عن الشركة وربطه بها.
  - **Backend API:** يتم تحديث `POST /api/customers` و `PUT /api/customers/:id` ليقبلا `companyId`.

### هـ. حسابات الشركات (Company Accounts) - جديد
- **الهدف:** إدارة العملاء التابعين لشركات، وتوفير حساب رئيسي لكل شركة يمكن من خلاله متابعة كل الطلبات.
- **[ ] الواجهة الرئيسية لإدارة الشركات (Companies List View):**
  - **Frontend:** جدول يعرض قائمة الشركات المسجلة مع معلومات أساسية (اسم الشركة، عدد العملاء) وأزرار تحكم (إضافة شركة، بحث).
  - **Backend API:** `GET /api/v1/companies` - لجلب قائمة الشركات.
- **[ ] إنشاء/تعديل شركة (Create/Update Company):**
  - **Frontend:** نموذج لإدخال بيانات الشركة (اسم الشركة، معلومات الاتصال، ...).
  - **Backend API:** `POST /api/v1/companies` و `PUT /api/v1/companies/:id`.
- **[ ] صفحة تفاصيل الشركة (Company Profile):**
  - **Frontend:** عرض بيانات الشركة، وقائمة بالعملاء التابعين لها، وسجل مجمع لكل طلبات الصيانة الخاصة بهم.
  - **Backend API:** `GET /api/v1/companies/:id` - لجلب بيانات الشركة.
  - **Backend API:** `GET /api/v1/companies/:id/customers` - لجلب قائمة العملاء التابعين للشركة.
  - **Backend API:** `GET /api/v1/companies/:id/repairs` - لجلب كل طلبات الصيانة لعملاء الشركة.
- **[ ] ربط عميل بشركة (Link Customer to Company):**
  - **Frontend:** عند إنشاء أو تعديل عميل، يتم إضافة حقل (Autocomplete) للبحث عن الشركة وربطه بها.
  - **Backend API:** يتم تحديث `POST /api/customers` و `PUT /api/customers/:id` ليقبلا `companyId`.

---

## 4. ⭐ ملاحظات هامة لضمان الجودة (Full-Stack Best Practices)

1.  **إصدار الـ API (API Versioning):**
    - **لماذا؟** لتسهيل التحديثات المستقبلية دون التأثير على الإصدارات الحالية من الواجهة الأمامية.
    - **كيف؟** استخدام `v1` في كل مسارات الـ API (مثال: `/api/v1/...`).

2.  **هيكل موحد لاستجابة الـ API (Consistent API Response):**
    - **لماذا؟** لتوحيد طريقة معالجة النجاح والفشل في الواجهة الأمامية.
    - **كيف؟** يجب أن تعود كل الاستجابات بنفس الشكل، مثال:
      ```json
      // للنجاح
      { "success": true, "data": { ... } }
      // للفشل
      { "success": false, "error": { "message": "..." } }
      
   - اعتماد Framer Motion لانتقالات Dropdown/Modal المعقدة عند الحاجة، مع الحفاظ على خفة الواجهة.

6. **تعميم DataView (Medium)**
   - تطبيق DataView بالمزايا الجديدة على Inventory وInvoices.

7. **واجهة الطباعة والتقارير (Medium)**
   - قوالب طباعة للفحص/التسليم/الفواتير + Export PDF/Excel.

8. **تحسين المحمول والتجربة اللمسية (Medium)**
   - مسافات ولمس أكبر للأزرار وتثبيت الشريط العائم للإجراءات.

9. **إتاحة الوصول (Accessibility) (Ongoing)**
   - مراجعة ARIA roles، تنقل لوحة المفاتيح، وإعلانات الشاشة للقوائم والإشعارات.

---

## ✅ تحديثات الجلسة الحالية (Inventory ↔ Invoice)

- **تحسين صفحة المخزون `InventoryPage`** (`frontend/react-app/src/pages/inventory/InventoryPage.js`)
  - إضافة `Breadcrumb` موّحد وعناصر إجراءات في الهيدر.
  - استخدام مكونات `SimpleCard` و`SimpleButton` وتوحيد أنماط الجداول.
  - تبويب واضح بين "العناصر" و"تنبيهات النقص" بأزرار متناسقة مع النظام.

- **صفحات الفواتير**
  - إنشاء `InvoicesPage` (قائمة أولية) و`InvoiceDetailsPage` (تفاصيل الفاتورة) مع مسارات:
    - `/invoices` و`/invoices/:id` في `src/App.js`.
  - `InvoiceDetailsPage` تعرض عناصر الفاتورة (قطع/خدمات) مع شارة لمصدر العناصر من `PartsUsed` وإجماليات المبلغ.

- **تكامل الخدمات (Frontend Services)**
  - استخدام `inventoryService.fetchInvoiceItems(invoiceId)` لعرض عناصر الفاتورة.
  - دعم التدفق: إصدار قطعة مع `invoiceId` مباشرة أو تحويل `PartsUsed` لاحقًا إلى `InvoiceItem`.

---

## 🎯 الخطوات القادمة الفورية (Next Steps)

0. **P0: إصلاح بحث العملاء في إنشاء الفاتورة (Invoice Customer Search)**
   - إضافة/مراجعة Logs في الراوتر لطباعة `q`, `page`, `pageSize`, وعدد النتائج.

---

  ## ✅ تحديثات الجلسة الحالية (Repairs Page)

  - **إصلاحات وقت التشغيل في `RepairsPage`:** تم حل مشكلة تعارض اسم `DataView` مع الكائن المدمج في JavaScript عبر استيراد المكوّن الصحيح من `frontend/react-app/src/components/ui/DataView.js`.
  - **توافق الحقول مع الـ API:** تمت مراجعة أعمدة البيانات في `frontend/react-app/src/pages/repairs/RepairsPage.js` وتأكيد تطابقها مع الحقول القادمة من `backend/routes/repairs.js`، بما في ذلك `deviceType`, `deviceBrand`, `deviceModel`, `customerName`, `customerPhone`, `status`, `priority`, `estimatedCost`, `createdAt`.
  - **توحيد حالات الحالة (Status Mapping):** تأكيد تحويلات الحالة بين قاعدة البيانات والواجهة (`pending`, `in-progress`, `on-hold`, `completed`, `cancelled`) ومعالجة القيم غير المعروفة بشكل آمن.
  - **حالة الصفحة:** تعمل بدون أخطاء 500 أو أخطاء Runtime، والبيانات التجريبية المصرية تظهر بشكل صحيح.

  ### ▶ الخطوة القادمة السريعة
  - تنظيف التحذيرات في `RepairsPage.js`: إزالة الاستيرادات/المتغيرات غير المستخدمة واستكمال أي استخدامات ناقصة.
   - مراجعة `InvoicesPage` لاستخدام الرد الصحيح وعرض النتائج واختيار العميل.
   - توحيد شكل الاستجابة في الواجهة (data/pagination) وتفادي الفلترة المزدوجة.

### ✅ تحديثات الجلسة الحالية (Repair Details Enhancements)

- **قسم مواصفات الجهاز المحسّن** (`frontend/react-app/src/pages/repairs/RepairDetailsPage.js`)
  - إمكانية تعديل المواصفات التفصيلية: المعالج، كرت الشاشة، الذاكرة، التخزين، حجم الشاشة، نظام التشغيل.
  - واجهة تحرير تفاعلية مع حفظ وإلغاء وتحقق أساسي للمدخلات.
  - عرض منظم للمواصفات بتصنيف واضح وتنسيق موحد للقيم.

- **قسم المدفوعات الجديد**
  - ملخص مالي شامل: إجمالي المدفوع، التكلفة المقدرة، المبلغ المتبقي.
  - إضافة دفعات جديدة مع دعم طرق دفع متعددة (نقدي/بطاقة/تحويل/شيك) وملاحظات لكل دفعة.
  - تتبع مرجعي لكل دفعة وإظهار سجل تاريخي للمدفوعات بتفاصيل كاملة.

- **سجل الأنشطة المحسّن**
  - تصفية متقدمة حسب نوع النشاط (نظام، فني، عميل) وترتيب زمني (الأحدث أولاً/الأقدم أولاً).
  - تصنيف بصري للأنشطة بألوان ورموز مميزة، وواجهة محسنة لعرض الملاحظات.

- **تحسينات عامة**
  - تبويبات جديدة: فصل تبويبَيْ “المدفوعات” و“سجل الأنشطة” عن “الملاحظات”.
  - دوال مساعدة لتنسيق البيانات وإدارة الحالة، وتحميل كسول للبيانات لتحسين الأداء.
  - معالجة أخطاء محسّنة مع بيانات تجريبية كاحتياطي لضمان الاستقرار.

#### 🎯 المزايا الجديدة
- إدارة مواصفات الجهاز: تعديل وحفظ مواصفات تفصيلية للأجهزة.
- تتبّع المدفوعات: إضافة ومراقبة جميع المدفوعات مع حساب المتبقي فورياً.
- سجل أنشطة ذكي: تصفية وترتيب الأنشطة حسب النوع والوقت مع تمييز بصري.
- واجهة مستخدم محسّنة: تنظيم أفضل وتجربة أكثر سلاسة.

1. **تحسين تناسق UI لصفحة المخزون**
   - اعتماد نفس شريط الأدوات المستخدم في الصفحات الأخرى (بحث، فلاتر، إجراءات سريعة) كأسلوب موحّد.
   - التفكير في ترقية جدول العناصر إلى `DataTable` الموحّد بالنظام إن لزم (فرز/ترقيم/إخفاء أعمدة).

2. **إكمال صفحة الفواتير**
   - ربط قائمة الفواتير ببيانات حقيقية (service `invoicesService` لاحقًا) بدل البيانات التجريبية.
   - إظهار الإجماليات والضرائب والخصومات وخيارات الطباعة.

3. **QA على تدفق المخزون ↔ الفاتورة**
   - اختبار: إصدار بدون فاتورة ثم التحويل إلى `InvoiceItem`.
   - اختبار: إصدار مباشر مع `invoiceId` والتحقق من الربط والخصم من المخزون.
   - التحقق من ظهور شارة "من قطع الإصلاح" في `/invoices/:id`.
