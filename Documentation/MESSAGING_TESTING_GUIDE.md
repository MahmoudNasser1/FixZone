# دليل اختبار نظام المراسلة

## 📋 قائمة الاختبارات

### ✅ 1. اختبارات Backend

#### 1.1 اختبارات الوحدة
```bash
cd backend
npm test -- tests/messaging
```

**النتائج المتوقعة:**
- ✅ Template Service Tests: 9/9
- ✅ WhatsApp Service Tests: 5/5
- ✅ Messaging Service Tests: 8/8
- ✅ Integration Tests: 4/4

#### 1.2 التحقق من الإعداد
```bash
cd backend
node scripts/verify-messaging-setup.js
```

**النتائج المتوقعة:**
- ✅ جدول MessagingLog موجود
- ✅ إعدادات المراسلة موجودة
- ✅ جميع Services تعمل
- ✅ Routes مربوطة

---

### ✅ 2. اختبارات Frontend

#### 2.1 صفحة مركز الرسائل
**الرابط:** `/messaging`

**الاختبارات:**
1. ✅ فتح الصفحة - يجب أن تظهر الإحصائيات
2. ✅ الفلترة - جرب جميع الفلاتر
3. ✅ عرض الرسائل - يجب أن تظهر قائمة الرسائل
4. ✅ تفاصيل الرسالة - اضغط على رسالة لعرض التفاصيل
5. ✅ إعادة المحاولة - جرب إعادة محاولة رسالة فاشلة
6. ✅ Pagination - تصفح الصفحات

#### 2.2 صفحة الفاتورة
**الرابط:** `/invoices/:id`

**الاختبارات:**
1. ✅ زر الإرسال في Header - يجب أن يظهر ChannelSelector
2. ✅ زر الإرسال في Quick Actions - يجب أن يرسل مباشرة
3. ✅ إرسال فاتورة - جرب إرسال فاتورة عبر WhatsApp
4. ✅ MessageLogViewer - يجب أن يظهر آخر 5 رسائل
5. ✅ إعادة المحاولة - جرب إعادة محاولة رسالة فاشلة

#### 2.3 صفحة طلب الإصلاح
**الرابط:** `/repairs/:id`

**الاختبارات:**
1. ✅ زر الإرسال - يجب أن يرسل إشعار طلب إصلاح
2. ✅ MessageLogViewer - يجب أن يظهر سجل المراسلات
3. ✅ إرسال عبر WhatsApp - جرب الإرسال

#### 2.4 صفحة الدفعة
**الرابط:** `/payments/:id`

**الاختبارات:**
1. ✅ زر الإرسال - يجب أن يرسل تذكير دفع
2. ✅ MessageLogViewer - يجب أن يظهر سجل المراسلات

#### 2.5 صفحة المدفوعات المتأخرة
**الرابط:** `/payments/overdue`

**الاختبارات:**
1. ✅ زر التذكير - يجب أن يرسل تذكير لكل دفعة
2. ✅ الإرسال المجمع - جرب إرسال تذكير لعدة دفعات

---

### ✅ 3. اختبارات التكامل

#### 3.1 إرسال فاتورة
1. افتح `/invoices/:id`
2. اضغط على زر الإرسال
3. اختر القناة (WhatsApp)
4. اضغط "إرسال"
5. ✅ يجب أن تفتح WhatsApp Web أو يتم الإرسال عبر API
6. ✅ يجب أن تظهر رسالة نجاح
7. ✅ يجب أن يظهر السجل في MessageLogViewer

#### 3.2 إرسال إشعار طلب إصلاح
1. افتح `/repairs/:id`
2. اضغط على زر الإرسال
3. ✅ يجب أن يرسل إشعار "تم الاستلام"
4. ✅ يجب أن يظهر السجل

#### 3.3 إرسال تذكير دفع
1. افتح `/payments/:id`
2. اضغط على زر الإرسال
3. ✅ يجب أن يرسل تذكير دفع
4. ✅ يجب أن يظهر السجل

---

### ✅ 4. اختبارات الأخطاء

#### 4.1 رقم هاتف غير موجود
- ✅ يجب أن تظهر رسالة خطأ واضحة
- ✅ يجب ألا يحاول الإرسال

#### 4.2 إعدادات غير مفعلة
- ✅ يجب أن تظهر رسالة توجيهية
- ✅ يجب أن يوجه المستخدم للإعدادات

#### 4.3 فشل الإرسال
- ✅ يجب أن تظهر رسالة خطأ
- ✅ يجب أن تظهر زر إعادة المحاولة
- ✅ يجب أن تسجل في السجل

---

### ✅ 5. اختبارات الأداء

#### 5.1 تحميل الصفحات
- ✅ صفحة مركز الرسائل يجب أن تحمل في < 2 ثانية
- ✅ MessageLogViewer يجب أن يحمل في < 1 ثانية

#### 5.2 الإرسال
- ✅ الإرسال عبر WhatsApp Web يجب أن يكون فوري
- ✅ الإرسال عبر API يجب أن يكون < 3 ثواني

---

## 📊 قائمة التحقق النهائية

### Backend
- [ ] جميع الاختبارات تمر (26/26)
- [ ] التحقق من الإعداد ناجح
- [ ] جدول MessagingLog يعمل
- [ ] جميع Services تعمل

### Frontend
- [ ] صفحة مركز الرسائل تعمل
- [ ] SendButton يعمل في جميع الصفحات
- [ ] MessageLogViewer يعمل
- [ ] ChannelSelector يعمل
- [ ] إعادة المحاولة تعمل

### التكامل
- [ ] إرسال فاتورة يعمل
- [ ] إرسال إشعار طلب إصلاح يعمل
- [ ] إرسال تذكير دفع يعمل
- [ ] السجل يسجل بشكل صحيح

---

**تاريخ الإنشاء:** 2025-01-12
**الحالة:** ✅ جاهز للاختبار

