# 📚 دوكيومنت نظام المراسلة - FixZone

## 📋 نظرة عامة

هذا المستند يشرح نظام المراسلة في FixZone بشكل شامل. يمكن استخدامه كمرجع عند إضافة ميزات جديدة أو تعديل الميزات الموجودة.

**آخر تحديث:** 2025-01-12

---

## 🏗️ البنية المعمارية

### 1. الطبقات (Layers)

```
┌─────────────────────────────────────────────────────────┐
│              Frontend (React)                            │
│  - MessagingSettingsPage (إعدادات)                     │
│  - InvoiceDetailsPage (إرسال فواتير)                    │
│  - RepairDetailsPage (إرسال طلبات)                      │
│  - MessagingCenterPage (مركز الرسائل)                   │
│  - messagingService.js (خدمة موحدة)                    │
│  - SendButton.js (مكون قابل لإعادة الاستخدام)           │
│  - ChannelSelector.js (اختيار القناة)                    │
│  - MessageLogViewer.js (عرض السجل)                      │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ HTTP/API
                     │
┌────────────────────▼────────────────────────────────────┐
│              Backend API                                 │
│  - /api/messaging/send (إرسال)                          │
│  - /api/messaging/settings (إعدادات)                   │
│  - /api/messaging/logs (سجل المراسلات)                  │
│  - /api/messaging/stats (إحصائيات)                      │
│  - /api/messaging/retry (إعادة المحاولة)                │
└────────────────────┬────────────────────────────────────┘
                     │
                     │
┌────────────────────▼────────────────────────────────────┐
│              Services Layer                              │
│  - messaging.service.js (منطق الإرسال الموحد)           │
│  - whatsapp.service.js (WhatsApp Web & API)              │
│  - email.service.js (SMTP & HTML Templates)              │
│  - template.service.js (قوالب الرسائل)                  │
│  - pdf.service.js (توليد PDF - قيد التطوير)            │
└────────────────────┬────────────────────────────────────┘
                     │
                     │
┌────────────────────▼────────────────────────────────────┐
│              Database                                    │
│  - SystemSetting (إعدادات المراسلة)                     │
│  - MessagingLog (سجل المراسلات)                         │
│  - RepairNotificationLog (إشعارات الإصلاح)              │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 هيكل الملفات

### Frontend

```
frontend/react-app/src/
├── pages/
│   ├── settings/
│   │   └── MessagingSettingsPage.js      # إعدادات المراسلة
│   ├── invoices/
│   │   └── InvoiceDetailsPage.js         # إرسال الفواتير ✅
│   ├── repairs/
│   │   └── RepairDetailsPage.js          # إرسال طلبات الإصلاح ✅
│   ├── payments/
│   │   ├── PaymentDetailsPage.js         # إرسال تذكيرات الدفع ✅
│   │   └── OverduePaymentsPage.js         # تذكيرات الدفع المتأخرة ✅
│   └── messaging/
│       └── MessagingCenterPage.js         # مركز الرسائل ✅
│
├── services/
│   ├── messagingService.js                # خدمة موحدة ✅
│   └── api.js                             # API service
│
└── components/
    └── messaging/
        ├── SendButton.js                   # زر إرسال قابل لإعادة الاستخدام ✅
        ├── ChannelSelector.js              # اختيار القناة ✅
        ├── MessageStatusBadge.js           # Badge للحالة ✅
        └── MessageLogViewer.js             # عرض سجل المراسلات ✅
```

### Backend

```
backend/
├── routes/
│   └── messaging.js                        # Routes للمراسلة ✅
│
├── controllers/
│   └── messagingController.js             # Controllers ✅
│
├── services/
│   ├── messaging.service.js                # منطق الإرسال الموحد ✅
│   ├── whatsapp.service.js                 # WhatsApp service ✅
│   ├── email.service.js                    # Email service ✅
│   ├── template.service.js                 # Template engine ✅
│   └── pdf.service.js                      # PDF generation (قيد التطوير)
│
├── repositories/
│   └── settingsRepository.js               # إدارة الإعدادات ✅
│
└── migrations/
    └── create_messaging_log_table.sql      # جدول سجل المراسلات ✅
```

---

## 🎯 الميزات الرئيسية

### ✅ المرحلة 1: البنية الأساسية (مكتملة 100%)

1. **قاعدة البيانات:**
   - ✅ جدول `MessagingLog` لتسجيل جميع الرسائل
   - ✅ إعدادات المراسلة في `SystemSetting`

2. **Backend Services:**
   - ✅ `messaging.service.js` - خدمة موحدة للإرسال
   - ✅ `whatsapp.service.js` - دعم WhatsApp Web & API
   - ✅ `email.service.js` - دعم SMTP & HTML Templates
   - ✅ `template.service.js` - محرك القوالب

3. **API Endpoints:**
   - ✅ `POST /api/messaging/send` - إرسال رسالة
   - ✅ `GET /api/messaging/logs` - جلب السجل
   - ✅ `GET /api/messaging/stats` - إحصائيات
   - ✅ `POST /api/messaging/retry` - إعادة المحاولة

---

### ✅ المرحلة 2: الواجهات الأساسية (مكتملة 100%)

1. **المكونات القابلة لإعادة الاستخدام:**
   - ✅ `SendButton.js` - زر إرسال موحد
   - ✅ `ChannelSelector.js` - اختيار القناة
   - ✅ `MessageStatusBadge.js` - Badge للحالة
   - ✅ `MessageLogViewer.js` - عرض السجل

2. **صفحة مركز الرسائل:**
   - ✅ `MessagingCenterPage.js` - صفحة كاملة مع:
     - إحصائيات شاملة
     - فلترة متقدمة
     - قائمة الرسائل
     - إعادة المحاولة

3. **تحديث الصفحات:**
   - ✅ `InvoiceDetailsPage.js` - دعم إرسال الفواتير
   - ✅ `RepairDetailsPage.js` - دعم إرسال طلبات الإصلاح
   - ✅ `PaymentDetailsPage.js` - دعم تذكيرات الدفع
   - ✅ `OverduePaymentsPage.js` - دعم التذكيرات المتأخرة

---

### ✅ المرحلة 3: دعم Email (مكتملة 90%)

1. **قوالب HTML:**
   - ✅ `createInvoiceEmailHTML()` - قالب HTML احترافي للفواتير
   - ✅ تصميم متجاوب وجميل
   - ✅ دعم RTL (من اليمين لليسار)

2. **إرسال Email:**
   - ✅ `sendInvoiceEmail()` - إرسال فواتير عبر Email
   - ✅ جلب بيانات الفاتورة والعميل تلقائياً
   - ✅ بناء محتوى HTML تلقائياً
   - ✅ دعم المرفقات (البنية جاهزة)

3. **تحديث Frontend:**
   - ✅ `SendButton` يجلب البريد الإلكتروني تلقائياً
   - ✅ `ChannelSelector` يدعم Email
   - ✅ رسائل توضيحية للمستخدم
   - ✅ التحقق من البيانات قبل الإرسال

4. **PDF Generation:**
   - ⏳ `pdf.service.js` - البنية جاهزة
   - ⏳ يحتاج مكتبة PDF (puppeteer/pdfkit)

---

## 🔧 كيفية الاستخدام

### إرسال فاتورة

```javascript
import messagingService from '../../services/messagingService';

// إرسال عبر واتساب فقط
await messagingService.sendInvoice(invoiceId, {
  channels: ['whatsapp'],
  recipient: customerPhone
});

// إرسال عبر Email فقط
await messagingService.sendInvoice(invoiceId, {
  channels: ['email'],
  emailRecipient: customerEmail
});

// إرسال عبر كليهما
await messagingService.sendInvoice(invoiceId, {
  channels: ['whatsapp', 'email'],
  recipient: customerPhone,
  emailRecipient: customerEmail,
  attachPDF: true // إرفاق PDF (عند توفر المكتبة)
});
```

### استخدام SendButton Component

```javascript
import SendButton from '../../components/messaging/SendButton';

<SendButton
  entityType="invoice"
  entityId={invoice.id}
  customerId={invoice.customerId}
  recipient={customer.phone}
  template="defaultMessage"
  showChannelSelector={true}
  defaultChannels={['whatsapp', 'email']}
  onSuccess={(result) => console.log('تم الإرسال:', result)}
  onError={(error) => console.error('خطأ:', error)}
/>
```

---

## 📊 إحصائيات النظام

### القنوات المدعومة

1. **WhatsApp:**
   - ✅ WhatsApp Web (رابط مباشر)
   - ✅ WhatsApp API (عند التكوين)

2. **Email:**
   - ✅ SMTP
   - ✅ HTML Templates
   - ⏳ PDF Attachments (قيد التطوير)

---

## 🔍 سجل المراسلات

جميع الرسائل يتم تسجيلها في جدول `MessagingLog` مع:
- نوع الكيان (invoice, repair, payment, etc.)
- معرف الكيان
- القناة المستخدمة
- الحالة (pending, sent, failed, etc.)
- رسالة الخطأ (إن وجدت)
- عدد المحاولات
- التاريخ والوقت

---

## 🐛 المشاكل المحلولة

### ✅ المشكلة 1: خطأ في إرسال طلبات الإصلاح
**الخطأ:** `TypeError: apiService.getRepairById is not a function`

**الحل:**
- تم تصحيح اسم الدالة من `getRepairById` إلى `getRepairRequest`
- تم تصحيح اسم الدالة من `getCustomerById` إلى `getCustomer`

**الملفات المحدثة:**
- `frontend/react-app/src/services/messagingService.js`

---

## 📝 ملاحظات مهمة

### أسماء الدوال في apiService

- ✅ `getRepairRequest(id)` - جلب طلب إصلاح
- ✅ `getCustomer(id)` - جلب عميل
- ✅ `getInvoiceById(id)` - جلب فاتورة
- ❌ `getRepairById` - غير موجود (استخدم `getRepairRequest`)
- ❌ `getCustomerById` - غير موجود (استخدم `getCustomer`)

---

## 🚀 الخطوات التالية

### المرحلة 4: الأتمتة (قيد التخطيط)

1. **إشعارات تلقائية:**
   - إشعار عند استلام طلب إصلاح
   - إشعار عند اكتمال التشخيص
   - إشعار عند اكتمال الإصلاح
   - إشعار عند جاهزية الاستلام

2. **تذكيرات تلقائية:**
   - تذكيرات الدفع المتأخرة
   - تذكيرات المتابعة

3. **إرسال مجمّع:**
   - إرسال رسائل متعددة دفعة واحدة
   - جدولة الإرسال

---

## 📚 المراجع

### الدوكيومنتات ذات الصلة

1. `MESSAGING_PHASE2_FINAL.md` - ملخص المرحلة 2
2. `MESSAGING_PHASE3_SUMMARY.md` - ملخص المرحلة 3
3. `MESSAGING_FRONTEND_EMAIL_UPDATE.md` - تحديثات Email في Frontend
4. `MESSAGING_FRONTEND_TEST_RESULTS.md` - نتائج اختبار Frontend

---

**آخر تحديث:** 2025-01-12  
**الحالة:** ✅ نظام كامل ومستقر
