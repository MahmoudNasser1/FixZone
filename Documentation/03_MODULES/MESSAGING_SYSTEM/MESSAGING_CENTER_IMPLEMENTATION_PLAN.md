# خطة تنفيذ مركز الرسائل والمتابعة - FixZone

## 📋 نظرة عامة

هذه خطة تنفيذ شاملة ومنظمة لبناء مركز الرسائل والمتابعة في FixZone. الخطة مقسمة إلى مراحل واضحة مع تفاصيل كل خطوة.

---

## 🎯 الأهداف الرئيسية

1. **مركز موحد** لإدارة جميع المراسلات
2. **تتبع شامل** لجميع الرسائل المرسلة
3. **أتمتة ذكية** للإشعارات التلقائية
4. **واجهة متابعة** لمراقبة حالة الرسائل
5. **تقارير وإحصائيات** عن أداء المراسلات

---

## 🏗️ البنية المعمارية المقترحة

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React)                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  MessagingCenterPage (صفحة المركز الرئيسية)         │  │
│  │  - قائمة الرسائل                                       │  │
│  │  - فلترة وبحث                                         │  │
│  │  - إحصائيات                                           │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Components (مكونات قابلة لإعادة الاستخدام)          │  │
│  │  - SendButton                                         │  │
│  │  - ChannelSelector                                    │  │
│  │  - MessageLogViewer                                   │  │
│  │  - MessageStatusBadge                                 │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Services                                             │  │
│  │  - messagingService.js (خدمة موحدة)                 │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP/API
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    Backend API                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Routes                                              │   │
│  │  - /api/messaging/send                              │   │
│  │  - /api/messaging/logs                               │   │
│  │  - /api/messaging/stats                              │   │
│  │  - /api/messaging/retry                              │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Controllers                                         │   │
│  │  - messaging.controller.js                           │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Services                                            │   │
│  │  - messaging.service.js (منطق الإرسال)              │   │
│  │  - whatsapp.service.js (WhatsApp API)               │   │
│  │  - email.service.js (SMTP)                           │   │
│  │  - template.service.js (قوالب)                       │   │
│  │  - automation.service.js (أتمتة)                    │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    Database                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  MessagingLog (سجل المراسلات)                       │   │
│  │  - جميع محاولات الإرسال                             │   │
│  │  - الحالات والأخطاء                                  │   │
│  │  - التوقيتات                                         │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  SystemSetting (إعدادات المراسلة)                   │   │
│  │  - قوالب الرسائل                                     │   │
│  │  - إعدادات القنوات                                  │   │
│  │  - إعدادات الأتمتة                                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📅 خطة التنفيذ - المراحل

### المرحلة 1: الأساسيات (الأسبوع الأول)

#### 1.1 قاعدة البيانات

**المهام:**
- [ ] إنشاء جدول `MessagingLog`
- [ ] إنشاء indexes للأداء
- [ ] إنشاء migration script

**الملفات:**
- `backend/migrations/create_messaging_log_table.sql`

**التفاصيل:**
```sql
CREATE TABLE MessagingLog (
    id INT AUTO_INCREMENT PRIMARY KEY,
    entityType ENUM('invoice', 'repair', 'quotation', 'payment', 'custom') NOT NULL,
    entityId INT NOT NULL,
    customerId INT,
    channel ENUM('whatsapp', 'email', 'sms') NOT NULL,
    recipient VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    template VARCHAR(100),
    status ENUM('pending', 'sent', 'delivered', 'failed', 'read') DEFAULT 'pending',
    sentBy INT,
    sentAt DATETIME NULL,
    deliveredAt DATETIME NULL,
    readAt DATETIME NULL,
    errorMessage TEXT,
    retryCount INT DEFAULT 0,
    metadata JSON,
    createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
    updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_entity (entityType, entityId),
    INDEX idx_customer (customerId),
    INDEX idx_recipient (recipient),
    INDEX idx_status (status),
    INDEX idx_sent_at (sentAt),
    INDEX idx_channel (channel)
);
```

**الوقت المتوقع:** 2 ساعات

---

#### 1.2 Backend Services

**المهام:**
- [ ] إنشاء `messaging.service.js`
- [ ] إنشاء `whatsapp.service.js`
- [ ] إنشاء `email.service.js`
- [ ] إنشاء `template.service.js`

**الملفات:**
- `backend/services/messaging.service.js`
- `backend/services/whatsapp.service.js`
- `backend/services/email.service.js`
- `backend/services/template.service.js`

**الوظائف الأساسية:**

**messaging.service.js:**
```javascript
class MessagingService {
  async sendMessage(data) {
    // 1. Validate data
    // 2. Load settings
    // 3. Prepare message from template
    // 4. Send via selected channel
    // 5. Log attempt
    // 6. Return result
  }
  
  async logMessage(data) {
    // Save to MessagingLog
  }
  
  async getMessageLogs(filters) {
    // Get logs with filters
  }
  
  async retryFailedMessage(logId) {
    // Retry failed message
  }
}
```

**الوقت المتوقع:** 8 ساعات

---

#### 1.3 Backend API

**المهام:**
- [ ] إنشاء routes
- [ ] إنشاء controller
- [ ] إضافة authentication middleware
- [ ] إضافة validation

**الملفات:**
- `backend/routes/messaging.routes.js`
- `backend/controllers/messaging.controller.js`

**Endpoints:**
```
POST   /api/messaging/send              # إرسال رسالة
GET    /api/messaging/logs               # الحصول على السجل
GET    /api/messaging/logs/:id            # رسالة محددة
POST   /api/messaging/retry/:id           # إعادة محاولة
GET    /api/messaging/stats               # إحصائيات
DELETE /api/messaging/logs/:id             # حذف سجل
```

**الوقت المتوقع:** 6 ساعات

---

#### 1.4 Frontend Service

**المهام:**
- [ ] إنشاء `messagingService.js`
- [ ] دمج مع API
- [ ] معالجة الأخطاء

**الملفات:**
- `frontend/react-app/src/services/messagingService.js`

**الوظائف:**
```javascript
class MessagingService {
  async sendInvoice(invoiceId, options) {}
  async sendRepairNotification(repairId, type, options) {}
  async sendQuotation(quotationId, options) {}
  async sendPaymentReminder(paymentId, options) {}
  async getMessageLogs(filters) {}
  async retryMessage(logId) {}
}
```

**الوقت المتوقع:** 4 ساعات

---

**إجمالي المرحلة 1:** 20 ساعة عمل

---

### المرحلة 2: الواجهات الأساسية (الأسبوع الثاني)

#### 2.1 صفحة مركز الرسائل

**المهام:**
- [ ] إنشاء `MessagingCenterPage.js`
- [ ] قائمة الرسائل مع pagination
- [ ] فلترة وبحث
- [ ] عرض تفاصيل الرسالة

**الملفات:**
- `frontend/react-app/src/pages/messaging/MessagingCenterPage.js`

**المكونات:**
- MessageList (قائمة الرسائل)
- MessageFilters (فلترة)
- MessageDetails (تفاصيل الرسالة)
- MessageStats (إحصائيات)

**الوقت المتوقع:** 12 ساعة

---

#### 2.2 مكونات قابلة لإعادة الاستخدام

**المهام:**
- [ ] `SendButton.js` - زر إرسال موحد
- [ ] `ChannelSelector.js` - اختيار القناة
- [ ] `MessageStatusBadge.js` - حالة الرسالة
- [ ] `MessageLogViewer.js` - عرض السجل

**الملفات:**
- `frontend/react-app/src/components/messaging/SendButton.js`
- `frontend/react-app/src/components/messaging/ChannelSelector.js`
- `frontend/react-app/src/components/messaging/MessageStatusBadge.js`
- `frontend/react-app/src/components/messaging/MessageLogViewer.js`

**الوقت المتوقع:** 8 ساعات

---

#### 2.3 تحديث الصفحات الموجودة

**المهام:**
- [ ] تحديث `InvoiceDetailsPage.js` لاستخدام الخدمة الموحدة
- [ ] إضافة زر إرسال في `QuotationDetailsPage.js`
- [ ] تحديث `RepairDetailsPage.js`
- [ ] تحديث `OverduePaymentsPage.js`

**الوقت المتوقع:** 8 ساعات

---

**إجمالي المرحلة 2:** 28 ساعة عمل

---

### المرحلة 3: دعم Email (الأسبوع الثالث)

#### 3.1 Email Service

**المهام:**
- [ ] إكمال `email.service.js`
- [ ] دعم SMTP
- [ ] دعم HTML templates
- [ ] دعم المرفقات (PDF)

**الملفات:**
- `backend/services/email.service.js`

**الوظائف:**
```javascript
class EmailService {
  async sendEmail(to, subject, body, attachments = []) {}
  async sendInvoiceEmail(invoiceId, customerEmail) {}
  async sendQuotationEmail(quotationId, customerEmail) {}
}
```

**الوقت المتوقع:** 6 ساعات

---

#### 3.2 تحديث الواجهات

**المهام:**
- [ ] إضافة خيار Email في `ChannelSelector`
- [ ] تحديث `SendButton` لدعم Email
- [ ] إضافة معاينة Email

**الوقت المتوقع:** 4 ساعات

---

#### 3.3 قوالب Email

**المهام:**
- [ ] قوالب HTML للفواتير
- [ ] قوالب HTML للعروض
- [ ] قوالب HTML لطلبات الإصلاح

**الوقت المتوقع:** 4 ساعات

---

**إجمالي المرحلة 3:** 14 ساعة عمل

---

### المرحلة 4: الأتمتة (الأسبوع الرابع)

#### 4.1 Automation Service

**المهام:**
- [ ] إنشاء `automation.service.js`
- [ ] إشعارات تلقائية لطلبات الإصلاح
- [ ] إشعارات تلقائية للفواتير
- [ ] تذكيرات الدفع التلقائية

**الملفات:**
- `backend/services/automation.service.js`

**الوظائف:**
```javascript
class AutomationService {
  async onRepairStatusChange(repairId, oldStatus, newStatus) {}
  async onInvoiceCreated(invoiceId) {}
  async onPaymentReceived(paymentId) {}
  async checkOverduePayments() {}
  async scheduleReminders() {}
}
```

**الوقت المتوقع:** 10 ساعات

---

#### 4.2 Event Listeners

**المهام:**
- [ ] إضافة listeners في controllers
- [ ] ربط الأحداث بالإشعارات
- [ ] اختبار الأتمتة

**الملفات:**
- تحديث controllers موجودة

**الوقت المتوقع:** 6 ساعات

---

#### 4.3 Cron Jobs

**المهام:**
- [ ] إنشاء cron job لتذكيرات الدفع
- [ ] إنشاء cron job للفحص الدوري
- [ ] إدارة الجدولة

**الملفات:**
- `backend/jobs/messagingCronJobs.js`

**الوقت المتوقع:** 4 ساعات

---

**إجمالي المرحلة 4:** 20 ساعة عمل

---

### المرحلة 5: القوالب والإعدادات (الأسبوع الخامس)

#### 5.1 تحديث صفحة الإعدادات

**المهام:**
- [ ] إضافة قوالب للعروض السعرية
- [ ] إضافة قوالب لتذكيرات الدفع
- [ ] إضافة قوالب لإشعارات الطلبات
- [ ] إضافة إعدادات الأتمتة

**الملفات:**
- `frontend/react-app/src/pages/settings/MessagingSettingsPage.js`

**الوقت المتوقع:** 8 ساعات

---

#### 5.2 Template Engine

**المهام:**
- [ ] تحسين `template.service.js`
- [ ] دعم متغيرات متقدمة
- [ ] معاينة القوالب
- [ ] اختبار القوالب

**الوقت المتوقع:** 6 ساعات

---

**إجمالي المرحلة 5:** 14 ساعة عمل

---

### المرحلة 6: التقارير والإحصائيات (الأسبوع السادس)

#### 6.1 صفحة التقارير

**المهام:**
- [ ] إنشاء `MessagingReportsPage.js`
- [ ] إحصائيات الإرسال
- [ ] رسوم بيانية
- [ ] تصدير التقارير

**الملفات:**
- `frontend/react-app/src/pages/messaging/MessagingReportsPage.js`

**الإحصائيات:**
- معدل نجاح الإرسال
- القنوات الأكثر استخداماً
- أوقات الذروة
- الرسائل الفاشلة

**الوقت المتوقع:** 10 ساعات

---

#### 6.2 API للإحصائيات

**المهام:**
- [ ] إضافة endpoints للإحصائيات
- [ ] تجميع البيانات
- [ ] تحسين الأداء

**الوقت المتوقع:** 4 ساعات

---

**إجمالي المرحلة 6:** 14 ساعة عمل

---

### المرحلة 7: التحسينات والاختبارات (الأسبوع السابع)

#### 7.1 Retry Mechanism

**المهام:**
- [ ] آلية إعادة المحاولة التلقائية
- [ ] جدولة إعادة المحاولة
- [ ] معالجة الأخطاء المتقدمة

**الوقت المتوقع:** 6 ساعات

---

#### 7.2 Bulk Send

**المهام:**
- [ ] إرسال مجمع للفواتير
- [ ] إرسال مجمع للعروض
- [ ] معالجة الطوابير

**الوقت المتوقع:** 8 ساعات

---

#### 7.3 الاختبارات

**المهام:**
- [ ] Unit tests
- [ ] Integration tests
- [ ] E2E tests

**الوقت المتوقع:** 8 ساعات

---

#### 7.4 التوثيق

**المهام:**
- [ ] تحديث الدوكيومنت
- [ ] إضافة أمثلة
- [ ] دليل الاستخدام

**الوقت المتوقع:** 4 ساعات

---

**إجمالي المرحلة 7:** 26 ساعة عمل

---

## 📊 ملخص الوقت الإجمالي

| المرحلة | الوقت (ساعة) | الأولوية |
|---------|-------------|----------|
| المرحلة 1: الأساسيات | 20 | عالية جداً |
| المرحلة 2: الواجهات | 28 | عالية |
| المرحلة 3: دعم Email | 14 | متوسطة |
| المرحلة 4: الأتمتة | 20 | متوسطة |
| المرحلة 5: القوالب | 14 | متوسطة |
| المرحلة 6: التقارير | 14 | منخفضة |
| المرحلة 7: التحسينات | 26 | منخفضة |
| **الإجمالي** | **136 ساعة** | |

**الوقت المتوقع:** 7 أسابيع (حوالي 20 ساعة/أسبوع)

---

## 🗂️ هيكل الملفات النهائي

```
backend/
├── migrations/
│   └── create_messaging_log_table.sql
├── routes/
│   └── messaging.routes.js
├── controllers/
│   └── messaging.controller.js
├── services/
│   ├── messaging.service.js
│   ├── whatsapp.service.js
│   ├── email.service.js
│   ├── template.service.js
│   └── automation.service.js
└── jobs/
    └── messagingCronJobs.js

frontend/react-app/src/
├── pages/
│   ├── messaging/
│   │   ├── MessagingCenterPage.js
│   │   └── MessagingReportsPage.js
│   └── settings/
│       └── MessagingSettingsPage.js (تحديث)
├── components/
│   └── messaging/
│       ├── SendButton.js
│       ├── ChannelSelector.js
│       ├── MessageStatusBadge.js
│       └── MessageLogViewer.js
└── services/
    └── messagingService.js
```

---

## ✅ قائمة التحقق الشاملة

### المرحلة 1: الأساسيات
- [ ] جدول MessagingLog
- [ ] messaging.service.js
- [ ] whatsapp.service.js
- [ ] email.service.js
- [ ] template.service.js
- [ ] messaging.routes.js
- [ ] messaging.controller.js
- [ ] messagingService.js (frontend)

### المرحلة 2: الواجهات
- [ ] MessagingCenterPage.js
- [ ] SendButton.js
- [ ] ChannelSelector.js
- [ ] MessageStatusBadge.js
- [ ] MessageLogViewer.js
- [ ] تحديث InvoiceDetailsPage.js
- [ ] تحديث QuotationDetailsPage.js
- [ ] تحديث RepairDetailsPage.js
- [ ] تحديث OverduePaymentsPage.js

### المرحلة 3: Email
- [ ] إكمال email.service.js
- [ ] قوالب HTML
- [ ] دعم المرفقات
- [ ] تحديث ChannelSelector

### المرحلة 4: الأتمتة
- [ ] automation.service.js
- [ ] Event listeners
- [ ] Cron jobs

### المرحلة 5: القوالب
- [ ] تحديث MessagingSettingsPage.js
- [ ] قوالب العروض
- [ ] قوالب التذكيرات
- [ ] إعدادات الأتمتة

### المرحلة 6: التقارير
- [ ] MessagingReportsPage.js
- [ ] API للإحصائيات
- [ ] رسوم بيانية

### المرحلة 7: التحسينات
- [ ] Retry mechanism
- [ ] Bulk send
- [ ] الاختبارات
- [ ] التوثيق

---

## 🔐 اعتبارات الأمان

1. **تشفير البيانات الحساسة:**
   - API tokens
   - SMTP passwords
   - أرقام الهواتف في logs

2. **التحقق من الصلاحيات:**
   - Middleware للتحقق من الصلاحيات
   - تسجيل من أرسل الرسالة

3. **Rate Limiting:**
   - تحديد عدد الرسائل في الدقيقة
   - حماية من الإساءة

---

## 📈 مؤشرات الأداء (KPIs)

1. **معدل نجاح الإرسال:** > 95%
2. **وقت الاستجابة:** < 2 ثانية
3. **معدل إعادة المحاولة:** < 5%
4. **تغطية الاختبارات:** > 80%

---

## 🚀 البدء في التنفيذ

### الخطوة الأولى: قاعدة البيانات

ابدأ بإنشاء جدول `MessagingLog`:

```bash
cd backend/migrations
# إنشاء ملف create_messaging_log_table.sql
```

### الخطوة الثانية: Backend Service

ابدأ بإنشاء `messaging.service.js`:

```bash
cd backend/services
# إنشاء messaging.service.js
```

### الخطوة الثالثة: Frontend Service

ابدأ بإنشاء `messagingService.js`:

```bash
cd frontend/react-app/src/services
# إنشاء messagingService.js
```

---

**آخر تحديث:** 2025-01-12
**الإصدار:** 1.0.0
**الحالة:** جاهز للتنفيذ ✅


