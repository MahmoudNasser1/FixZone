# استراتيجية الإرسال للعملاء - FixZone

## 📋 نظرة عامة

هذا المستند يحدد استراتيجية شاملة لإرسال المراسلات للعملاء في جميع حالات النظام.

---

## 🎯 المبادئ الأساسية

1. **المركزية:** استخدام خدمة موحدة لجميع عمليات الإرسال
2. **المرونة:** دعم قنوات متعددة (WhatsApp, Email, SMS)
3. **التتبع:** تسجيل جميع محاولات الإرسال
4. **الأتمتة:** إرسال تلقائي عند الحاجة
5. **التحكم:** إمكانية الإرسال اليدوي أيضاً

---

## 📍 الأماكن التي تحتاج أزرار/وظائف إرسال

### 1. الفواتير (Invoices)

#### الأماكن:
- ✅ `InvoiceDetailsPage.js` - زر "إرسال للعميل" (تم)
- ❌ `InvoicesPageNew.js` - Bulk send للفواتير المحددة
- ❌ `InvoiceCard.js` - زر إرسال سريع

#### الحالات:
- **إنشاء فاتورة جديدة:** إرسال تلقائي (اختياري من الإعدادات)
- **تحديث فاتورة:** إرسال يدوي فقط
- **استلام دفعة:** إرسال إشعار تأكيد (تلقائي)
- **تأخر الدفع:** تذكير تلقائي (يومي/أسبوعي)

#### القنوات:
- WhatsApp (Web/API)
- Email (مع PDF مرفق)

---

### 2. طلبات الإصلاح (Repair Requests)

#### الأماكن:
- ✅ `RepairDetailsPage.js` - زر "إرسال رسالة واتساب" (موجود لكن يحتاج تحسين)
- ❌ `RepairsPage.js` - Bulk send للطلبات المحددة

#### الحالات والإشعارات:

| الحالة | نوع الإشعار | تلقائي/يدوي | القنوات |
|--------|------------|------------|---------|
| `received` | استلام الطلب | تلقائي | WhatsApp, Email |
| `diagnosed` | اكتمال التشخيص | تلقائي | WhatsApp, Email |
| `quote_ready` | جاهزية العرض | تلقائي | WhatsApp, Email |
| `quote_sent` | تم إرسال العرض | تلقائي | WhatsApp, Email |
| `in_progress` | بدء الإصلاح | يدوي/تلقائي | WhatsApp |
| `completed` | اكتمال الإصلاح | تلقائي | WhatsApp, Email |
| `ready_pickup` | جاهز للاستلام | تلقائي | WhatsApp, Email, SMS |
| `delivered` | تم التسليم | تلقائي | WhatsApp, Email |

#### القوالب المطلوبة:
- رسالة استلام الطلب (موجود)
- رسالة اكتمال التشخيص
- رسالة جاهزية العرض
- رسالة بدء الإصلاح
- رسالة اكتمال الإصلاح
- رسالة جاهزية الاستلام
- رسالة تأكيد التسليم

---

### 3. العروض السعرية (Quotations)

#### الأماكن:
- ❌ `QuotationDetailsPage.js` - زر "إرسال للعميل" (غير موجود)
- ❌ `QuotationsPage.js` - Bulk send للعروض المحددة
- ❌ `QuotationForm.js` - خيار "حفظ وإرسال"

#### الحالات:
- **إنشاء عرض جديد:** إرسال تلقائي (اختياري)
- **تحديث عرض:** إرسال يدوي فقط
- **موافقة العميل:** إشعار تأكيد (تلقائي)
- **رفض العميل:** إشعار (تلقائي)

#### القنوات:
- WhatsApp (مع رابط العرض)
- Email (مع PDF مرفق)

#### القوالب المطلوبة:
- رسالة إرسال عرض السعر
- رسالة تأكيد الموافقة
- رسالة تأكيد الرفض

---

### 4. المدفوعات (Payments)

#### الأماكن:
- ❌ `PaymentDetailsPage.js` - زر "إرسال إشعار" (غير موجود)
- ❌ `OverduePaymentsPage.js` - وظيفة `handleSendReminder` (غير مكتملة)
- ❌ `PaymentsPage.js` - Bulk send للتذكيرات

#### الحالات:
- **استلام دفعة:** إشعار تأكيد (تلقائي)
- **تأخر الدفع:** تذكير تلقائي (يومي/أسبوعي)
- **دفعة جزئية:** إشعار بالمبلغ المتبقي (تلقائي)

#### القنوات:
- WhatsApp
- Email
- SMS (للتذكيرات المهمة)

#### القوالب المطلوبة:
- رسالة تأكيد استلام الدفعة
- رسالة تذكير بالدفع المتأخر
- رسالة المبلغ المتبقي

---

### 5. العملاء (Customers)

#### الأماكن:
- ❌ `CustomerDetailsPage.js` - زر "إرسال رسالة" (غير موجود)
- ❌ `CustomersPage.js` - Bulk send للعملاء المحددين

#### الحالات:
- **رسالة مخصصة:** إرسال يدوي فقط
- **تذكير بزيارة:** إرسال تلقائي (اختياري)

---

## 🔄 خريطة تدفق الإرسال

```
┌─────────────────────────────────────────────────────────┐
│                    حدث في النظام                        │
│  (إنشاء فاتورة، تغيير حالة طلب، استلام دفعة...)        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          فحص الإعدادات (Messaging Settings)             │
│  - هل الإرسال التلقائي مفعل؟                           │
│  - ما هي القنوات المفعلة؟                              │
│  - ما هي القوالب المستخدمة؟                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          تحضير الرسالة (Message Preparation)          │
│  - تحميل القالب                                        │
│  - استبدال المتغيرات                                   │
│  - إضافة البيانات المطلوبة                             │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          اختيار القناة (Channel Selection)              │
│  - WhatsApp (Web/API)                                   │
│  - Email                                               │
│  - SMS (للمستقبل)                                      │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          إرسال الرسالة (Send Message)                   │
│  - محاولة الإرسال                                      │
│  - معالجة الأخطاء                                      │
│  - Retry عند الفشل                                      │
│  - Fallback للقناة البديلة                             │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          تسجيل الإرسال (Log Message)                   │
│  - حفظ في MessagingLog                                 │
│  - تحديث حالة الإرسال                                  │
│  - حفظ الأخطاء إن وجدت                                 │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          إشعار المستخدم (Notify User)                   │
│  - Success notification                                 │
│  - Error notification                                  │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 قوالب الرسائل المطلوبة

### 1. الفواتير

#### قالب إرسال الفاتورة:
```
مرحباً {customerName} 👋

فاتورة رقم: #{invoiceId}
تاريخ الفاتورة: {invoiceDate}

المبلغ الإجمالي: {totalAmount} {currency}
المبلغ المدفوع: {amountPaid} {currency}
المبلغ المتبقي: {remainingAmount} {currency}

{invoiceLink}

شكراً لثقتك في Fix Zone 🛠️
للاستفسار: https://fixzzone.com/contact/
```

#### قالب تأكيد استلام الدفعة:
```
مرحباً {customerName}

تم استلام دفعة بمبلغ {paymentAmount} {currency}
فاتورة رقم: #{invoiceId}

المبلغ المتبقي: {remainingAmount} {currency}

شكراً لك
```

#### قالب تذكير بالدفع المتأخر:
```
مرحباً {customerName}

تذكير ودي: فاتورة رقم #{invoiceId} مستحقة الدفع
المبلغ: {totalAmount} {currency}
تاريخ الاستحقاق: {dueDate}

{paymentLink}

نرجو سرعة السداد
```

---

### 2. طلبات الإصلاح

#### قالب استلام الطلب (موجود):
```
جهازك وصل Fix Zone يا فندم

ده ملخص الطلب:
• رقم الطلب: {repairNumber}
• الجهاز: {deviceInfo}
• المشكلة: {problem}

تقدر تشوف التحديثات أول بأول من هنا:
{trackingUrl}

فريق الفنيين هيبدأ الفحص خلال الساعات القادمة.
```

#### قالب اكتمال التشخيص:
```
مرحباً {customerName}

اكتمل تشخيص جهازك #{repairNumber}

التشخيص: {diagnosis}
التكلفة المتوقعة: {estimatedCost} {currency}

{quotationLink}

في انتظار موافقتك للبدء في الإصلاح
```

#### قالب جاهزية الاستلام:
```
مرحباً {customerName} 🎉

جهازك #{repairNumber} جاهز للاستلام!

يمكنك استلامه من:
{location}

{invoiceLink}

شكراً لثقتك في Fix Zone
```

---

### 3. العروض السعرية

#### قالب إرسال العرض:
```
مرحباً {customerName}

عرض سعر لطلب الإصلاح #{repairNumber}

المبلغ الإجمالي: {totalAmount} {currency}
صالح حتى: {validUntil}

{quotationLink}

في انتظار موافقتك
```

---

## ⚙️ إعدادات الإرسال التلقائي

### إعدادات مقترحة في `MessagingSettingsPage.js`:

```javascript
{
  automation: {
    enabled: true,
    invoice: {
      sendOnCreate: false,  // إرسال تلقائي عند الإنشاء
      sendOnUpdate: false,  // إرسال تلقائي عند التحديث
      sendOnPayment: true,  // إرسال تأكيد عند الدفع
      reminderEnabled: true, // تذكير بالدفع المتأخر
      reminderDays: [1, 3, 7], // أيام التذكير
    },
    repair: {
      sendOnReceived: true,    // إرسال عند الاستلام
      sendOnDiagnosed: true,   // إرسال عند التشخيص
      sendOnCompleted: true,   // إرسال عند الاكتمال
      sendOnReady: true,       // إرسال عند الجاهزية
    },
    quotation: {
      sendOnCreate: true,      // إرسال تلقائي عند الإنشاء
      sendOnApproval: true,    // إرسال عند الموافقة
    },
    payment: {
      sendOnReceived: true,    // إرسال تأكيد عند الاستلام
      reminderEnabled: true,   // تذكير بالدفع المتأخر
      reminderDays: [1, 3, 7, 14], // أيام التذكير
    }
  }
}
```

---

## 🎨 تصميم الأزرار والواجهات

### 1. زر الإرسال الأساسي

**الموقع:** في صفحة التفاصيل (Details Page)

**التصميم:**
```jsx
<SimpleButton 
  onClick={handleSend}
  className="bg-green-600 hover:bg-green-700"
>
  <Send className="w-4 h-4 ml-2" />
  إرسال للعميل
</SimpleButton>
```

### 2. قائمة منسدلة لاختيار القناة

**الموقع:** بجانب زر الإرسال

**التصميم:**
```jsx
<DropdownMenu>
  <DropdownMenuTrigger>
    <SimpleButton variant="outline">
      <Send className="w-4 h-4 ml-2" />
      إرسال
      <ChevronDown className="w-4 h-4 mr-2" />
    </SimpleButton>
  </DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuItem onClick={() => sendViaWhatsApp()}>
      <MessageSquare className="w-4 h-4 ml-2" />
      واتساب
    </DropdownMenuItem>
    <DropdownMenuItem onClick={() => sendViaEmail()}>
      <Mail className="w-4 h-4 ml-2" />
      بريد إلكتروني
    </DropdownMenuItem>
    <DropdownMenuItem onClick={() => sendViaBoth()}>
      <Send className="w-4 h-4 ml-2" />
      واتساب + بريد
    </DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

### 3. Bulk Send

**الموقع:** في صفحات القوائم (List Pages)

**التصميم:**
```jsx
<SimpleButton
  onClick={handleBulkSend}
  disabled={selectedItems.length === 0}
  variant="outline"
>
  <Send className="w-4 h-4 ml-2" />
  إرسال المحدد ({selectedItems.length})
</SimpleButton>
```

---

## 📊 جدول أولويات التنفيذ

| المهمة | الأولوية | الحالة | الموقع |
|--------|---------|--------|--------|
| خدمة موحدة للمراسلة | عالية | ❌ | `services/messagingService.js` |
| API endpoints | عالية | ❌ | `backend/routes/messaging.routes.js` |
| جدول تسجيل المراسلات | عالية | ❌ | Migration |
| تحديث InvoiceDetailsPage | عالية | ✅ | تم |
| إضافة دعم Email | متوسطة | ❌ | `messagingService.js` |
| إرسال العروض السعرية | متوسطة | ❌ | `QuotationDetailsPage.js` |
| تذكيرات الدفع | متوسطة | ❌ | `OverduePaymentsPage.js` |
| إشعارات تلقائية | متوسطة | ❌ | `automationService.js` |
| Bulk send | منخفضة | ❌ | List pages |
| جدولة الإرسال | منخفضة | ❌ | Future feature |

---

## 🔐 الأمان والخصوصية

1. **تشفير البيانات الحساسة:**
   - API tokens
   - SMTP passwords
   - أرقام الهواتف (في logs)

2. **التحقق من الصلاحيات:**
   - فقط المستخدمون المصرح لهم يمكنهم الإرسال
   - تسجيل من أرسل الرسالة

3. **حماية البيانات:**
   - عدم عرض أرقام الهواتف في logs
   - حذف البيانات القديمة بعد فترة

---

## 📈 المقاييس والتقارير

### مقاييس مقترحة:

1. **معدل نجاح الإرسال:**
   - عدد الرسائل المرسلة بنجاح
   - عدد الرسائل الفاشلة
   - معدل النجاح %

2. **القنوات الأكثر استخداماً:**
   - WhatsApp vs Email
   - Web vs API

3. **أوقات الإرسال:**
   - أوقات الذروة
   - أفضل الأوقات للإرسال

4. **معدل القراءة:**
   - عدد الرسائل المقروءة
   - متوسط وقت القراءة

---

## ✅ قائمة التحقق

### المرحلة 1: الأساسيات
- [ ] إنشاء `messagingService.js`
- [ ] إنشاء API endpoints
- [ ] إنشاء جدول `MessagingLog`
- [ ] تحديث `InvoiceDetailsPage` (تم ✅)

### المرحلة 2: التوسع
- [ ] إضافة دعم Email
- [ ] إضافة قوالب لجميع الحالات
- [ ] إضافة أزرار إرسال في جميع الصفحات
- [ ] إكمال وظيفة تذكيرات الدفع

### المرحلة 3: الأتمتة
- [ ] إضافة إشعارات تلقائية
- [ ] إضافة retry mechanism
- [ ] إضافة تقارير المراسلات
- [ ] إضافة bulk send

---

**آخر تحديث:** 2025-01-12
**الإصدار:** 1.0.0

