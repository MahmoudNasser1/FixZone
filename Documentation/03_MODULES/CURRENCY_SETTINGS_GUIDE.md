# 🇪🇬 دليل إعدادات العملة - نظام FixZone

## 📍 الموقع والعملة الافتراضية

**الدولة:** جمهورية مصر العربية  
**العملة:** الجنيه المصري (EGP)  
**الرمز:** ج.م  
**التنسيق:** ar-EG

---

## ⚙️ الإعدادات الافتراضية

### ملف الإعدادات الرئيسي
📁 `frontend/react-app/src/config/settings.js`

```javascript
export const defaultSettings = {
  company: {
    name: 'FixZone',
    address: 'مول البستان التجاري - الدور الأرضي',
    city: 'القاهرة',
    country: 'مصر',
    phone: '01270388043',
    website: '',
    logoUrl: '/logo.png',
  },
  currency: {
    code: 'EGP',                    // رمز العملة الدولي
    symbol: 'ج.م',                  // رمز العملة المحلي
    name: 'الجنيه المصري',         // اسم العملة
    locale: 'ar-EG',                // التنسيق المحلي
    minimumFractionDigits: 2,       // عدد الأرقام العشرية
    position: 'after',              // موضع الرمز (بعد الرقم)
  },
}
```

---

## 🔄 كيفية تغيير إعدادات العملة

### من واجهة المستخدم:

1. اذهب إلى: **http://localhost:3000/settings**
2. اختر تبويب **"العملة"**
3. قم بتعديل:
   - **رمز العملة (Code):** EGP
   - **الرمز (Symbol):** ج.م
   - **اسم العملة:** الجنيه المصري
   - **المنطقة (Locale):** ar-EG
   - **الأجزاء العشرية:** 2

4. اضغط **"حفظ الإعدادات"**

### من الكود:

يمكنك تعديل الإعدادات الافتراضية في:
```
frontend/react-app/src/config/settings.js
```

---

## 📊 أماكن استخدام العملة في النظام

### 1. **عرض الأموال (formatMoney)**

يستخدم في جميع أنحاء النظام:

```javascript
import { useSettings } from '../../context/SettingsContext';

function MyComponent() {
  const { formatMoney } = useSettings();
  
  return <div>{formatMoney(1000)}</div>;
  // النتيجة: ١٬٠٠٠٫٠٠ ج.م
}
```

### 2. **الصفحات التي تستخدم العملة:**

- ✅ صفحة طلبات الإصلاح
- ✅ صفحة الفواتير
- ✅ صفحة المدفوعات
- ✅ صفحة الخدمات
- ✅ صفحة المخزون
- ✅ صفحة التقارير المالية
- ✅ لوحة التحكم (Dashboard)

### 3. **قاعدة البيانات:**

العملة مخزنة كـ `VARCHAR` في جداول:
- `Invoice.currency`
- `Payment.currency`
- `Service.basePrice`

---

## 🌍 العملات المدعومة

النظام يدعم حالياً:

| العملة | الرمز | الكود |
|-------|------|------|
| الجنيه المصري | ج.م | EGP |
| الدولار الأمريكي | $ | USD |
| اليورو | € | EUR |

**العملة الافتراضية:** الجنيه المصري (EGP)

---

## 🔧 التخصيصات المطبقة

### ✅ التغييرات المنفذة:

1. **الإعدادات الافتراضية:**
   - العملة: EGP (الجنيه المصري)
   - الرمز: ج.م
   - التنسيق: ar-EG

2. **الصفحات المحدثة:**
   - `NewRepairPageEnhanced.js` - تغيير "ريال" إلى "ج.م"
   - `CreateInvoicePage.js` - إزالة "ريال سعودي" وإضافة EGP كافتراضي

3. **الإعدادات:**
   - إضافة حقل `city` (القاهرة)
   - إضافة حقل `position` لموضع رمز العملة

---

## 📝 أمثلة على الاستخدام

### مثال 1: عرض سعر خدمة

```javascript
const { formatMoney } = useSettings();

<div>
  السعر: {formatMoney(150)}
  {/* النتيجة: ١٥٠٫٠٠ ج.م */}
</div>
```

### مثال 2: عرض إجمالي فاتورة

```javascript
const { formatMoney } = useSettings();

<div>
  الإجمالي: {formatMoney(invoice.totalAmount)}
  {/* النتيجة: ٢٬٥٠٠٫٠٠ ج.م */}
</div>
```

### مثال 3: إدخال مبلغ

```html
<input 
  type="number" 
  placeholder="المبلغ بالجنيه المصري"
  step="0.01"
  min="0"
/>
```

---

## 🎨 التنسيق المحلي

النظام يستخدم `Intl.NumberFormat` لتنسيق الأموال:

```javascript
new Intl.NumberFormat('ar-EG', {
  style: 'currency',
  currency: 'EGP',
  minimumFractionDigits: 2
}).format(1000)

// النتيجة: ١٬٠٠٠٫٠٠ ج.م
```

### خصائص التنسيق:

- ✅ الأرقام العربية (١٢٣٤٥٦٧٨٩٠)
- ✅ الفواصل العشرية (٫)
- ✅ فواصل الآلاف (٬)
- ✅ رمز العملة بعد الرقم (ج.م)

---

## 🔄 تحديث العملة في إصدارات قادمة

إذا أردت إضافة عملات جديدة:

### 1. تحديث قائمة العملات

في `CreateInvoicePage.js`:

```javascript
<select name="currency">
  <option value="EGP">جنيه مصري (EGP) - ج.م</option>
  <option value="USD">دولار أمريكي (USD)</option>
  <option value="EUR">يورو (EUR)</option>
  <option value="SAR">ريال سعودي (SAR)</option> // إضافة جديدة
</select>
```

### 2. تحديث دالة التنسيق

في `settings.js`:

```javascript
export function formatMoney(amount, settings) {
  const cfg = (settings && settings.currency) || defaultSettings.currency;
  // معالجة خاصة لعملات معينة إذا لزم الأمر
  return new Intl.NumberFormat(cfg.locale, {
    style: 'currency',
    currency: cfg.code,
    minimumFractionDigits: cfg.minimumFractionDigits ?? 2,
  }).format(Number(amount || 0));
}
```

---

## 📞 الدعم والمساعدة

إذا كنت بحاجة إلى:
- تغيير العملة الافتراضية
- إضافة عملات جديدة
- تخصيص التنسيق

راجع:
1. `frontend/react-app/src/config/settings.js`
2. `frontend/react-app/src/context/SettingsContext.js`
3. `frontend/react-app/src/pages/settings/SystemSettingsPage.js`

---

## ✅ نقاط التحقق

- [x] العملة الافتراضية: EGP (الجنيه المصري)
- [x] الرمز: ج.م
- [x] التنسيق: ar-EG
- [x] الأرقام العشرية: 2
- [x] الموقع: مصر
- [x] المدينة: القاهرة
- [x] الهاتف: +20 (رمز مصر)
- [x] إزالة "ريال" من النظام
- [x] تحديث جميع الصفحات
- [x] واجهة إعدادات العملة في الإعدادات

---

**آخر تحديث:** 2 أكتوبر 2025  
**الإصدار:** 1.0  
**النظام:** FixZone ERP - نظام إدارة الصيانة

