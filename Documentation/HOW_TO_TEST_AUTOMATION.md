# 🧪 دليل اختبار الإشعارات الأوتوماتيكية

## 📋 نظرة عامة

الإشعارات الأوتوماتيكية تعمل في الحالات التالية:
1. **تغيير حالة طلب الإصلاح** - يتم إرسال إشعار تلقائياً عند تغيير الحالة
2. **إنشاء فاتورة جديدة** - يتم إرسال إشعار عند إنشاء فاتورة
3. **تذكيرات الدفع المتأخرة** - يتم إرسال تذكيرات يومية للفواتير المتأخرة
4. **تذكيرات قبل الاستحقاق** - يتم إرسال تذكيرات قبل موعد الاستحقاق

---

## 🔧 خطوات الإعداد قبل الاختبار

### 1. تفعيل الأتمتة

1. افتح صفحة **إعدادات المراسلة**
2. اذهب إلى تبويب **"الأتمتة"**
3. تأكد من تفعيل **"تفعيل الأتمتة"** ✅
4. حدد القنوات الافتراضية (واتساب و/أو بريد إلكتروني)
5. فعّل الإشعارات المطلوبة:
   - ✅ إشعار عند استلام الطلب (RECEIVED)
   - ✅ إشعار عند اكتمال التشخيص (INSPECTION)
   - ✅ إشعار عند بانتظار الموافقة (AWAITING_APPROVAL)
   - إلخ...
6. اضغط **"حفظ"**

### 2. التحقق من إعدادات القنوات

**واتساب:**
- تأكد من تفعيل واتساب في تبويب "القنوات"
- إذا كنت تستخدم WhatsApp Web، تأكد من فتح WhatsApp Web في المتصفح
- إذا كنت تستخدم WhatsApp API، تأكد من إدخال API URL و Token

**البريد الإلكتروني:**
- تأكد من تفعيل البريد الإلكتروني
- أدخل إعدادات SMTP بشكل صحيح
- اختبر الإعدادات باستخدام زر "تجربة"

---

## 🧪 طرق الاختبار

### الطريقة 1: اختبار يدوي (الأسهل)

#### أ) اختبار إشعارات طلبات الإصلاح

1. **إنشاء طلب إصلاح جديد:**
   - اذهب إلى **"إدارة الإصلاحات"** → **"إضافة طلب جديد"**
   - أدخل بيانات العميل (يجب أن يكون لديه رقم هاتف أو بريد إلكتروني)
   - احفظ الطلب
   - ✅ **يجب أن يرسل إشعار تلقائياً** عند إنشاء الطلب (إذا كان الحالة RECEIVED)

2. **تغيير حالة طلب الإصلاح:**
   - افتح طلب إصلاح موجود
   - غيّر الحالة من القائمة المنسدلة (مثلاً: من RECEIVED إلى INSPECTION)
   - احفظ التغييرات
   - ✅ **يجب أن يرسل إشعار تلقائياً** للعميل

**الحالات المدعومة:**
- `RECEIVED` → إشعار عند الاستلام
- `INSPECTION` → إشعار عند اكتمال التشخيص
- `AWAITING_APPROVAL` → إشعار عند بانتظار الموافقة
- `UNDER_REPAIR` → إشعار عند البدء في الإصلاح
- `WAITING_PARTS` → إشعار عند بانتظار قطع الغيار
- `READY_FOR_PICKUP` → إشعار عند جاهزية الاستلام
- `READY_FOR_DELIVERY` / `DELIVERED` → إشعار عند اكتمال الإصلاح
- `REJECTED` → إشعار عند رفض الطلب
- `ON_HOLD` → إشعار عند تعليق الطلب

#### ب) اختبار إشعارات الفواتير

1. **إنشاء فاتورة جديدة:**
   - اذهب إلى **"النظام المالي"** → **"الفواتير"** → **"فاتورة جديدة"**
   - اختر عميل (يجب أن يكون لديه رقم هاتف أو بريد إلكتروني)
   - أضف العناصر واحفظ الفاتورة
   - ✅ **يجب أن يرسل إشعار تلقائياً** للعميل

#### ج) اختبار تذكيرات الدفع

**للتذكيرات المتأخرة:**
1. أنشئ فاتورة بتاريخ استحقاق في الماضي (مثلاً: أمس)
2. تأكد من أن الفاتورة غير مدفوعة أو مدفوعة جزئياً
3. انتظر حتى الساعة 9 صباحاً (أو شغّل Cron Job يدوياً - انظر الطريقة 2)
4. ✅ **يجب أن يرسل تذكير تلقائياً**

**للتذكيرات قبل الاستحقاق:**
1. أنشئ فاتورة بتاريخ استحقاق بعد 3 أيام (أو حسب الإعدادات)
2. تأكد من أن الفاتورة غير مدفوعة
3. انتظر حتى الساعة 10 صباحاً (أو شغّل Cron Job يدوياً)
4. ✅ **يجب أن يرسل تذكير تلقائياً**

---

### الطريقة 2: اختبار باستخدام Script (أسرع)

#### استخدام Script الاختبار المدمج

```bash
cd /opt/lampp/htdocs/FixZone/backend
node scripts/test-automation.js
```

**ما يفعله هذا Script:**
1. ✅ يتحقق من تفعيل الأتمتة
2. ✅ يعرض القنوات الافتراضية
3. ✅ يختبر إشعارات طلبات الإصلاح (يغيّر الحالة إلى RECEIVED)
4. ✅ يختبر إشعارات الفواتير
5. ✅ يختبر تذكيرات الدفع المتأخرة
6. ✅ يختبر تذكيرات قبل الاستحقاق
7. ✅ يعرض آخر 5 رسائل مرسلة

**مثال على الناتج:**
```
🧪 اختبار نظام الأتمتة
==================================================

1️⃣ التحقق من تفعيل الأتمتة...
   ✅ الأتمتة: مفعلة

2️⃣ القنوات الافتراضية...
   ✅ القنوات: whatsapp

3️⃣ اختبار إشعارات طلبات الإصلاح...
   📋 طلب الإصلاح: #123
   📊 الحالة الحالية: RECEIVED
   
   🔄 اختبار تغيير الحالة إلى RECEIVED...
   ✅ تم إرسال الإشعار بنجاح

4️⃣ اختبار إشعارات الفواتير...
   📋 الفاتورة: #456
   
   🔄 اختبار إشعار إنشاء الفاتورة...
   ✅ تم إرسال الإشعار بنجاح

5️⃣ اختبار تذكيرات الدفع المتأخرة...
   ✅ تم إرسال 2 تذكير للدفعات المتأخرة

6️⃣ اختبار تذكيرات قبل الاستحقاق...
   ✅ تم إرسال 1 تذكير قبل الاستحقاق

7️⃣ آخر الرسائل المرسلة (آخر 5)...
   1. repair #123
      القناة: whatsapp
      الحالة: sent
      المستلم: 201234567890
      الوقت: 2025-01-12 10:30:00
```

---

### الطريقة 3: اختبار Cron Jobs يدوياً

#### تشغيل تذكيرات الدفع المتأخرة يدوياً

```bash
cd /opt/lampp/htdocs/FixZone/backend
node -e "
const automationService = require('./services/automation.service');
automationService.checkOverduePayments().then(count => {
  console.log('✅ تم إرسال', count, 'تذكير للدفعات المتأخرة');
  process.exit(0);
}).catch(err => {
  console.error('❌ خطأ:', err);
  process.exit(1);
});
"
```

#### تشغيل تذكيرات قبل الاستحقاق يدوياً

```bash
cd /opt/lampp/htdocs/FixZone/backend
node -e "
const automationService = require('./services/automation.service');
automationService.sendPaymentReminders().then(count => {
  console.log('✅ تم إرسال', count, 'تذكير قبل الاستحقاق');
  process.exit(0);
}).catch(err => {
  console.error('❌ خطأ:', err);
  process.exit(1);
});
"
```

---

## 📊 التحقق من النتائج

### 1. التحقق من سجل الرسائل

**من الواجهة:**
- اذهب إلى **"مركز الرسائل"** (Messaging Center)
- ستجد جميع الرسائل المرسلة مع تفاصيلها

**من قاعدة البيانات:**
```sql
SELECT 
  id, entityType, entityId, channel, status, 
  recipient, sentAt, errorMessage, createdAt
FROM MessagingLog 
ORDER BY createdAt DESC 
LIMIT 10;
```

### 2. التحقق من وصول الرسائل

**واتساب:**
- تحقق من وصول الرسالة على رقم الهاتف المحدد
- إذا كنت تستخدم WhatsApp Web، تحقق من المتصفح

**البريد الإلكتروني:**
- تحقق من صندوق الوارد (والبريد المزعج)
- تحقق من سجلات SMTP في Backend

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا يتم إرسال الإشعارات

**التحقق من:**
1. ✅ هل الأتمتة مفعلة في الإعدادات؟
2. ✅ هل القنوات (واتساب/بريد) مفعلة؟
3. ✅ هل الإشعار المحدد مفعل في الإعدادات؟
4. ✅ هل العميل لديه رقم هاتف أو بريد إلكتروني؟
5. ✅ تحقق من Console في Backend للأخطاء

**الأوامر للتحقق:**
```bash
# التحقق من تفعيل الأتمتة
cd /opt/lampp/htdocs/FixZone/backend
node -e "
const automationService = require('./services/automation.service');
automationService.isAutomationEnabled().then(enabled => {
  console.log('الأتمتة:', enabled ? 'مفعلة ✅' : 'معطلة ❌');
  process.exit(0);
});
"
```

### المشكلة: رسائل واتساب لا تصل

**التحقق من:**
1. ✅ هل WhatsApp Web مفتوح في المتصفح؟
2. ✅ هل API URL و Token صحيحان (إذا كنت تستخدم API)؟
3. ✅ تحقق من Console في Backend

### المشكلة: رسائل البريد الإلكتروني لا تصل

**التحقق من:**
1. ✅ هل إعدادات SMTP صحيحة؟
2. ✅ هل البريد الإلكتروني للعميل صحيح؟
3. ✅ تحقق من صندوق البريد المزعج
4. ✅ تحقق من سجلات SMTP في Backend

---

## 📝 ملاحظات مهمة

1. **الحد الأدنى بين التذكيرات:**
   - يتم التحقق من عدم إرسال تذكير في آخر X يوم (حسب الإعدادات)
   - إذا أرسلت تذكيراً اليوم، لن يتم إرسال آخر حتى يمر الحد الأدنى

2. **الجدولة:**
   - تذكيرات المتأخرة: يومياً الساعة 9 صباحاً (أو حسب الإعدادات)
   - تذكيرات قبل الاستحقاق: يومياً الساعة 10 صباحاً (أو حسب الإعدادات)
   - يمكن تغيير الجدولة من إعدادات الأتمتة

3. **القوالب:**
   - يمكن تخصيص القوالب من تبويب "القوالب" في الإعدادات
   - يمكن إضافة قوالب مخصصة وربطها بحالات معينة

---

## 🎯 سيناريوهات اختبار موصى بها

### السيناريو 1: دورة كاملة لطلب إصلاح
1. إنشاء طلب إصلاح جديد → ✅ إشعار RECEIVED
2. تغيير الحالة إلى INSPECTION → ✅ إشعار التشخيص
3. تغيير الحالة إلى AWAITING_APPROVAL → ✅ إشعار بانتظار الموافقة
4. تغيير الحالة إلى UNDER_REPAIR → ✅ إشعار قيد الإصلاح
5. تغيير الحالة إلى READY_FOR_PICKUP → ✅ إشعار جاهزية الاستلام
6. إنشاء فاتورة → ✅ إشعار الفاتورة
7. تغيير الحالة إلى DELIVERED → ✅ إشعار التسليم

### السيناريو 2: تذكيرات الدفع
1. إنشاء فاتورة بتاريخ استحقاق أمس → انتظر 9 صباحاً → ✅ تذكير متأخر
2. إنشاء فاتورة بتاريخ استحقاق بعد 3 أيام → انتظر 10 صباحاً → ✅ تذكير قبل الاستحقاق

---

## ✅ قائمة التحقق السريعة

- [ ] تفعيل الأتمتة في الإعدادات
- [ ] تفعيل القنوات (واتساب/بريد)
- [ ] تفعيل الإشعارات المطلوبة
- [ ] التحقق من بيانات العميل (هاتف/بريد)
- [ ] اختبار إشعار طلب إصلاح
- [ ] اختبار إشعار فاتورة
- [ ] اختبار تذكيرات الدفع (يدوياً أو عبر Cron)
- [ ] التحقق من سجل الرسائل
- [ ] التحقق من وصول الرسائل فعلياً

---

## 🚀 نصائح للاختبار

1. **استخدم رقم هاتف تجريبي** للتأكد من وصول الرسائل
2. **استخدم بريد إلكتروني تجريبي** يمكنك الوصول إليه بسهولة
3. **راقب Console في Backend** لرؤية السجلات والأخطاء
4. **استخدم Script الاختبار** للتحقق السريع من كل شيء
5. **اختبر كل حالة** على حدة للتأكد من عملها بشكل صحيح

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من Console في Backend
2. تحقق من سجل الرسائل في قاعدة البيانات
3. استخدم Script الاختبار للتحقق من الإعدادات
4. راجع هذا الدليل مرة أخرى

---

**آخر تحديث:** 2025-01-12


