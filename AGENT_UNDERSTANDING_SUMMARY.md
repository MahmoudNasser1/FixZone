# 🎯 ملخص فهم النظام - AGENT UNDERSTANDING SUMMARY

**الحالة:** جاهز للمراجعة  
**الغرض:** توثيق فهم مساعد الذكاء الاصطناعي لنظام FixZone ERP بناءً على التحليلات الأولية، وتحديد النقاط الأساسية التي سيبنى عليها العمل المستقبلي في ضمان الجودة.

---

### 1. ملخص فهم النظام ومنطق العمل (Business Logic)

لقد فهمت أن FixZone ERP هو نظام متكامل لإدارة ورش الإصلاح، ويتمحور حول تدفق عمل أساسي ومترابط:

1.  **طلب الإصلاح (Repair Request):** هو نقطة البداية. يتم تسجيل بيانات العميل والجهاز والمشكلة.
2.  **التشخيص والصرف (Diagnostics & Consumption):** يقوم الفني بتحديد الخدمات المطلوبة واستهلاك قطع الغيار من المخزون. هذه الخطوة تخلق رابطًا مباشرًا مع وحدة المخزون.
3.  **الفوترة (Invoicing):** يتم إنشاء فاتورة بناءً على طلب الإصلاح (لتشمل الخدمات وقطع الغيار) أو بشكل مباشر لبيع منتج. النظام يميز بوضوح بين فواتير البيع (`sale`) وفواتير الشراء (`purchase`).
4.  **الدفع (Payment):** يتم تسجيل الدفعات المرتبطة بالفواتير، مما يؤثر مباشرة على حالة الفاتورة (مدفوعة، مدفوعة جزئيًا، إلخ).
5.  **إدارة المخزون:** هي عملية مستمرة تتأثر بفواتير الشراء (زيادة المخزون) وعمليات الإصلاح والمبيعات المباشرة (نقص المخزون).

### 2. ملخص بنية الواجهة الأمامية (Frontend Architecture)

بناءً على التحليل، أفهم أن الواجهة الأمامية مبنية على حزمة تقنيات حديثة:

- **الإطار والمكتبات:** React 18 (مع Hooks), React Router v6 لإدارة المسارات والتنقل.
- **إدارة الحالة:** Zustand لإدارة الحالة العامة (Global State)، مع وجود `stores` منفصلة للمصادقة (`authStore`) وواجهة المستخدم (`uiStore`).
- **واجهة المستخدم:** مكتبة `shadcn/ui` تستخدم لبناء مكونات واجهة المستخدم، مما يضمن الاتساق وسهولة التطوير.
- **هيكلة المشروع:** يتبع المشروع هيكلة منظمة تفصل بين `pages`, `components`, `stores`, `services`، مما يسهل فهمه وصيانته.
- **التشغيل:** من المرجح أن المشروع يستخدم Vite كأداة بناء وتشغيل (Build Tool) نظرًا لحداثة الحزمة التقنية.

### 3. ملخص العلاقات بين الوحدات (Module Interdependencies)

فهمي للعلاقات بين الوحدات هو كما يلي، وهي نقطة حرجة للاختبارات التكاملية:

- **`Repair` ←→ `Inventory`:** وحدة الإصلاحات **تستهلك** من المخزون (PartsUsed)، مما يستدعي تحديث `StockLevel` وإنشاء `StockMovement`.
- **`Repair` ←→ `Invoices`:** طلب الإصلاح هو **مصدر** لإنشاء فاتورة بيع. أي تغيير في الخدمات أو القطع في الإصلاح يجب أن ينعكس في الفاتورة المرتبطة به.
- **`Inventory` ←→ `Invoices`:** المخزون **يتأثر** بنوعي الفواتير. فواتير الشراء تزيد من `StockLevel`، وفواتير البيع المباشر تنقصه.
- **`Customers/Vendors` ←→ `Invoices`:** الفواتير دائمًا ما تكون مرتبطة إما بعميل (`Customer`) في حالة البيع، أو بمورد (`Vendor`) في حالة الشراء.

### 4. أهم النقاط التي سأبني عليها الاختبارات

سأركز استراتيجية الاختبار على النقاط التالية لضمان أقصى تغطية وتأثير:

1.  **صحة تدفقات العمل الأساسية (Core Flows):** الأولوية القصوى ستكون لاختبارات التكامل (Integration) والاختبارات الشاملة (E2E) التي تحاكي رحلة المستخدم الكاملة (إنشاء إصلاح ← إصدار فاتورة ← دفع).
2.  **سلامة البيانات (Data Integrity):** التأكد من أن العمليات في وحدة ما تنعكس بشكل صحيح في الوحدات الأخرى (e.g., استخدام قطعة غيار ينقص المخزون فعلًا).
3.  **إدارة الحالة والمصادقة:** كتابة اختبارات وحدات دقيقة للـ Zustand stores و `ProtectedRoute` لأنها تتحكم في حالة التطبيق وأمانه بالكامل.
4.  **المكونات المعقدة:** المكونات التي تحتوي على منطق عمل معقد (مثل `CreateInvoicePage`) ستخضع لاختبارات تكامل مكثفة.

### 5. نقاط تحتاج إلى توضيح (Points for Clarification)

لكي أعمل بأقصى فعالية، هناك بعض المعلومات التي أحتاج إليها:

1.  **عقود الـ API (API Contracts):** هل يوجد توثيق رسمي للـ API (مثل ملف Swagger/OpenAPI)؟ هذا سيساعدني على التأكد من صحة الطلبات (Payloads) والاستجابات (Responses) بشكل دقيق جدًا في اختباراتي.
2.  **قواعد العمل التفصيلية (Detailed Business Rules):** أحتاج إلى فهم أعمق للحالات الاستثنائية (Edge Cases). على سبيل المثال:
    - ما هي قواعد التحقق من صحة المدخلات (Validation Rules) لكل فورم؟
    - ماذا يحدث عند محاولة استخدام قطعة غيار غير متوفرة في المخزون؟ هل يتم منع العملية أم يتم إنشاء طلب؟
    - هل يمكن تعديل فاتورة بعد أن تم دفع جزء منها؟
3.  **بيئة الاختبار (Testing Environment):** لقد قمت بإعداد ملفات الاختبار. هل تم إكمال إعداد مشغل الاختبارات (Vitest) في `package.json`؟ وكيف هو الأمر المخصص لتشغيل الاختبارات (e.g., `npm test`)؟

---

لقد قمت بإنشاء هذا الملخص بناءً على المعلومات المتاحة. أنا الآن في انتظار تأكيدك ومراجعتك قبل الانتقال إلى أي خطوات عملية أخرى.

