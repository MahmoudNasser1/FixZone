# 🎨 خطة عمل Full-Stack متكاملة - FixZone ERP

هذا المستند يتتبع خطة العمل والميزات التي سيتم تنفيذها في نظام FixZone ERP.

---

### **المرحلة الأولى: الأساسيات وقاعدة البيانات**
-   [x] تصميم مخطط قاعدة البيانات الكامل (Schema).
-   [x] إعداد المشروع (Backend + Frontend).

### **المرحلة الثانية: المصادقة والهيكل الأساسي**
-   [x] بناء نظام المصادقة (Login API).
-   [x] ربط الواجهة الأمامية بنظام المصادقة.
-   [x] بناء الهيكل العام للتطبيق (Layout: Sidebar, Topbar).

### **المرحلة الثالثة: تحسينات الواجهة الرئيسية (Layout Enhancements)**

خطة تطوير الشريطين العلوي والجانبي لزيادة احترافية الواجهة وتحسين تجربة المستخدم.

1.  **الشريط الجانبي القابل للطي (Collapsible Sidebar):**
    -   **الحالة:** `تم الإنجاز ✔️`

2.  **القائمة المنسدلة للمستخدم (User Dropdown Menu):**
    -   **الحالة:** `تم الإنجاز ✔️`

3.  **زر تبديل المظهر (Theme Toggle):**
    -   **الحالة:** `تم الإنجاز ✔️`

4.  **أيقونة الإشعارات (Notifications Bell):**
    -   **الحالة:** `تم الإنجاز ✔️`

5.  **قوائم فرعية متداخلة (Nested Sub-menus):**
    -   **الحالة:** `تم الإنجاز ✔️`

---

### **المرحلة الرابعة: الميزات الأساسية (Core Features)**

-   [ ] **إدارة طلبات الإصلاح (Repairs Management):**
    -   عرض قائمة بجميع طلبات الإصلاح في جدول بيانات.
    -   إمكانية إضافة طلب إصلاح جديد.
    -   تعديل حالة طلب الإصلاح.
-   [ ] **إدارة العملاء (CRM):**
    -   عرض قائمة بالعملاء.
    -   إضافة وتعديل بيانات العملاء.

## 🎯 مقدمة

هذه الوثيقة هي المرجع الأساسي لتصميم وتنفيذ نظام **FixZone ERP** بشكل متكامل، حيث تربط كل مهمة واجهة أمامية (Frontend) بنقطة النهاية (API Endpoint) المقابلة لها في الواجهة الخلفية (Backend). تم إعدادها لضمان بناء نظام احترافي، آمن، قابل للتطوير، وعالي الجودة.

---

## 1. 🏛️ المبادئ الأساسية للتصميم (Core Design Principles)

- **التقنية المستخدمة:** React.js + Tailwind CSS.
- **الهوية البصرية:** تصميم عصري، نظيف، واحترافي يوحي بالثقة والتقنية.
- **لوحة الألوان:**
  - **أساسي:** درجات الأزرق (للعناصر التفاعلية والروابط).
  - **محايد:** درجات الرمادي (للخلفيات والنصوص).
  - **قاعدة:** أبيض (للمساحات والمحتوى الرئيسي).
- **الخطوط (Typography):** استخدام خط **Cairo** أو **Inter** لضمان وضوح القراءة باللغتين العربية والإنجليزية.
- **الأيقونات (Iconography):** استخدام مكتبة **Lucide Icons** أو **Tabler Icons** لتوحيد شكل الأيقونات.
- **المفاهيم الأساسية:**
  - **استجابة كاملة (Fully Responsive):** يعمل بكفاءة على جميع الشاشات (مكتبية، لوحية، هواتف).
  - **دعم الوضع الليلي (Dark Mode):** ضروري لراحة العين، خاصة للفنيين.
  - **دعم اللغة العربية (RTL):** تصميم الواجهات لتعمل بسلاسة من اليمين إلى اليسار.
  - **مؤثرات حركية دقيقة (Micro-animations):** لإعطاء إحساس بالحيوية عند التفاعل.
  - **صديق للطباعة (Print-Friendly):** تصميم خاص للتقارير والفواتير عند الطباعة.
  - **متجاوب مع جميع الأجهزة (Cross-Device):** يعمل بكفاءة على جميع الأجهزة.

---

## 2. 🧱 المكونات العامة القابلة لإعادة الاستخدام (Global UI Components)

سيتم بناء هذه المكونات أولاً لتكون أساسًا لكل الواجهات (لا تتطلب ربط مباشر بالـ API بنفسها، بل تستخدمه الصفحات التي تحتويها):

- **[ ] الهيكل العام (Main Layout):**
  - **الشريط الجانبي (Sidebar):** قائمة تنقل رئيسية قابلة للطي، تعرض القوائم حسب صلاحيات المستخدم.
  - **الشريط العلوي (Topbar):** يحتوي على اسم المستخدم، الإشعارات، زر تبديل الوضع الليلي، وزر تسجيل الخروج.
- **[ ] الأزرار (Buttons):** تصميم موحد للأزرار الرئيسية، الثانوية، وأزرار الحذف.
- **[ ] النماذج (Forms):** تصميم موحد لحقول الإدخال، القوائم المنسدلة، مربعات الاختيار، وغيرها.
- [x] **الجداول (Tables):** مكون جدول احترافي تم بناؤه مع الميزات المتقدمة التالية:
  - [x] الفرز (Sorting)
  - [x] الترقيم (Pagination)
  - [x] إخفاء/إظهار الأعمدة (Column Visibility)
  - [x] تحديد الصفوف (Row Selection)
  - [x] بحث شامل (Global Search)
  - [x] فلاتر متقدمة (Faceted Filtering)
  - [x] إجراءات مجمعة (Bulk Actions)
  - [x] تصدير البيانات (Data Export to CSV)
- **[ ] البطاقات (Cards):** لعرض الإحصائيات والمعلومات المختصرة كما في لوحة التحكم.
- **[ ] النوافذ المنبثقة (Modals):** لتأكيد الإجراءات أو عرض نماذج الإدخال السريعة.
- **[ ] الإشعارات (Notifications/Toasts):** لتقديم ملاحظات فورية للمستخدم.
- **[ ] الشارات (Badges):** لعرض الحالات المختلفة بألوان مميزة.
- **[ ] مؤشرات التحميل (Loaders/Skeletons):** لتحسين تجربة المستخدم أثناء انتظار البيانات.

---

## 3. 🗺️ تفصيل الواجهات والربط مع الواجهة الخلفية (UI & API Integration)

### أ. المصادقة وإدارة الوصول (Authentication & Access Management)

- **[x] صفحة تسجيل الدخول (Login Page):** (اكتمل بنجاح وأصبحت آمنة)
  - **Frontend:** تم تحديث الواجهة الأمامية للتعامل مع الـ Cookies.
  - **Backend API:** `POST /api/auth/login`
    - **المخرجات:** معلومات المستخدم الأساسية (الاسم، الدور).
    - **التفاصيل:** تم تأمين عملية الدخول بالكامل. التوكن يُرسل الآن كـ `httpOnly cookie` آمن، وتم حل جميع مشاكل CORS.
- **[ ] تسجيل مستخدم جديد (User Registration - للمدراء فقط):**
  - **Frontend:** نموذج خاص في لوحة تحكم المدير لإنشاء مستخدمين جدد (فنيين، موظفي استقبال).
  - **Backend API:** `POST /api/v1/users/register`
    - **المدخلات:** `name`, `email`, `phone`, `password`, `roleId`.
    - **التفاصيل:** يجب أن يكون المستخدم الذي يقوم بالإنشاء مديرًا (Admin). يتم تشفير كلمة المرور باستخدام `bcrypt`.
- **[ ] استعادة كلمة المرور (Password Reset):**
  - **Frontend (الخطوة 1):** صفحة تطلب البريد الإلكتروني للمستخدم.
  - **Backend API (الخطوة 1):** `POST /api/v1/auth/forgot-password` - لإرسال رابط إعادة تعيين إلى بريد المستخدم.
  - **Frontend (الخطوة 2):** صفحة جديدة (يتم الوصول إليها من الرابط) لإدخال كلمة المرور الجديدة.
  - **Backend API (الخطوة 2):** `POST /api/v1/auth/reset-password` - لتحديث كلمة المرور في قاعدة البيانات.

### ب. تحسينات واجهة تسجيل الدخول (Login Page UI Enhancements)
- **[ ] إضافة "تذكرني" (Remember Me):**
  - **Frontend:** إضافة checkbox في صفحة الدخول.
  - **Backend:** تعديل صلاحية الـ cookie لتمتد لفترة أطول إذا تم تحديد الخيار.
- **[ ] إضافة "هل نسيت كلمة المرور؟" (Forgot Password?):
  - **Frontend:** إضافة رابط يوجه المستخدم إلى صفحة استعادة كلمة المرور.
  - **Backend:** (سيتم تنفيذها لاحقًا ضمن قسم استعادة كلمة المرور).

### ج. الهيكل العام للتطبيق (Main Application Layout)
- **[ ] الشريط الجانبي (Sidebar):**
  - **Frontend:** بناء مكون الشريط الجانبي الثابت الذي يحتوي على روابط التنقل الرئيسية.
- **[ ] الشريط العلوي (Topbar):**
  - **Frontend:** بناء مكون الشريط العلوي لعرض اسم المستخدم، الإشعارات، وزر الخروج.

### د. لوحة التحكم (Dashboard)
- **[x] بطاقات الإحصائيات (Stats Cards):**
  - **Frontend:** عرض سريع للأرقام الهامة (الأجهزة المستلمة، تحت الإصلاح، إلخ).
  - **Backend API:** `GET /api/dashboard/stats` - لجلب الإحصائيات الرئيسية.
- **[x] الرسوم البيانية (Charts):**
  - **Frontend:** رسوم بيانية تفاعلية (أعمدة، دائري) لعرض الإحصائيات بشكل مرئي.
  - **Backend API:** `GET /api/dashboard/charts-data` - لجلب بيانات الرسوم البيانية.
- **[ ] قسم التنبيهات (Alerts Section):**
  - **Frontend:** قائمة بالأجهزة المتأخرة والمخزون المنخفض.
  - **Backend API:** `GET /api/dashboard/alerts` - لجلب التنبيهات الهامة.
- **[ ] قائمة مهام الفنيين (Technician Tasks):**
  - **Frontend:** عرض مبسط للمهام الموكلة للفنيين.
  - **Backend API:** `GET /api/dashboard/technician-tasks` - لجلب مهام الفنيين.

### د. إدارة الشركات والعملاء (Company & Customer Management)

- **[ ] الواجهة الرئيسية (List View):
  - **Frontend:** جدول احترافي يعرض كل الشركات مع أعمدة (اسم الشركة، جهة الاتصال، الهاتف، عدد العملاء المرتبطين) وأزرار تحكم (شركة جديدة، بحث).
  - **Backend API:** `GET /api/companies` - لجلب قائمة الشركات مع دعم للبحث والترقيم.

- **[ ] إنشاء شركة جديدة (Create Company):
  - **Frontend:** نموذج (Modal أو صفحة) لإدخال بيانات الشركة الجديدة (الاسم، العنوان، الهاتف، البريد الإلكتروني، جهة الاتصال).
  - **Backend API:** `POST /api/companies` - لإرسال بيانات الشركة الجديدة وحفظها.

- **[ ] صفحة تفاصيل الشركة (Company Details):
  - **Frontend:** عرض شامل لكل بيانات الشركة، بالإضافة إلى قائمة بالعملاء المرتبطين بهذه الشركة.
  - **Backend API:** `GET /api/companies/:id` - لجلب بيانات شركة معينة.
  - **Backend API:** `GET /api/companies/:id/customers` - لجلب قائمة العملاء المرتبطين بالشركة.

- **[ ] تعديل بيانات الشركة (Update Company):
  - **Frontend:** نموذج لتعديل بيانات الشركة.
  - **Backend API:** `PUT /api/companies/:id` - لتحديث بيانات الشركة.

---

### ج. طلبات الصيانة (Repair Tickets)
- **[ ] الواجهة الرئيسية (List View):**
  - **Frontend:** جدول احترافي يعرض كل طلبات الصيانة مع أعمدة (رقم الطلب، العميل، الجهاز، الحالة، الفني، تاريخ الاستلام) وأزرار تحكم (طلب جديد، بحث، تصفية).
  - **Backend API:** `GET /api/repairs` - لجلب قائمة طلبات الصيانة مع دعم للبحث والتصفية والترقيم.
- **[ ] إنشاء طلب جديد (Create Ticket):**
  - **Frontend:** نموذج (Modal أو صفحة) لإدخال بيانات الطلب الجديد.
  - **Backend API:** `POST /api/repairs` - لإرسال بيانات الطلب الجديد وحفظه.
- **[ ] صفحة تفاصيل الطلب (Ticket Details):**
  - **Frontend:** عرض شامل لكل بيانات الطلب، سجل التغييرات (Logs)، وقسم لإضافة الملاحظات.
  - **Backend API:** `GET /api/repairs/:id` - لجلب بيانات طلب معين.
  - **Backend API:** `GET /api/repairs/:id/logs` - لجلب سجل التغييرات.
  - **Backend API:** `POST /api/repairs/:id/notes` - لإضافة ملاحظة جديدة.
- **[ ] تعديل طلب (Update Ticket):**
  - **Frontend:** نموذج لتعديل بيانات الطلب.
  - **Backend API:** `PUT /api/repairs/:id` - لتحديث بيانات الطلب.
- **[ ] تغيير حالة الطلب (Change Status):**
  - **Frontend:** قائمة منسدلة أو أزرار لتغيير حالة الطلب.
  - **Backend API:** `PATCH /api/repairs/:id/status` - لتحديث حالة الطلب فقط.
- **[ ] نماذج الطباعة (Printing):**
  - **Frontend:** أزرار لطباعة تقرير الفحص، التسليم، أو الملصق.
  - **Backend API:** `GET /api/repairs/:id/print/inspection` (أو `/delivery`, `/sticker`) - لجلب بيانات مهيأة للطباعة أو ملف PDF جاهز.

### د. العملاء (CRM)
- **[ ] عرض قائمة العملاء:**
  - **Frontend:** جدول يعرض قائمة العملاء مع خيارات البحث.
  - **Backend API:** `GET /api/customers` - لجلب قائمة العملاء.
- **[ ] صفحة ملف العميل (Customer Profile):**
  - **Frontend:** عرض بيانات العميل، سجل طلباته، الملاحظات، وتقييماته.
  - **Backend API:** `GET /api/customers/:id` - لجلب بيانات عميل معين.
  - **Backend API:** `GET /api/customers/:id/repairs` - لجلب سجل طلبات العميل.

### هـ. حسابات الشركات (Company Accounts) - جديد
- **الهدف:** إدارة العملاء التابعين لشركات، وتوفير حساب رئيسي لكل شركة يمكن من خلاله متابعة كل الطلبات.
- **[ ] الواجهة الرئيسية لإدارة الشركات (Companies List View):**
  - **Frontend:** جدول يعرض قائمة الشركات المسجلة مع معلومات أساسية (اسم الشركة، عدد العملاء) وأزرار تحكم (إضافة شركة، بحث).
  - **Backend API:** `GET /api/v1/companies` - لجلب قائمة الشركات.
- **[ ] إنشاء/تعديل شركة (Create/Update Company):**
  - **Frontend:** نموذج لإدخال بيانات الشركة (اسم الشركة، معلومات الاتصال، ...).
  - **Backend API:** `POST /api/v1/companies` و `PUT /api/v1/companies/:id`.
- **[ ] صفحة تفاصيل الشركة (Company Profile):**
  - **Frontend:** عرض بيانات الشركة، وقائمة بالعملاء التابعين لها، وسجل مجمع لكل طلبات الصيانة الخاصة بهم.
  - **Backend API:** `GET /api/v1/companies/:id` - لجلب بيانات الشركة.
  - **Backend API:** `GET /api/v1/companies/:id/customers` - لجلب قائمة العملاء التابعين للشركة.
  - **Backend API:** `GET /api/v1/companies/:id/repairs` - لجلب كل طلبات الصيانة لعملاء الشركة.
- **[ ] ربط عميل بشركة (Link Customer to Company):**
  - **Frontend:** عند إنشاء أو تعديل عميل، يتم إضافة حقل (Autocomplete) للبحث عن الشركة وربطه بها.
  - **Backend API:** يتم تحديث `POST /api/customers` و `PUT /api/customers/:id` ليقبلا `companyId`.

### هـ. حسابات الشركات (Company Accounts) - جديد
- **الهدف:** إدارة العملاء التابعين لشركات، وتوفير حساب رئيسي لكل شركة يمكن من خلاله متابعة كل الطلبات.
- **[ ] الواجهة الرئيسية لإدارة الشركات (Companies List View):**
  - **Frontend:** جدول يعرض قائمة الشركات المسجلة مع معلومات أساسية (اسم الشركة، عدد العملاء) وأزرار تحكم (إضافة شركة، بحث).
  - **Backend API:** `GET /api/v1/companies` - لجلب قائمة الشركات.
- **[ ] إنشاء/تعديل شركة (Create/Update Company):**
  - **Frontend:** نموذج لإدخال بيانات الشركة (اسم الشركة، معلومات الاتصال، ...).
  - **Backend API:** `POST /api/v1/companies` و `PUT /api/v1/companies/:id`.
- **[ ] صفحة تفاصيل الشركة (Company Profile):**
  - **Frontend:** عرض بيانات الشركة، وقائمة بالعملاء التابعين لها، وسجل مجمع لكل طلبات الصيانة الخاصة بهم.
  - **Backend API:** `GET /api/v1/companies/:id` - لجلب بيانات الشركة.
  - **Backend API:** `GET /api/v1/companies/:id/customers` - لجلب قائمة العملاء التابعين للشركة.
  - **Backend API:** `GET /api/v1/companies/:id/repairs` - لجلب كل طلبات الصيانة لعملاء الشركة.
- **[ ] ربط عميل بشركة (Link Customer to Company):**
  - **Frontend:** عند إنشاء أو تعديل عميل، يتم إضافة حقل (Autocomplete) للبحث عن الشركة وربطه بها.
  - **Backend API:** يتم تحديث `POST /api/customers` و `PUT /api/customers/:id` ليقبلا `companyId`.

---

## 4. ⭐ ملاحظات هامة لضمان الجودة (Full-Stack Best Practices)

1.  **إصدار الـ API (API Versioning):**
    - **لماذا؟** لتسهيل التحديثات المستقبلية دون التأثير على الإصدارات الحالية من الواجهة الأمامية.
    - **كيف؟** استخدام `v1` في كل مسارات الـ API (مثال: `/api/v1/...`).

2.  **هيكل موحد لاستجابة الـ API (Consistent API Response):**
    - **لماذا؟** لتوحيد طريقة معالجة النجاح والفشل في الواجهة الأمامية.
    - **كيف؟** يجب أن تعود كل الاستجابات بنفس الشكل، مثال:
      ```json
      // للنجاح
      { "success": true, "data": { ... } }
      // للفشل
      { "success": false, "error": { "message": "..." } }
      ```

3.  **الأمان والصلاحيات (Security & Authorization):**
    - **JWT Middleware:** يجب حماية جميع نقاط الـ API (ما عدا `login`) بـ Middleware يتحقق من صلاحية الـ JWT Token.
    - **التحكم في الوصول المستند إلى الدور (RBAC):** يجب أن يتحقق الـ Backend من صلاحية المستخدم (مدير، فني، ...) قبل السماح له بتنفيذ أي إجراء.

4.  **التحقق من صحة البيانات (Data Validation):**
    - **Backend:** التحقق من كل البيانات القادمة من المستخدم باستخدام مكتبة مثل `zod` أو `joi` قبل معالجتها. هذا هو خط الدفاع الأهم.
    - **Frontend:** التحقق المبدئي في النماذج لتحسين تجربة المستخدم وإعطاء ملاحظات فورية.

5.  **إدارة الحالة في الواجهة الأمامية (Frontend State Management):**
    - **لماذا؟** لتجنب تعقيد تمرير البيانات بين المكونات (Prop Drilling).
    - **كيف؟** استخدام مكتبة مثل **Zustand** (خفيفة وسهلة) أو **Redux Toolkit** (أكثر قوة) لإدارة الحالة العامة للتطبيق (مثل بيانات المستخدم المسجل دخوله).

6.  **تجربة المستخدم (UX - Loading, Error, Empty States):**
    - **لماذا؟** لتقديم تجربة مستخدم سلسة وواضحة.
    - **كيف؟** كل مكون يقوم بجلب بيانات من الـ API يجب أن يعالج ويعرض واجهة مناسبة لثلاث حالات:
      1.  **أثناء التحميل (Loading):** عرض مؤشر تحميل أو هيكل عظمي (Skeleton).
      2.  **عند حدوث خطأ (Error):** عرض رسالة خطأ واضحة مع زر لإعادة المحاولة.
      3.  **عندما تكون البيانات فارغة (Empty):** عرض رسالة توضح عدم وجود بيانات مع دعوة لاتخاذ إجراء (مثل: "لا يوجد طلبات صيانة بعد، قم بإنشاء طلب جديد").

7.  **إدارة الإعدادات والبيئة (Environment Variables):**
    - **لماذا؟** لفصل الإعدادات الحساسة (مثل روابط الـ API والمفاتيح السرية) عن الكود.
    - **كيف؟** استخدام ملفات `.env` في كل من الـ Frontend (`REACT_APP_...`) والـ Backend.

---

## 5. 🚀 خطة التنفيذ (Implementation Plan)

**المرحلة الأولى: التأسيس (Foundation)**
- **[x]** إعداد مشروع React + Tailwind CSS.
- **[x]** بناء مكون الشريط العلوي (Navbar/Topbar).
- **[x]** دمج React Router للتنقل.
- **[x]** بناء الهيكل العام (Layout) مع الشريط الجانبي (Sidebar).
- **[x]** بناء نظام تبديل الوضع الليلي (Dark Mode).
- **[x]** إضافة دعم RTL (من اليمين إلى اليسار).
- **[x]** بناء المكونات العامة القابلة لإعادة الاستخدام (Button, Card, Table, Modal, Badge).

**المرحالة الثانية: الوحدات الأساسية (Core Modules - Frontend & Backend)**
- **[ ] **المصادقة (Authentication):**
    - **Frontend:** **[x]** بناء صفحة تسجيل الدخول.
    - **Backend:** **[ ]** بناء `POST /api/auth/login`.
- **[ ] **لوحة التحكم (Dashboard):**
    - **Frontend:** **[x]** بناء واجهة لوحة التحكم (بطاقات ورسوم بيانية).
    - **Backend:** **[x]** بناء `GET /api/dashboard/stats` و `GET /api/dashboard/charts-data`.
    - **Frontend:** **[ ]** إضافة قسم التنبيهات والمهام.
    - **Backend:** **[ ]** بناء `GET /api/dashboard/alerts` و `GET /api/dashboard/technician-tasks`.
- **[ ] **طلبات الصيانة (Repair Tickets):**
    - **Frontend:** **[ ]** بناء واجهة عرض القائمة (الجدول، البحث، التصفية).
    - **Backend:** **[ ]** بناء `GET /api/repairs`.
    - **Frontend:** **[ ]** بناء نموذج إنشاء/تعديل الطلب.
    - **Backend:** **[ ]** بناء `POST /api/repairs` و `PUT /api/repairs/:id`.
    - **Frontend:** **[ ]** بناء صفحة تفاصيل الطلب.
    - **Backend:** **[ ]** بناء `GET /api/repairs/:id` و `GET /api/repairs/:id/logs`.

**المرحلة الثالثة: الوحدات الثانوية والتحسينات (Secondary Modules & Polish)**
- **[ ] **العملاء (CRM):**
    - **Frontend:** **[ ]** بناء واجهة قائمة العملاء وملف العميل.
    - **Backend:** **[ ]** بناء `GET /api/customers` و `GET /api/customers/:id`.
- **[ ] **حسابات الشركات (Company Accounts):**
    - **Backend:** **[ ]** بناء `CRUD APIs` للشركات (`/api/v1/companies`).
    - **Frontend:** **[ ]** بناء واجهة إدارة الشركات (قائمة، إنشاء، تفاصيل).
    - **Backend:** **[ ]** تعديل `APIs` العملاء لتدعم الربط مع الشركات.
    - **Frontend:** **[ ]** تعديل نماذج العملاء لإضافة حقل الشركة.
- **[ ] **حسابات الشركات (Company Accounts):**
    - **Backend:** **[ ]** بناء `CRUD APIs` للشركات (`/api/v1/companies`).
    - **Frontend:** **[ ]** بناء واجهة إدارة الشركات (قائمة، إنشاء، تفاصيل).
    - **Backend:** **[ ]** تعديل `APIs` العملاء لتدعم الربط مع الشركات.
    - **Frontend:** **[ ]** تعديل نماذج العملاء لإضافة حقل الشركة.
- **[ ]** بناء واجهة المخزون (Frontend + Backend).
- **[ ]** بناء واجهة الفنيين (Frontend + Backend).
- **[ ]** بناء واجهة الإعدادات (Frontend + Backend).
- **[ ]** تطبيق تصميمات الطباعة (Frontend + Backend).
- **[ ]** إضافة المؤثرات الحركية الدقيقة (Micro-animations).
