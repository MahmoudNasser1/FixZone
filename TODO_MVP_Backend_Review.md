## 📋 خطة عمل MVP لنظام Fix Zone ERP

**الهدف العام:** تطوير ومراجعة نسخة الـ MVP من الـ Backend لـ Fix Zone ERP لضمان الاستقرار، الوظائف الأساسية، وقابلية التوسع.

### المرحلة 1: استقرار وتكامل الـ API Routes للوحدات الأساسية في MVP

**الهدف:** التأكد من أن جميع وحدات الـ MVP الأساسية لديها API Routes كاملة، مستقرة، ومختبرة.

*   **1.1 مراجعة وتأكيد الـ Routes الحالية:**
    *   `RepairRequest` (والمتعلقات: `RepairRequestService`, `PartsUsed`, `StatusUpdateLogs`)
    *   `Customer`
    *   `Technician`
    *   `InventoryItem` (والمتعلقات: `StockLevel`, `StockMovement`)
    *   `Vendor`
    *   `Service`
    *   `InspectionType`, `InspectionReport`, `InspectionComponent`
    *   `Notification` (الأساسية فقط، حسب MVP)
    *   `SystemSetting` (الأساسية فقط)
    *   `AuditLog`
    *   **المهام:**
        *   لكل وحدة من الوحدات المذكورة أعلاه، تأكد من وجود عمليات CRUD كاملة (GET All, GET by ID, POST, PUT, DELETE).
        *   تحقق من أن عمليات الحذف (DELETE) تستخدم "Soft Delete" حيثما ينطبق (وجود عمود `deletedAt`) و "Hard Delete" حيثما لا ينطبق.
        *   تأكد من أن جميع الـ Routes تحتوي على `try/catch` لمعالجة الأخطاء بشكل صحيح وإرجاع رسائل خطأ مناسبة مع أكواد HTTP Status الصحيحة.
        *   تحقق من وجود `validation` للمدخلات المطلوبة في طلبات `POST` و `PUT` لكل الـ Entities.

*   **1.2 معالجة الأخطاء المتبقية في `server.js`:**
    *   **المهام:**
        *   تأكد من أن جميع الـ `require` statements لجميع الـ Routers موجودة في بداية الملف وقبل استخدام أي Router.
        *   تأكد من أن جميع الـ `app.use` statements لجميع الـ Routers موضوعة بشكل صحيح (بعد الـ `require` الخاص بها) لتجنب أي `ReferenceError` مستقبلية.
        *   قم بإزالة أي `app.use` statements مكررة أو غير مستخدمة.

### المرحلة 2: اختبار الوظائف الأساسية (Manual API Testing)

**الهدف:** التأكد يدويًا من أن جميع عمليات CRUD للوحدات الأساسية في MVP تعمل كما هو متوقع.

*   **2.1 تشغيل الخادم:**
    *   **المهام:**
        *   قم بتشغيل `node server.js` وتأكد من عدم وجود أي أخطاء في الـ console.

*   **2.2 اختبار الـ API باستخدام أداة (مثل Postman أو Insomnia):**
    *   **المهام:**
        *   **GET Requests:** اختبر جلب جميع السجلات وجلب سجل واحد لكل وحدة من وحدات MVP.
        *   **POST Requests:** اختبر إنشاء سجلات جديدة لكل وحدة، وتأكد من عمل الـ validation بشكل صحيح (جرب إرسال بيانات ناقصة أو غير صحيحة).
        *   **PUT Requests:** اختبر تحديث السجلات الموجودة لكل وحدة.
        *   **DELETE Requests:** اختبر حذف السجلات (سواء كان Soft أو Hard Delete) وتأكد من أن التغييرات تنعكس في قاعدة البيانات.

*   **2.3 التحقق من قاعدة البيانات:**
    *   **المهام:**
        *   بعد كل عملية POST/PUT/DELETE، تحقق مباشرة من قاعدة البيانات (باستخدام phpMyAdmin أو MySQL Workbench) للتأكد من أن البيانات تم إدخالها/تحديثها/حذفها بشكل صحيح.

### المرحلة 3: مراجعة الـ MVP الأولية والتحضير لتكامل الـ Frontend

**الهدف:** تجهيز الـ Backend ليكون جاهزًا للربط مع الـ Frontend.

*   **3.1 توثيق الـ API (أساسي):**
    *   **المهام:**
        *   قم بإنشاء ملف Markdown بسيط (مثال: `API_ENDPOINTS_MVP.md`) يلخص الـ Endpoints الأساسية، الـ Methods (GET, POST, PUT, DELETE)، وأمثلة لـ Request/Response لكل وحدة من وحدات MVP. هذا سيساعد مطور الـ Frontend بشكل كبير.

*   **3.2 إعداد CORS (إذا لم يكن موجودًا):**
    *   **المهام:**
        *   تأكد من أن CORS (Cross-Origin Resource Sharing) مفعّل بشكل صحيح في `server.js` للسماح لتطبيقات الـ Frontend بالاتصال بالـ Backend.

*   **3.3 متغيرات البيئة (Environment Variables):**
    *   **المهام:**
        *   تأكد من أن جميع المعلومات الحساسة (مثل بيانات اتصال قاعدة البيانات، الـ PORT) يتم تحميلها من متغيرات البيئة بدلاً من كتابتها مباشرة في الكود.

---

**ملاحظات هامة:**

*   **التركيز على MVP:** هذه الخطة تركز فقط على الميزات الأساسية المحددة في `PROTOTYPE_FEATURES.md`. الميزات المتقدمة مثل الـ Authentication/Authorization، الإشعارات المتقدمة، التكاملات الخارجية، والوحدات المالية الكاملة سيتم تناولها في مراحل لاحقة.
*   **التواصل:** إذا واجهت أي مشكلة أو احتجت إلى توضيح بشأن أي جزء من هذه المهام، فلا تتردد في السؤال.

---
