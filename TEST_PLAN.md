# 📋 خطة اختبار الواجهة الأمامية (Frontend) - FixZone ERP

**التاريخ:** 2025-11-14  
**الغرض:** تحديد استراتيجية شاملة لاختبار الواجهة الأمامية لتطبيق FixZone ERP لضمان الجودة، الموثوقية، وسهولة الصيانة.

---

## 1. 🎯 الأهداف الرئيسية للاختبار

- **ضمان صحة الوظائف (Correctness):** التأكد من أن جميع الميزات تعمل كما هو متوقع في "تقرير التحليل المعمق".
- **الموثوقية (Reliability):** ضمان استقرار التطبيق وعدم حدوث أخطاء غير متوقعة أثناء تفاعل المستخدم.
- **تحسين تجربة المستخدم (UX):** التأكد من أن الواجهات سريعة الاستجابة وسهلة الاستخدام.
- **منع الأخطاء المستقبلية (Regression Prevention):** إنشاء شبكة أمان من الاختبارات الآلية التي تكتشف الأخطاء الجديدة عند إجراء تعديلات على الكود.
- **توثيق سلوك المكونات (Behavior Documentation):** الاختبارات ستكون بمثابة توثيق حي لكيفية عمل كل مكون.

---

## 2. 🧪 استراتيجية الاختبار: هرم الاختبار

سنعتمد استراتيجية "هرم الاختبار" (Testing Pyramid) التي تركز على وجود قاعدة عريضة من اختبارات الوحدات السريعة، تليها طبقة أقل من اختبارات التكامل، ثم قمة صغيرة من الاختبارات الشاملة البطيئة.

![Testing Pyramid](https://martinfowler.com/bliki/images/testPyramid/test-pyramid.png)

### 2.1. اختبارات الوحدات (Unit Testing)

- **الهدف:** اختبار أصغر أجزاء التطبيق (المكونات، الدوال، الـ Hooks) بشكل منعزل.
- **الأدوات:** **Vitest** (لتشغيل الاختبارات) + **React Testing Library** (لمحاكاة تفاعل المستخدم مع المكونات).
- **المتطلبات:**
  - عمل Mocking لأي اعتماديات خارجية (API calls, Zustand stores, props).
  - يجب أن يكون كل اختبار مستقلاً تمامًا عن الآخر.
- **ماذا نختبر؟**
  - **المكونات:** هل يتم عرضها بشكل صحيح؟ هل تستجيب لتفاعل المستخدم (مثل النقر)؟ هل تعرض الحالات المختلفة (loading, error, data)؟
  - **الدوال المساعدة (Utils):** هل تعيد المخرجات الصحيحة لمدخلات مختلفة؟
  - **Zustand Stores:** هل تقوم الـ `actions` بتحديث الـ `state` بشكل صحيح؟
- **التغطية المستهدفة (Target Coverage):** **80%** على الأقل للمكونات والمنطق البرمجي الحرج (stores, utils).

### 2.2. اختبارات التكامل (Integration Testing)

- **الهدف:** اختبار كيفية عمل عدة مكونات معًا كوحدة واحدة.
- **الأدوات:** **Vitest** + **React Testing Library**.
- **المتطلبات:**
  - عمل Mocking للـ API calls على مستوى الشبكة (باستخدام مكتبة مثل `msw` - Mock Service Worker) لمحاكاة استجابات السيرفر الحقيقية.
  - لا يتم عمل Mocking للـ Zustand stores أو React Router، بل يتم اختبار تفاعلها الحقيقي.
- **ماذا نختبر؟**
  - **تدفقات النماذج (Forms):** ملء فورم كامل -> الضغط على إرسال -> التأكد من إرسال طلب API الصحيح وتحديث الواجهة.
  - **تدفقات الصفحات (Page Flows):** التنقل بين صفحة قائمة وصفحة تفاصيل -> التأكد من أن البيانات تنتقل وتُعرض بشكل صحيح.
  - **تفاعل الـ Store مع المكونات:** إجراء تغيير في مكون يؤدي إلى تحديث الـ store -> التأكد من أن مكونًا آخر يستمع لنفس الـ store قد تم تحديثه.
- **التغطية المستهدفة:** التركيز على تدفقات العمل الرئيسية والمهمة (إنشاء فاتورة، إضافة طلب إصلاح، تسجيل الدخول).

### 2.3. الاختبارات الشاملة (End-to-End Testing)

- **الهدف:** اختبار النظام بأكمله من وجهة نظر المستخدم النهائي، من خلال محاكاة التفاعل الحقيقي في المتصفح.
- **الأدوات المقترحة:** **Playwright** أو **Cypress**.
- **المتطلبات:**
  - بيئة اختبار منفصلة (Staging Environment) مع قاعدة بيانات خاصة بالاختبار.
  - يجب أن تعمل الاختبارات ضد الـ Backend الحقيقي.
- **ماذا نختبر؟ (أهم 3 سيناريوهات)**
  1.  **سيناريو الفوترة الكامل:** تسجيل الدخول -> إنشاء عميل -> إنشاء طلب إصلاح -> إضافة قطع غيار وخدمات -> إنشاء فاتورة من طلب الإصلاح -> تسجيل دفعة -> تسجيل الخروج.
  2.  **سيناريو المخزون الكامل:** تسجيل الدخول -> إنشاء فاتورة شراء لمورد -> استلام البضاعة (زيادة المخزون) -> استخدام قطعة غيار في إصلاح (نقص المخزون).
  3.  **سيناريو إدارة المستخدمين:** تسجيل الدخول كـ Admin -> إنشاء مستخدم جديد بصلاحيات محددة -> تسجيل الخروج -> تسجيل الدخول بالمستخدم الجديد والتأكد من أن صلاحياته مطبقة.
- **التغطية المستهدفة:** التركيز على "المسارات السعيدة" (Happy Paths) للسيناريوهات الأكثر أهمية لضمان عملها دائمًا.

---

## 3. 🚀 أولويات التنفيذ

لتحقيق أقصى فائدة في أسرع وقت، سيتم تنفيذ الاختبارات بالترتيب التالي:

1.  **الأولوية القصوى (الأسبوع الأول):**
    - **Unit Tests for Zustand Stores:** (`useAuthStore`, `uiStore`) لأنها أساس حالة التطبيق.
    - **Unit Tests for Critical Utils & Hooks:** أي دوال معقدة لإدارة التنسيق أو الحسابات.
    - **Unit Tests for Reusable UI Components:** (`Button`, `Input`, `Card`, `DataTable`) لضمان اتساق واجهة المستخدم.

2.  **الأولوية العالية (الأسبوع الثاني):**
    - **Integration Tests for Authentication:** (صفحة تسجيل الدخول، `ProtectedRoute`).
    - **Unit Tests for Core Pages:** ( `DashboardPage`, `InvoicesPage`, `RepairsPage`) مع عمل Mock للبيانات.

3.  **الأولوية المتوسطة (مستمر):**
    - **Integration Tests for Core Flows:** (إنشاء فاتورة، إنشاء طلب إصلاح).
    - **Unit Tests لباقي المكونات والصفحات.**

4.  **الأولوية المنخفضة (بعد استقرار التطبيق):**
    - **E2E Tests** للسيناريوهات الحرجة.

---

## 4. 🛠️ الإعداد والبيئة

1.  **تثبيت الاعتماديات:**
    ```bash
    npm install --save-dev vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event msw
    ```
2.  **إعداد Vitest:** إنشاء ملف `vitest.config.js` في جذر المشروع (`frontend/react-app`).
3.  **إعداد MSW:** إعداد Mock Service Worker لعمل Mocking للـ API calls في اختبارات التكامل.
4.  **تحديد مكان الاختبارات:** سيتم وضع ملفات الاختبار بجوار المكونات التي تختبرها (e.g., `Button.jsx` and `Button.test.jsx`) لتسهيل العثور عليها.

بهذه الخطة، سنتمكن من بناء نظام اختبار قوي ومتكامل يضمن جودة المنتج على المدى الطويل.
