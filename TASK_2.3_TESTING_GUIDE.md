# 🧪 دليل اختبار المهمة 2.3: إدارة المخزون الكاملة
## Task 2.3 Testing Guide: Comprehensive Inventory Management

**التاريخ:** 2025-10-27  
**الحالة:** ✅ **جاهز للاختبار**

---

## 🎯 خطوات الاختبار

### 1. تسجيل الدخول
1. افتح المتصفح: `http://localhost:3000`
2. سجل الدخول باستخدام:
   - **Email:** `admin@fixzone.com`
   - **Password:** `admin123`

---

## ✅ الميزات الجديدة للاختبار

### 1. ✅ إدارة المخزون (InventoryPageEnhanced)

#### اختبار إضافة/تعديل الكمية:
1. اذهب إلى: **المخزون** (`/inventory`)
2. اختر صنف من الجدول
3. اضغط على زر **إدارة المخزون** (أيقونة Warehouse 🏭)
4. في Modal:
   - اختر **المخزن**
   - أدخل **الكمية** (مثلاً: 50)
   - أدخل **الحد الأدنى** (اختياري، مثلاً: 10)
   - اضغط **حفظ**

**النتيجة المتوقعة:**
- ✅ يجب أن يظهر إشعار نجاح
- ✅ يجب أن تتحدث الكمية في الجدول
- ✅ يجب أن يتم تسجيل StockMovement تلقائياً

#### اختبار عرض المخازن:
1. في جدول المخزون، لاحظ عمود **المخزون**
2. يجب أن تظهر:
   - الكمية الإجمالية
   - عدد المخازن لكل صنف (مثلاً: "2 مخازن")

---

### 2. ✅ حركات المخزون (StockMovementPage)

#### اختبار عرض الحركات:
1. اذهب إلى: **حركة المخزون** (`/inventory/stock-movements`)
2. يجب أن تظهر قائمة بالحركات

**الفلاتر المتاحة:**
- البحث (اسم الصنف، المستخدم، SKU)
- نوع الحركة (IN/OUT/TRANSFER)
- تاريخ البداية
- تاريخ النهاية
- صنف محدد (ID)

#### اختبار الفلترة:
1. اختر **نوع الحركة:** IN
2. يجب أن تظهر فقط حركات الدخول
3. جرب فلترة أخرى (OUT, TRANSFER)

**النتيجة المتوقعة:**
- ✅ يجب أن تعمل جميع الفلاتر بشكل صحيح
- ✅ يجب أن يظهر pagination إذا كانت الحركات كثيرة

---

### 3. ✅ نقل المخزون (StockTransferPage)

#### اختبار إنشاء نقل جديد:
1. اذهب إلى: **نقل بين المخازن** (`/stock-transfer`)
2. اضغط **إنشاء نقل جديد**
3. املأ البيانات:
   - **من مخزن:** اختر مخزن
   - **إلى مخزن:** اختر مخزن آخر
   - **تاريخ النقل:** اليوم
   - **السبب:** (اختياري)
   - **ملاحظات:** (اختياري)
4. أضف عناصر:
   - اختر **الصنف**
   - أدخل **الكمية**
   - أدخل **السعر** (اختياري)
   - اضغط **إضافة**
5. اضغط **إنشاء**

**النتيجة المتوقعة:**
- ✅ يجب أن يظهر إشعار نجاح
- ✅ يجب أن يظهر النقل الجديد في الجدول بحالة "قيد الانتظار"

#### اختبار استلام النقل:
1. في جدول النقلات:
   - النقل بحالة "قيد الانتظار" → اضغط ✅ (موافقة)
   - النقل بحالة "معتمد" → اضغط 🚚 (شحن)
   - النقل بحالة "تم الشحن" → اضغط ✅ (استلام)
   - النقل بحالة "تم الاستلام" → اضغط ✅ (إكمال)

**النتيجة المتوقعة:**
- ✅ يجب أن تتحدث حالة النقل
- ✅ عند الاستلام، يجب أن يتحدث المخزون تلقائياً
- ✅ يجب أن يتم تسجيل StockMovement تلقائياً

---

## 🔍 اختبار Backend APIs مباشرة (باستخدام curl)

### 1. تسجيل الدخول والحصول على Token:
```bash
TOKEN=$(curl -s -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"identifier":"admin@fixzone.com","password":"admin123"}' \
  | jq -r '.token // .data.token // empty')

echo "Token: $TOKEN"
```

### 2. اختبار StockLevel API:
```bash
# جلب جميع مستويات المخزون
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:3001/api/stock-levels | jq '.'

# إنشاء/تحديث مستوى مخزون
curl -X POST http://localhost:3001/api/stock-levels \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "inventoryItemId": 1,
    "warehouseId": 1,
    "quantity": 50,
    "minLevel": 10
  }' | jq '.'
```

### 3. اختبار StockMovement API:
```bash
# جلب جميع الحركات
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3001/api/stock-movements?type=IN&page=1&limit=10" | jq '.'

# إنشاء حركة دخول
curl -X POST http://localhost:3001/api/stock-movements \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "IN",
    "inventoryItemId": 1,
    "toWarehouseId": 1,
    "quantity": 10
  }' | jq '.'
```

### 4. اختبار StockTransfer API:
```bash
# جلب جميع النقلات
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:3001/api/stock-transfer | jq '.'

# إنشاء نقل جديد
curl -X POST http://localhost:3001/api/stock-transfer \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "fromWarehouseId": 1,
    "toWarehouseId": 2,
    "transferDate": "2025-10-27",
    "reason": "نقل للفرع",
    "items": [
      {
        "inventoryItemId": 1,
        "quantity": 5,
        "unitPrice": 100
      }
    ]
  }' | jq '.'
```

---

## 📊 قائمة التحقق (Checklist)

### ✅ Backend APIs
- [ ] `POST /api/stock-levels` - إنشاء/تحديث StockLevel
- [ ] `PUT /api/stock-levels/:id` - تعديل الكمية
- [ ] `GET /api/stock-movements` - جلب الحركات (مع filters)
- [ ] `POST /api/stock-movements` - إنشاء حركة IN/OUT
- [ ] `POST /api/stock-transfer` - إنشاء نقل جديد
- [ ] `PUT /api/stock-transfer/:id/receive` - استلام النقل

### ✅ Frontend
- [ ] InventoryPageEnhanced - عرض المخازن
- [ ] InventoryPageEnhanced - Modal إدارة المخزون
- [ ] InventoryPageEnhanced - إضافة/تعديل الكمية
- [ ] StockMovementPage - عرض الحركات
- [ ] StockMovementPage - الفلترة والبحث
- [ ] StockTransferPage - إنشاء نقل جديد
- [ ] StockTransferPage - استلام النقل

### ✅ Integration
- [ ] عند إضافة/تعديل الكمية → تحديث StockLevel + تسجيل StockMovement
- [ ] عند استلام النقل → تحديث StockLevel في كلا المخازن + تسجيل StockMovement
- [ ] عند حذف/تعديل حركة → معكوس الحركة + تحديث StockLevel

---

## 🐛 المشاكل المحتملة وحلولها

### مشكلة: لا يمكن تسجيل الدخول
**الحل:** 
- تحقق من بيانات المستخدم في قاعدة البيانات
- جرب استخدام curl للحصول على token
- تحقق من logs السيرفر: `tail -f /tmp/backend.log`

### مشكلة: StockMovement لا يُسجل تلقائياً
**الحل:**
- تحقق من Backend logs
- تحقق من أن `authMiddleware` موجود
- تحقق من أن `userId` موجود في request

### مشكلة: الكمية لا تتحدث
**الحل:**
- تحقق من أن `quantity` في الجدول (وليس `availableQuantity`)
- تحقق من StockLevel في قاعدة البيانات
- تحقق من StockMovement للتأكد من تسجيل الحركة

---

## 📝 ملاحظات

1. **جميع APIs محمية بـ `authMiddleware`** - يجب تسجيل الدخول أولاً
2. **تسجيل StockMovement تلقائياً** عند تغيير الكميات
3. **تحديث StockLevel تلقائياً** عند الحركات والنقل
4. **معكوس الحركات** عند التعديل أو الحذف

---

**آخر تحديث:** 2025-10-27  
**المطور:** Auto (Cursor AI)  
**الحالة:** ✅ **جاهز للاختبار**

