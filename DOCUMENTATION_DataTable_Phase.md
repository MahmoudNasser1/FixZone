# 📝 توثيق مرحلة بناء مكونات واجهة المستخدم الأساسية

## 🎯 نظرة عامة

هذه الوثيقة توثق مرحلة بناء المكونات الأساسية لواجهة المستخدم (UI) في مشروع **FixZone ERP**، مع التركيز بشكل خاص على بناء مكون `DataTable` القابل لإعادة الاستخدام وميزاته المتقدمة. تم بناء هذه المكونات باستخدام React و Tailwind CSS، مع اتباع أفضل الممارسات لإنشاء كود نظيف وموثوق.

--- 

## 🧱 المكونات التي تم إنشاؤها

فيما يلي قائمة بالمكونات الرئيسية والثانوية التي تم إنشاؤها خلال هذه المرحلة، مع توضيح مسار كل ملف والغرض منه.

### 1. مكونات `DataTable` الأساسية

| المكون                   | المسار                                                 | الوصف                                                                                                                               |
| ------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| `DataTable.js`           | `frontend/react-app/src/components/ui/DataTable.js`    | المكون الرئيسي للجدول. يدير الحالة العامة للجدول (الفرز، الفلاتر، التحديد، الترقيم) ويعرض الصفوف والأعمدة.                      |
| `DataTableToolbar.js`    | `frontend/react-app/src/components/ui/DataTableToolbar.js` | شريط الأدوات العلوي للجدول. يحتوي على البحث، الفلاتر، الإجراءات المجمعة، زر التصدير، وزر التحكم في إظهار الأعمدة.                 |
| `DataTablePagination.js` | `frontend/react-app/src/components/ui/DataTablePagination.js` | مكون التحكم في ترقيم الصفحات (الانتقال للصفحة التالية/السابقة، عرض عدد الصفوف).                                                    |
| `DataTableColumnHeader.js`| `frontend/react-app/src/components/ui/DataTableColumnHeader.js` | مكون رأس العمود الذي يعرض اسم العمود ويوفر وظيفة الفرز عند النقر عليه.                                                              |
| `columns.js` (مثال)       | `frontend/react-app/src/pages/payments/columns.js`     | ملف تعريف أعمدة الجدول لصفحة المدفوعات. يحدد كيفية عرض البيانات، الفرز، والفلترة لكل عمود.                                      |

### 2. مكونات الميزات المتقدمة

| المكون                       | المسار                                                         | الوصف                                                                                                                                   |
| ---------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `DataTableFacetedFilter.js`  | `frontend/react-app/src/components/ui/DataTableFacetedFilter.js` | مكون الفلتر المتقدم الذي يظهر كقائمة منبثقة (Popover) مع خيارات متعددة للاختيار، ويستخدم لتصفية الأعمدة ذات القيم المحددة (مثل "الحالة"). |
| `Command.js`                 | `frontend/react-app/src/components/ui/Command.js`              | مكون واجهة مستخدم لبناء قوائم الأوامر التفاعلية التي يمكن البحث فيها. يُستخدم داخل `DataTableFacetedFilter`.                     |

### 3. مكونات واجهة المستخدم المساعدة (UI Primitives)

| المكون           | المسار                                             | الوصف                                                                                              |
| --------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `Badge.js`      | `frontend/react-app/src/components/ui/Badge.js`    | مكون شارة (Badge) لعرض الحالات المختلفة بألوان مميزة.                                              |
| `Button.js`     | `frontend/react-app/src/components/ui/Button.js`   | مكون زر موحد بأنماط مختلفة (أساسي، ثانوي، إلخ).                                                     |
| `Card.js`       | `frontend/react-app/src/components/ui/Card.js`     | مكون بطاقة (Card) يستخدم لتجميع المحتوى ذي الصلة في حاوية بصرية.                                   |
| `Checkbox.js`   | `frontend/react-app/src/components/ui/Checkbox.js` | مكون مربع اختيار (Checkbox)، يُستخدم في تحديد الصفوف.                                              |
| `Dialog.js`     | `frontend/react-app/src/components/ui/Dialog.js`   | مكون نافذة حوار (Dialog/Modal) لعرض محتوى فوق الصفحة، مثل النماذج.                                 |
| `DropdownMenu.js`| `frontend/react-app/src/components/ui/DropdownMenu.js`| مكون قائمة منسدلة، يُستخدم في قائمة "View" وقائمة "Bulk Actions".                                     |
| `Input.js`      | `frontend/react-app/src/components/ui/Input.js`    | مكون حقل إدخال موحد.                                                                              |
| `Label.js`      | `frontend/react-app/src/components/ui/Label.js`    | مكون ملصق (Label) لعناوين حقول النماذج لتحسين الوصولية.                                            |
| `Popover.js`    | `frontend/react-app/src/components/ui/Popover.js`  | مكون نافذة منبثقة (Popover) عام، يُستخدم كأساس لـ `DataTableFacetedFilter`.                           |
| `Separator.js`  | `frontend/react-app/src/components/ui/Separator.js`| مكون خط فاصل بصري، يُستخدم للفصل بين العناصر في القوائم.                                           |
| `Skeleton.js`   | `frontend/react-app/src/components/ui/Skeleton.js` | مكون هيكل عظمي (Skeleton) يستخدم كعنصر نائب أثناء تحميل البيانات لتحسين تجربة المستخدم.             |
| `Sonner.js`     | `frontend/react-app/src/components/ui/Sonner.js`   | مكون إشعارات منبثقة (Toast/Sonner) لعرض رسائل موجزة للمستخدم.                                       |
| `Table.js`      | `frontend/react-app/src/components/ui/Table.js`    | المكونات الأساسية للجدول (`<table>`, `<thead>`, `<tbody>`, etc.) التي يستخدمها `DataTable.js`.       |
| `Textarea.js`   | `frontend/react-app/src/components/ui/Textarea.js` | مكون حقل نصي متعدد الأسطر.                                                                        |

--- 

## ✨ الخصائص والميزات الرئيسية المطبقة

1.  **إعادة الاستخدام (Reusability):** تم تصميم `DataTable` ليكون قابلاً لإعادة الاستخدام في أي مكان في التطبيق. يتم التحكم في سلوكه ومظهره عن طريق تمرير `props` مثل `columns` و `data`.
2.  **الفرز (Sorting):** يمكن للمستخدمين فرز البيانات تصاعديًا أو تنازليًا بالنقر على رؤوس الأعمدة.
3.  **البحث الشامل (Global Search):** يوفر شريط الأدوات حقل بحث يقوم بتصفية البيانات عبر جميع الأعمدة في الوقت الفعلي.
4.  **الفلترة المتقدمة (Faceted Filtering):** يمكن للمستخدمين تطبيق فلاتر دقيقة على أعمدة معينة (مثل عمود الحالة) عن طريق تحديد قيم متعددة من قائمة منبثقة.
5.  **الإجراءات المجمعة (Bulk Actions):** عند تحديد صف واحد أو أكثر، يظهر شريط أدوات جديد يسمح بتنفيذ إجراءات مجمعة مثل الحذف أو تغيير الحالة.
6.  **تصدير البيانات (Data Export):** يمكن للمستخدمين تصدير البيانات المعروضة حاليًا (سواء كانت مفلترة أم لا) إلى ملف CSV بنقرة زر واحدة.
7.  **التحكم في الأعمدة (Column Visibility):** يمكن للمستخدمين إخفاء أو إظهار الأعمدة التي يرغبون في رؤيتها من خلال قائمة "View".
8.  **الترقيم (Pagination):** يتم تقسيم البيانات على صفحات متعددة مع وجود أزرار تحكم للتنقل بينها، مما يحسن الأداء وتجربة المستخدم عند التعامل مع كميات كبيرة من البيانات.
