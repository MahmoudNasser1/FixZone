# 📥 استيراد البيانات من CSV - Import CSV Data

## 📋 المتطلبات (Requirements)

### 1. تثبيت مكتبة mysql-connector-python:

```bash
# الطريقة 1: استخدام pip3
pip3 install mysql-connector-python

# الطريقة 2: إذا لم تعمل الطريقة الأولى
python3 -m pip install mysql-connector-python --user

# الطريقة 3: استخدام pip
pip install mysql-connector-python
```

## 📁 ملفات CSV المطلوبة

يجب أن تكون الملفات في مجلد `IN/`:

1. **قائمة قائمة العملاء.csv** - قائمة العملاء
2. **الفواتير الغير مقفوله.csv** - الفواتير المفتوحة
3. **الفواتير المنتهيه.csv** - الفواتير المنتهية

## 🚀 الاستخدام

### الطريقة 1: تشغيل مباشر

```bash
cd /opt/lampp/htdocs/FixZone
python3 backend/scripts/import_csv_data.py
```

### الطريقة 2: مع متغيرات البيئة

```bash
cd /opt/lampp/htdocs/FixZone
DB_HOST=localhost DB_USER=root DB_PASSWORD= DB_NAME=FZ python3 backend/scripts/import_csv_data.py
```

### الطريقة 3: مع قاعدة بيانات مختلفة (مثلاً marina)

```bash
cd /opt/lampp/htdocs/FixZone
DB_NAME=marina python3 backend/scripts/import_csv_data.py
```

## 📊 ما يقوم به السكربت

### 1. استيراد العملاء:
- يقرأ ملف `قائمة قائمة العملاء.csv`
- ينشئ/يحدث سجلات العملاء في جدول `Customer`
- يحافظ على رقم الهاتف إذا كان موجوداً

### 2. استيراد الفواتير:
- يقرأ ملفات الفواتير (المفتوحة والمنتهية)
- ينشئ سجلات في جدول `Invoice`
- يربط الفواتير بالعملاء تلقائياً
- يحول التواريخ من العربية إلى الإنجليزية
- يحول المبالغ من النص العربي إلى أرقام

## 🔄 Mapping بين CSV وقاعدة البيانات

### جدول Customer:
- `الاسم` → `name`
- `رقم الجوال` → `phone`
- `المنطقة` → `address`
- `علامه` → `notes` (في customFields)

### جدول Invoice:
- `#` → `id` (auto increment)
- `العميل` → `customerId` (يربط بالعميل)
- `التاريخ` → `createdAt`
- `الإجمالى` → `totalAmount`
- `المدفوع` → `amountPaid`
- `الحالة` → `status` (محولة إلى إنجليزية)
- `النوع` → `invoiceType`

## ⚠️ ملاحظات مهمة

1. **التواريخ**: يجب أن تكون بتنسيق عربي مثل "الخميس, ٢٠ نوفمبر ٢٠٢٥"
2. **الأرقام**: يمكن أن تكون عربية أو إنجليزية
3. **العملاء**: إذا كان العميل موجود، يتم تحديثه. إذا لم يكن موجود، يتم إنشاؤه.
4. **الفواتير**: إذا كانت الفاتورة موجودة، يتم تخطيها.

## 🐛 حل المشاكل

### خطأ: ModuleNotFoundError: No module named 'mysql'

**الحل:**
```bash
pip3 install mysql-connector-python
# أو
python3 -m pip install mysql-connector-python --user
```

### خطأ: Access denied for user

**الحل:**
تأكد من إعدادات قاعدة البيانات في ملف `.env` أو المتغيرات البيئية

### خطأ: Table doesn't exist

**الحل:**
تأكد من أن قاعدة البيانات موجودة والجداول تم إنشاؤها

## 📝 أمثلة على الأوامر

```bash
# استيراد لقاعدة FZ الافتراضية
python3 backend/scripts/import_csv_data.py

# استيراد لقاعدة marina
DB_NAME=marina python3 backend/scripts/import_csv_data.py

# مع كلمة مرور
DB_PASSWORD=mypassword python3 backend/scripts/import_csv_data.py
```

