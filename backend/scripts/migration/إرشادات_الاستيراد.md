# إرشادات الاستيراد من النظام القديم 📚

---

## ✅ تم الاستيراد بنجاح!

تم نقل جميع البيانات من النظام القديم (u539485933_maintain) إلى النظام الجديد (FixZone)

---

## 📊 الإحصائيات

| البيان | العدد المستورد |
|--------|----------------|
| العملاء | 962 |
| الأجهزة | 1,265 |
| طلبات الإصلاح | 1,265 |
| القيم المساعدة | 49 |
| الفئات | 7 |

---

## 🎯 كيف تستخدم البيانات المستوردة؟

### 1. عرض جميع العملاء:
- اذهب إلى صفحة **العملاء** في النظام
- ستجد 962 عميل مستورد من النظام القديم

### 2. عرض طلبات الإصلاح:
- اذهب إلى صفحة **الإصلاحات**
- ستجد 1,265 طلب إصلاح
- كل طلب مرتبط بعميل وجهاز

### 3. البحث عن بيانات قديمة:
- كل سجل محفوظ فيه رقم النظام القديم
- يمكنك البحث بالرقم القديم

---

## 🔍 البحث عن البيانات

### البحث بالرقم القديم:

#### للعملاء:
```sql
SELECT * FROM Customer 
WHERE customFields LIKE '%"old_system_id":123%';
```

#### لطلبات الإصلاح:
```sql
SELECT * FROM RepairRequest 
WHERE customFields LIKE '%"old_invoice_id":456%';
```

---

## 📋 معلومات محفوظة

### لكل عميل:
- الاسم
- رقم الهاتف
- العنوان
- الرقم القديم في النظام (للمرجع)

### لكل جهاز:
- الماركة (Brand)
- الموديل (Model)
- السيريال (Serial Number)
- المواصفات (CPU, RAM, GPU, HDD)

### لكل طلب إصلاح:
- رقم الطلب
- العميل والجهاز
- وصف المشكلة
- ملاحظات الفحص
- التكلفة المقدرة والنهائية
- الحالة
- رمز التتبع (Tracking Token)

---

## 🔗 العلاقات بين البيانات

```
العميل (Customer)
    ↓
الجهاز (Device)
    ↓
طلب الإصلاح (RepairRequest)
```

### مثال:
- عميل اسمه "أحمد محمد"
- لديه جهاز "Dell Latitude"
- له طلب إصلاح رقم "REP-000123"

---

## 💡 استعلامات مفيدة

### 1. عرض معلومات طلب كامل:
```sql
SELECT 
  rr.requestNumber as 'رقم الطلب',
  c.name as 'اسم العميل',
  c.phone as 'الهاتف',
  d.brand as 'ماركة الجهاز',
  d.model as 'موديل الجهاز',
  rr.problemDescription as 'المشكلة',
  rr.status as 'الحالة',
  rr.estimatedCost as 'التكلفة المقدرة',
  rr.finalCost as 'التكلفة النهائية'
FROM RepairRequest rr
JOIN Customer c ON c.id = rr.customerId
JOIN Device d ON d.id = rr.deviceId
WHERE rr.requestNumber = 'REP-000001';
```

### 2. عرض جميع طلبات عميل معين:
```sql
SELECT 
  rr.requestNumber,
  d.brand,
  d.model,
  rr.status,
  rr.receivedAt
FROM RepairRequest rr
JOIN Customer c ON c.id = rr.customerId
JOIN Device d ON d.id = rr.deviceId
WHERE c.phone = '01234567890'
ORDER BY rr.receivedAt DESC;
```

### 3. إحصائيات الحالات:
```sql
SELECT 
  CASE 
    WHEN status = 'RECEIVED' THEN 'مستلم'
    WHEN status = 'INSPECTION' THEN 'تحت الفحص'
    WHEN status = 'UNDER_REPAIR' THEN 'تحت الإصلاح'
    WHEN status = 'READY_FOR_DELIVERY' THEN 'جاهز للتسليم'
    WHEN status = 'DELIVERED' THEN 'تم التسليم'
    ELSE status
  END as 'الحالة',
  COUNT(*) as 'العدد'
FROM RepairRequest
GROUP BY status
ORDER BY COUNT(*) DESC;
```

---

## ⚠️ ملاحظات مهمة

### 1. البيانات المستبعدة:
- **3 عملاء** كانوا محذوفين في النظام القديم
- **3 فواتير** كانت محذوفة في النظام القديم
- لم يتم استيرادهم للحفاظ على نظافة البيانات

### 2. حالات طلبات الإصلاح:
تم تحويل الحالات من النظام القديم:
- `1` → `RECEIVED` (مستلم)
- `2` → `INSPECTION` (تحت الفحص)
- `3` → `AWAITING_APPROVAL` (بانتظار الموافقة)
- `4` → `UNDER_REPAIR` (تحت الإصلاح)
- `5` → `READY_FOR_DELIVERY` (جاهز للتسليم)
- `6` → `DELIVERED` (تم التسليم)
- `7` → `REJECTED` (ملغي)
- `8` → `WAITING_PARTS` (بانتظار قطع غيار)
- `9` → `ON_HOLD` (معلق)

### 3. رموز التتبع (Tracking Tokens):
- تم إنشاء رمز تتبع فريد لكل طلب إصلاح
- يمكن للعميل استخدامه لتتبع طلبه عبر الموقع

---

## 🎓 نصائح للاستخدام

### 1. للبحث عن عميل:
- استخدم رقم الهاتف (أسرع طريقة)
- أو ابحث بالاسم

### 2. لعرض سجل عميل:
- ابحث عن العميل
- اذهب إلى صفحة تفاصيله
- ستجد جميع الأجهزة وطلبات الإصلاح

### 3. للتحقق من البيانات:
```sql
-- التأكد من عدد السجلات
SELECT 
  'العملاء' as 'الجدول',
  COUNT(*) as 'العدد'
FROM Customer WHERE deletedAt IS NULL
UNION ALL
SELECT 'الأجهزة', COUNT(*) FROM Device WHERE deletedAt IS NULL
UNION ALL
SELECT 'طلبات الإصلاح', COUNT(*) FROM RepairRequest WHERE deletedAt IS NULL;
```

---

## 📞 للدعم والمساعدة

إذا احتجت لمعلومات إضافية:
1. راجع الملف: `DATA_IMPORT_SUMMARY.md`
2. راجع الملف: `backend/scripts/migration/README.md`
3. راجع الملف: `backend/scripts/migration/QUICK_START.md`

---

## 🎊 تهانينا!

الآن لديك جميع بيانات النظام القديم في النظام الجديد!

يمكنك البدء بالعمل بشكل طبيعي وجميع البيانات التاريخية محفوظة.

---

**تاريخ الاستيراد:** 21 نوفمبر 2025  
**الحالة:** ✅ مكتمل ١٠٠٪  
**البيانات:** ✅ جاهزة للاستخدام
