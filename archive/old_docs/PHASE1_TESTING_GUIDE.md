# 🧪 دليل اختبار Phase 1 - نظام المخزون

**التاريخ:** 2 أكتوبر 2025  
**الهدف:** دليل شامل لاختبار كل ما تم تطويره في Phase 1

---

## 📋 جدول المحتويات

1. [اختبار قاعدة البيانات](#1-اختبار-قاعدة-البيانات)
2. [اختبار Backend APIs](#2-اختبار-backend-apis)
3. [اختبار Frontend](#3-اختبار-frontend)
4. [اختبار التكامل](#4-اختبار-التكامل)
5. [الأخطاء المتوقعة وحلولها](#5-الأخطاء-المتوقعة-وحلولها)

---

## 1. اختبار قاعدة البيانات

### 1.1 التحقق من الجداول الجديدة

```sql
-- فتح MySQL من Terminal
mysql -u root -p
USE FZ;

-- 1. التحقق من جميع الجداول
SHOW TABLES;

-- النتيجة المتوقعة: يجب أن ترى 17 جدول على الأقل بما في ذلك:
-- InventoryItemCategory
-- InventoryItemVendor  
-- StockTransfer
-- StockTransferItem
-- StockCount
-- StockCountItem
-- VendorPayment
-- StockAlert
-- BarcodeScan
```

**✅ ما يجب أن تراه:**
- 17 جدول إجمالاً
- 9 جداول جديدة من Phase 1

---

### 1.2 التحقق من البيانات التجريبية

```sql
-- عرض إحصائيات البيانات
SELECT 
  '📊 الإحصائيات' as info,
  (SELECT COUNT(*) FROM Vendor WHERE deletedAt IS NULL) as vendors,
  (SELECT COUNT(*) FROM InventoryItem WHERE deletedAt IS NULL) as items,
  (SELECT COUNT(*) FROM InventoryItemCategory) as categories,
  (SELECT COUNT(*) FROM StockLevel) as stock_levels,
  (SELECT COUNT(*) FROM StockMovement) as movements;
```

**✅ النتيجة المتوقعة:**
```
vendors: 26
items: 33
categories: 6
stock_levels: 58
movements: 19
```

---

### 1.3 اختبار الأعمدة الجديدة

```sql
-- التحقق من أعمدة InventoryItem
DESCRIBE InventoryItem;

-- يجب أن ترى:
-- ✅ barcode
-- ✅ partNumber
-- ✅ brand
-- ✅ model
-- ✅ categoryId
-- ✅ condition
-- ✅ reorderPoint
-- ✅ preferredVendorId
-- ✅ deletedAt
```

---

### 1.4 اختبار Views

```sql
-- اختبار View المخزون الشامل
SELECT * FROM v_inventory_summary LIMIT 5;

-- يجب أن ترى: name, sku, totalQuantity, totalValue, categoryName

-- اختبار View الأصناف المنخفضة
SELECT * FROM v_low_stock_items;

-- يجب أن ترى: الأصناف التي كميتها أقل من reorderPoint
```

**✅ ما يجب أن تراه:**
- Views تعمل بدون أخطاء
- بيانات واضحة ومنظمة
- حسابات صحيحة

---

### 1.5 اختبار العلاقات (Foreign Keys)

```sql
-- اختبار ربط الصنف بالفئة
SELECT 
  i.name as item,
  c.name as category
FROM InventoryItem i
LEFT JOIN InventoryItemCategory c ON i.categoryId = c.id
WHERE i.deletedAt IS NULL
LIMIT 10;

-- يجب أن ترى أصناف مربوطة بفئاتها
```

**✅ النتيجة المتوقعة:**
```
شاشة LCD Samsung A50 → شاشات (Screens)
بطارية ليثيوم → بطاريات (Batteries)
```

---

## 2. اختبار Backend APIs

### 2.1 تجهيز بيئة الاختبار

```bash
# تشغيل السيرفر
cd /opt/lampp/htdocs/FixZone/backend
node server.js

# أو
npm start
```

**✅ ما يجب أن تراه:**
```
Server running on port 4000
Database connected successfully
```

---

### 2.2 اختبار Inventory APIs

#### أ) GET /api/inventory (جلب جميع الأصناف)

**استخدام curl:**
```bash
curl http://localhost:4000/api/inventory
```

**استخدام Postman:**
- Method: GET
- URL: `http://localhost:4000/api/inventory`

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 1,
        "name": "شاشة LCD هاتف",
        "sku": "PART-001",
        "barcode": "BAR0000000001",
        "brand": "Samsung",
        "purchasePrice": 150.00,
        "sellingPrice": 250.00,
        "totalQuantity": 80,
        "totalAvailable": 67,
        "categoryName": "شاشات (Screens)"
      }
      // ... المزيد
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "totalItems": 33,
      "totalPages": 2
    },
    "summary": {
      "totalItems": 33,
      "activeItems": 33,
      "lowStockItems": 0,
      "outOfStockItems": 0,
      "totalValue": 30800.00
    }
  }
}
```

---

#### ب) GET /api/inventory/:id (جلب صنف واحد)

```bash
curl http://localhost:4000/api/inventory/1
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "item": {
      "id": 1,
      "name": "شاشة LCD هاتف",
      "sku": "PART-001",
      "totalQuantity": 80,
      "totalAvailable": 67
    },
    "stockLevels": [
      {
        "warehouseId": 1,
        "warehouseName": "المستودع الرئيسي",
        "currentQuantity": 45,
        "availableQuantity": 40
      }
    ],
    "recentMovements": [ /* آخر 10 حركات */ ],
    "vendors": [ /* موردين هذا الصنف */ ]
  }
}
```

---

#### ج) GET /api/inventory?search=lcd (بحث)

```bash
curl "http://localhost:4000/api/inventory?search=lcd"
```

**✅ النتيجة المتوقعة:**
- جميع الأصناف التي تحتوي على "lcd" في الاسم أو SKU

---

#### د) GET /api/inventory?category=1 (فلتر حسب فئة)

```bash
curl "http://localhost:4000/api/inventory?category=1"
```

**✅ النتيجة المتوقعة:**
- فقط الأصناف من فئة "شاشات"

---

#### هـ) GET /api/inventory?lowStock=true (أصناف منخفضة)

```bash
curl "http://localhost:4000/api/inventory?lowStock=true"
```

**✅ النتيجة المتوقعة:**
- الأصناف التي كميتها <= reorderPoint

---

#### و) POST /api/inventory (إضافة صنف جديد)

```bash
curl -X POST http://localhost:4000/api/inventory \
  -H "Content-Type: application/json" \
  -d '{
    "name": "اختبار صنف جديد",
    "purchasePrice": 100,
    "sellingPrice": 180,
    "categoryId": 1,
    "reorderPoint": 10
  }'
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "message": "تم إضافة الصنف بنجاح",
  "data": {
    "id": 34,
    "sku": "PART-034"
  }
}
```

---

#### ز) POST مع بيانات خاطئة (اختبار Validation)

```bash
curl -X POST http://localhost:4000/api/inventory \
  -H "Content-Type: application/json" \
  -d '{
    "name": "",
    "purchasePrice": -10,
    "sellingPrice": 5
  }'
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": false,
  "message": "خطأ في البيانات المدخلة",
  "errors": [
    {
      "field": "name",
      "message": "اسم الصنف مطلوب"
    },
    {
      "field": "purchasePrice",
      "message": "سعر الشراء يجب أن يكون موجب"
    },
    {
      "field": "sellingPrice",
      "message": "سعر البيع يجب أن يكون أكبر من سعر الشراء"
    }
  ]
}
```

---

### 2.3 اختبار Stock Levels API

```bash
# جلب مستويات المخزون
curl http://localhost:4000/api/stock-levels
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": [
    {
      "inventoryItemId": 1,
      "itemName": "شاشة LCD هاتف",
      "warehouseId": 1,
      "warehouseName": "المستودع الرئيسي",
      "currentQuantity": 45,
      "reservedQuantity": 5,
      "availableQuantity": 40,
      "status": "adequate"
    }
  ]
}
```

---

### 2.4 اختبار Stock Movements API

```bash
# جلب الحركات المخزنية
curl http://localhost:4000/api/stock-movements
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "movements": [
      {
        "id": 1,
        "movementType": "in",
        "itemName": "شاشة LCD",
        "warehouseName": "المستودع الرئيسي",
        "quantity": 50,
        "totalCost": 7500.00,
        "createdAt": "2025-09-17"
      }
    ],
    "summary": {
      "totalIn": 150,
      "totalOut": 85,
      "netMovement": 65
    }
  }
}
```

---

### 2.5 اختبار Vendors API

```bash
# جلب الموردين
curl http://localhost:4000/api/vendors
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "vendors": [
      {
        "id": 3,
        "name": "الشركة العالمية للشاشات",
        "phone": "01201234567",
        "rating": 4.8,
        "status": "active",
        "totalOrders": 0,
        "totalPurchases": 3510.00
      }
    ]
  }
}
```

---

### 2.6 اختبار Warehouses API

```bash
# جلب المخازن
curl http://localhost:4000/api/warehouses
```

**✅ النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "المستودع الرئيسي",
      "location": "القاهرة",
      "type": "main",
      "isActive": true
    }
  ]
}
```

---

## 3. اختبار Frontend

### 3.1 فتح صفحة المخزون

```
1. افتح المتصفح
2. اذهب إلى: http://localhost:3000
3. سجل دخول (admin/admin أو حسابك)
4. اذهب إلى قسم "المخزون"
```

**✅ ما يجب أن تراه:**

```
┌─────────────────────────────────────────────────┐
│ 📦 المخزون                           [+ إضافة] │
├─────────────────────────────────────────────────┤
│                                                  │
│ [🔍 بحث...] [الفئة ▼] [الحالة ▼]              │
│                                                  │
│ ┌──────────────────────────────────────────┐   │
│ │ الاسم | SKU | الفئة | المخزون | الحالة │   │
│ ├──────────────────────────────────────────┤   │
│ │ شاشة LCD | PART-001 | شاشات | 80 | ✅  │   │
│ │ بطارية | PART-002 | بطاريات | 160 | ✅ │   │
│ └──────────────────────────────────────────┘   │
│                                                  │
│ الصفحة 1 من 2                                  │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

### 3.2 اختبار البحث

```
1. في خانة البحث، اكتب: "LCD"
2. اضغط Enter
```

**✅ النتيجة المتوقعة:**
- تظهر فقط الأصناف التي تحتوي على "LCD"
- الصفحة تتحدث تلقائياً

---

### 3.3 اختبار الفلاتر

```
1. اختر فئة "شاشات" من القائمة المنسدلة
2. لاحظ التغيير
```

**✅ النتيجة المتوقعة:**
- تظهر فقط الأصناف من فئة "شاشات"
- العدد في الأعلى يتحدث

---

### 3.4 اختبار عرض تفاصيل صنف

```
1. اضغط على أي صنف في الجدول
2. أو اضغط زر "عرض" 👁️
```

**✅ النتيجة المتوقعة:**
```
┌────────────────────────────────────┐
│ 📦 تفاصيل الصنف          [✕]      │
├────────────────────────────────────┤
│                                     │
│ الاسم: شاشة LCD Samsung A50       │
│ SKU: PART-001                      │
│ الباركود: BAR0000000001           │
│                                     │
│ 💰 الأسعار:                        │
│   شراء: 150 ج.م                   │
│   بيع: 250 ج.م                     │
│                                     │
│ 📊 المخزون:                        │
│   المستودع الرئيسي: 45 قطعة      │
│   مستودع الجيزة: 20 قطعة          │
│                                     │
│ 🔄 آخر الحركات: [قائمة]          │
│                                     │
└────────────────────────────────────┘
```

---

### 3.5 اختبار إضافة صنف جديد

```
1. اضغط زر "+ إضافة صنف"
2. املأ النموذج:
   - الاسم: صنف تجريبي
   - سعر الشراء: 100
   - سعر البيع: 200
   - الفئة: شاشات
3. اضغط "حفظ"
```

**✅ النتيجة المتوقعة:**
- ✅ رسالة نجاح: "تم إضافة الصنف بنجاح"
- ✅ الصنف يظهر في القائمة
- ✅ SKU يُنشأ تلقائياً

---

### 3.6 اختبار Validation في Frontend

```
1. اضغط "+ إضافة صنف"
2. اترك الاسم فارغاً
3. ضع سعر شراء: -10
4. ضع سعر بيع: 5
5. اضغط "حفظ"
```

**✅ النتيجة المتوقعة:**
```
❌ خطأ:
- اسم الصنف مطلوب
- سعر الشراء يجب أن يكون موجب
- سعر البيع يجب أن يكون أكبر من سعر الشراء
```

---

## 4. اختبار التكامل

### 4.1 سيناريو كامل: من إضافة صنف إلى صرفه

```
الخطوة 1: إضافة صنف جديد
→ اذهب للمخزون
→ أضف صنف جديد
→ ✅ النجاح

الخطوة 2: إضافة مخزون للصنف
→ اذهب لمستويات المخزون
→ أضف 50 قطعة للمستودع الرئيسي
→ ✅ المخزون يظهر

الخطوة 3: صرف قطعة لصيانة
→ اذهب لطلبات الصيانة
→ أضف قطعة من المخزون
→ ✅ المخزون ينقص تلقائياً

الخطوة 4: تحقق من الحركة
→ اذهب لحركات المخزون
→ ✅ تظهر حركة "OUT" مسجلة
```

---

## 5. الأخطاء المتوقعة وحلولها

### ❌ خطأ 1: "Cannot GET /api/inventory"

**السبب:** السيرفر لا يعمل

**الحل:**
```bash
cd /opt/lampp/htdocs/FixZone/backend
node server.js
```

---

### ❌ خطأ 2: "Database connection failed"

**السبب:** MySQL لا يعمل

**الحل:**
```bash
sudo /opt/lampp/lampp start
```

---

### ❌ خطأ 3: "SKU already exists"

**السبب:** محاولة إضافة صنف بـ SKU موجود

**الحل:**
- اترك SKU فارغاً ليُنشأ تلقائياً
- أو استخدم SKU فريد

---

### ❌ خطأ 4: "Cannot read property 'length' of undefined"

**السبب:** البيانات لم تُحمّل من API

**الحل:**
- تحقق من أن السيرفر يعمل
- تحقق من console في المتصفح (F12)
- راجع الـ API URL

---

### ❌ خطأ 5: "500 Internal Server Error"

**السبب:** خطأ في السيرفر

**الحل:**
- راجع logs السيرفر
- تحقق من صحة الـ SQL queries
- راجع الـ console

---

## 📊 Checklist شامل

### قاعدة البيانات:
- [ ] 17 جدول موجودة
- [ ] 26 مورد
- [ ] 33 صنف
- [ ] 6 فئات
- [ ] 58 مستوى مخزون
- [ ] 19 حركة
- [ ] 3 Views تعمل

### Backend APIs:
- [ ] GET /api/inventory يعمل
- [ ] GET /api/inventory/:id يعمل
- [ ] POST /api/inventory يعمل
- [ ] PUT /api/inventory/:id يعمل
- [ ] DELETE /api/inventory/:id يعمل
- [ ] البحث يعمل
- [ ] الفلاتر تعمل
- [ ] Pagination يعمل
- [ ] Validation يعمل
- [ ] Error handling يعمل

### Frontend:
- [ ] صفحة المخزون تفتح
- [ ] الجدول يعرض البيانات
- [ ] البحث يعمل
- [ ] الفلاتر تعمل
- [ ] إضافة صنف تعمل
- [ ] تعديل صنف يعمل
- [ ] حذف صنف يعمل
- [ ] عرض التفاصيل يعمل

---

## ✅ النتيجة النهائية

إذا نجحت في جميع الاختبارات أعلاه، فهذا يعني:

```
✅ قاعدة البيانات: سليمة 100%
✅ Backend APIs: تعمل بشكل صحيح
✅ Validation: فعالة
✅ Error Handling: موحد ومحترف
✅ Frontend: يعرض البيانات
✅ التكامل: يعمل بسلاسة
```

**🎉 Phase 1 ناجح وجاهز للمرحلة التالية!**

---

**التاريخ:** 2 أكتوبر 2025  
**الحالة:** ✅ دليل جاهز للاستخدام

