# مكونات UI المحسنة - Fix Zone ERP

هذا المجلد يحتوي على مجموعة شاملة من مكونات UI المحسنة والمخصصة لنظام Fix Zone ERP.

## 🚀 التحسينات الجديدة

### 🏗️ مكونات التخطيط المحسنة
- **MainLayout** - تخطيط رئيسي مرن مع دعم العناوين والإجراءات
- **Sidebar** - شريط جانبي محسن مع تنظيم أفضل وإشعارات
- **TopBar** - شريط علوي مع اختصارات سريعة وبحث مدمج
- **Breadcrumb** - مسار تنقل تلقائي وذكي

## 📋 قائمة المكونات

### 🎨 المكونات الأساسية
- **Alert** - تنبيهات وإشعارات متنوعة مع أيقونات وأنواع مختلفة
- **Avatar** - صور المستخدمين مع حالات الاتصال والمجموعات
- **Badge** - شارات للحالات والأولويات والعدادات
- **Button** - أزرار متنوعة مع أحجام وأنواع مختلفة
- **Card** - بطاقات للمحتوى مع أنواع مخصصة
- **Loading** - مؤشرات التحميل المتنوعة

### 📝 مكونات النماذج
- **Form** - نماذج محسنة مع التحقق من البيانات
- **Input** - حقول الإدخال المحسنة
- **Textarea** - مناطق النص الكبيرة
- **Switch** - مفاتيح التبديل المحسنة
- **Checkbox** - مربعات الاختيار

### 🗂️ مكونات التنظيم
- **Tabs** - علامات التبويب المتقدمة
- **Breadcrumb** - مسار التنقل
- **DataTable** - جداول البيانات المتقدمة
- **Pagination** - ترقيم الصفحات المحسن
- **Progress** - مؤشرات التقدم والخطوات

### 💬 مكونات التفاعل
- **Modal** - النوافذ المنبثقة
- **Toast** - الإشعارات المؤقتة
- **Dialog** - مربعات الحوار
- **DropdownMenu** - القوائم المنسدلة
- **Popover** - النوافذ المنبثقة الصغيرة

## 🚀 الميزات الرئيسية

### ✨ التصميم المتقدم
- استخدام **Tailwind CSS** للتصميم السريع
- دعم **class-variance-authority** للأنواع المتعددة
- تصميم متجاوب يدعم جميع الأحجام
- دعم كامل للغة العربية واتجاه RTL

### 🎯 سهولة الاستخدام
- مكونات قابلة للإعادة الاستخدام
- واجهات برمجية واضحة ومتسقة
- دعم TypeScript (عند الحاجة)
- توثيق شامل لكل مكون

### 🔧 المرونة والتخصيص
- أنواع متعددة لكل مكون (variants)
- أحجام مختلفة (sizes)
- إمكانية التخصيص الكامل
- دعم الخصائص المخصصة

## 📖 أمثلة الاستخدام

### Button Component
```jsx
import { Button, LoadingButton, IconButton } from './components/ui';

// زر أساسي
<Button variant="default" size="md">حفظ</Button>

// زر مع تحميل
<LoadingButton loading={isLoading}>إرسال</LoadingButton>

// زر مع أيقونة
<IconButton icon={<Plus />} variant="outline">إضافة</IconButton>
```

### Card Component
```jsx
import { Card, StatsCard, FeatureCard } from './components/ui';

// بطاقة إحصائيات
<StatsCard
  title="إجمالي الطلبات"
  value="1,234"
  change="+12%"
  trend="up"
/>

// بطاقة ميزة
<FeatureCard
  icon={<Settings />}
  title="إدارة الإعدادات"
  description="تحكم في جميع إعدادات النظام"
/>
```

### DataTable Component
```jsx
import { DataTable } from './components/ui';

<DataTable
  columns={columns}
  data={data}
  searchable
  filterable
  exportable
/>
```

### Form Components
```jsx
import { Form, FormFieldGroup, FormInput } from './components/ui';

<Form>
  <FormFieldGroup
    label="اسم العميل"
    name="customerName"
    required
    placeholder="أدخل اسم العميل"
  />
</Form>
```

## 🎨 الأنواع المتاحة (Variants)

### Button Variants
- `default` - الزر الافتراضي
- `destructive` - للإجراءات الخطيرة
- `outline` - زر بحدود
- `secondary` - زر ثانوي
- `ghost` - زر شفاف
- `link` - زر كرابط

### Badge Variants
- `default` - الشارة الافتراضية
- `success` - للحالات الناجحة
- `warning` - للتحذيرات
- `destructive` - للأخطاء
- `info` - للمعلومات

### Alert Variants
- `default` - التنبيه الافتراضي
- `success` - تنبيه النجاح
- `warning` - تنبيه التحذير
- `destructive` - تنبيه الخطر
- `info` - تنبيه المعلومات

## 🔧 التخصيص المتقدم

### إضافة أنواع جديدة
```jsx
// في ملف المكون
const buttonVariants = cva(
  "base-classes",
  {
    variants: {
      variant: {
        // إضافة نوع جديد
        custom: "custom-classes",
      },
    },
  }
);
```

### تخصيص الألوان
```jsx
// في ملف Tailwind config
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          // ألوان مخصصة
        },
      },
    },
  },
};
```

## 🎯 أمثلة الاستخدام لمكونات التخطيط

### استخدام MainLayout
```jsx
import { MainLayout, SimpleLayout } from '../components/layout';
import { Button } from '../components/ui/Button';
import { Plus, Filter } from 'lucide-react';

// التخطيط الأساسي مع عنوان وإجراءات
const MyPage = () => {
  const pageActions = (
    <>
      <Button variant="outline" size="sm">
        <Filter className="w-4 h-4 ml-2" />
        فلترة
      </Button>
      <Button size="sm">
        <Plus className="w-4 h-4 ml-2" />
        إضافة جديد
      </Button>
    </>
  );

  return (
    <MainLayout 
      pageTitle="إدارة طلبات الإصلاح"
      pageActions={pageActions}
      showBreadcrumb={true}
    >
      {/* محتوى الصفحة */}
    </MainLayout>
  );
};

// التخطيط المبسط
const SimplePageExample = () => (
  <SimpleLayout title="العملاء" actions={<Button>عميل جديد</Button>}>
    {/* محتوى الصفحة */}
  </SimpleLayout>
);
```

### استخدام Breadcrumb المخصص
```jsx
import { Breadcrumb, SimpleBreadcrumb } from '../components/layout';
import { Home, Users, User } from 'lucide-react';

// مسار تنقل مخصص
const customBreadcrumb = [
  { label: 'الرئيسية', href: '/dashboard', icon: Home },
  { label: 'العملاء', href: '/customers', icon: Users },
  { label: 'أحمد محمد', isActive: true, icon: User }
];

<Breadcrumb items={customBreadcrumb} />

// مسار تنقل مبسط
<SimpleBreadcrumb 
  title="ملف العميل" 
  parentTitle="العملاء" 
  parentHref="/customers" 
/>
```

### تخصيص Sidebar
```jsx
// يمكن تخصيص الـ Sidebar من خلال تعديل ملف Sidebar.js
// أو إنشاء مكون مخصص يستورد المكونات الأساسية
```

## 📱 الاستجابة والتوافق

جميع المكونات مصممة لتكون:
- **متجاوبة** - تعمل على جميع أحجام الشاشات
- **متوافقة** - تدعم جميع المتصفحات الحديثة
- **سريعة** - محسنة للأداء
- **قابلة للوصول** - تدعم معايير الوصول

## 🚀 الخطوات التالية

1. **تطوير مكونات إضافية** حسب احتياجات المشروع
2. **إضافة اختبارات** للمكونات الحرجة
3. **تحسين الأداء** والتحميل الكسول
4. **إضافة المزيد من الأمثلة** والتوثيق

## 💡 نصائح للاستخدام

- استخدم `cn()` لدمج الفئات بطريقة آمنة
- اعتمد على المتغيرات المحددة مسبقاً
- اختبر المكونات على أحجام شاشات مختلفة
- استخدم المكونات المحسنة بدلاً من الأساسية عند الحاجة

---

**تم تطوير هذه المكونات خصيصاً لنظام Fix Zone ERP مع التركيز على الأداء وسهولة الاستخدام.**
