# ⚡ تعليمات الاختبار السريع - Phase 1

**آخر تحديث:** 2 أكتوبر 2025  
**المدة:** 10-15 دقيقة

---

## 🚀 اختبار سريع (3 طرق)

### ✅ الطريقة 1: الاختبار التلقائي (الأسهل!)

```bash
# في Terminal
cd /opt/lampp/htdocs/FixZone

# شغل السيرفر في الخلفية
cd backend
node server.js &

# ارجع للمجلد الرئيسي وشغل الاختبار
cd ..
node testing/test-inventory-phase1.js
```

**✅ النتيجة المتوقعة:**
```
====================================
🧪 Phase 1 - Inventory Module Tests
====================================

Test 1: GET /api/inventory
✅ جلب الأصناف - 33 صنف
✅ Pagination موجود
✅ الإحصائيات - قيمة المخزون: 30800 ج.م

Test 2: GET /api/inventory/:id
✅ جلب صنف واحد - شاشة LCD هاتف
✅ مستويات المخزون - 3 مخزن

Test 3: Search
✅ البحث - وجد 11 صنف

... إلخ

====================================
📊 ملخص النتائج
====================================

إجمالي الاختبارات: 9
✅ نجح: 9
❌ فشل: 0
📊 نسبة النجاح: 100.0%

🎉 تهانينا! جميع الاختبارات نجحت!
✅ Phase 1 يعمل بشكل صحيح 100%
```

---

### ✅ الطريقة 2: الاختبار اليدوي من قاعدة البيانات

```bash
# افتح MySQL
mysql -u root -p
```

```sql
USE FZ;

-- 1. عدد الموردين (يجب: 26)
SELECT COUNT(*) FROM Vendor WHERE deletedAt IS NULL;

-- 2. عدد الأصناف (يجب: 33)
SELECT COUNT(*) FROM InventoryItem WHERE deletedAt IS NULL;

-- 3. الفئات (يجب: 6)
SELECT id, name FROM InventoryItemCategory;

-- 4. قيمة المخزون (يجب: ~30,800)
SELECT SUM(sl.currentQuantity * i.purchasePrice) as total_value
FROM StockLevel sl
JOIN InventoryItem i ON sl.inventoryItemId = i.id;

-- 5. عرض بعض الأصناف
SELECT 
  i.name,
  i.sku,
  c.name as category,
  SUM(sl.currentQuantity) as qty
FROM InventoryItem i
LEFT JOIN InventoryItemCategory c ON i.categoryId = c.id
LEFT JOIN StockLevel sl ON i.id = sl.inventoryItemId
WHERE i.deletedAt IS NULL
GROUP BY i.id
LIMIT 10;
```

**✅ ما يجب أن تراه:**
- 26 مورد ✅
- 33 صنف ✅
- 6 فئات ✅
- قيمة ~30,800 ج.م ✅
- أصناف منظمة بفئاتها ✅

---

### ✅ الطريقة 3: الاختبار من المتصفح

#### أ) اختبار Backend (Postman أو curl):

**1. اختبر API بسيط:**
```bash
curl http://localhost:3001/api/inventory
```

**✅ يجب أن ترى:**
```json
{
  "success": true,
  "data": {
    "items": [ /* 33 صنف */ ]
  }
}
```

**2. اختبر البحث:**
```bash
curl "http://localhost:3001/api/inventory?search=lcd"
```

**✅ يجب أن ترى:**
- فقط الأصناف التي تحتوي على "LCD"

---

#### ب) اختبار Frontend:

```
1. افتح المتصفح
2. اذهب إلى: http://localhost:3000
3. سجل دخول (إذا لزم)
4. اذهب لقسم "المخزون" أو "Inventory"
```

**✅ ما يجب أن تراه:**

```
┌─────────────────────────────────────────────┐
│ 📦 المخزون                      [+ إضافة]  │
├─────────────────────────────────────────────┤
│                                              │
│ 📊 الإحصائيات:                              │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │
│ │  33  │ │30,800│ │  0   │ │  0   │        │
│ │أصناف │ │ قيمة │ │منخفض │ │ نفذ  │        │
│ └──────┘ └──────┘ └──────┘ └──────┘        │
│                                              │
│ [🔍 ابحث...] [الفئة ▼] [الحالة ▼]         │
│                                              │
│ ┌──────────────────────────────────────┐   │
│ │ الاسم      │ SKU  │ الفئة  │ المخزون│   │
│ ├──────────────────────────────────────┤   │
│ │ شاشة LCD   │ 001  │ شاشات │ 80 🟢 │   │
│ │ بطارية     │ 002  │ بطاريات│ 160 🟢│   │
│ │ ...        │ ...  │ ...   │ ...   │   │
│ └──────────────────────────────────────┘   │
│                                              │
└─────────────────────────────────────────────┘
```

---

## 🧪 اختبارات إضافية

### اختبار 1: إضافة صنف جديد

```
في Frontend:
1. اضغط "+ إضافة صنف"
2. املأ:
   - الاسم: اختبار صنف
   - سعر الشراء: 100
   - سعر البيع: 200
3. احفظ
```

**✅ النتيجة:**
- رسالة نجاح
- الصنف يظهر في القائمة

---

### اختبار 2: البحث

```
1. في خانة البحث، اكتب: "iphone"
2. انتظر قليلاً
```

**✅ النتيجة:**
- تظهر كل أصناف iPhone

---

### اختبار 3: الفلترة

```
1. اختر فئة "شاشات" من القائمة
```

**✅ النتيجة:**
- تظهر فقط 11 صنف شاشات

---

## ⚠️ إذا ظهرت أخطاء

### خطأ: "Cannot connect to server"

**الحل:**
```bash
# تأكد أن السيرفر يعمل
cd /opt/lampp/htdocs/FixZone/backend
node server.js

# يجب أن ترى:
# Server running on port 3001 ✅
```

---

### خطأ: "MySQL connection failed"

**الحل:**
```bash
# شغل XAMPP
sudo /opt/lampp/lampp start

# أو
sudo systemctl start mysql
```

---

### خطأ: "No data found"

**الحل:**
```bash
# تأكد من تطبيق البيانات التجريبية
cd /opt/lampp/htdocs/FixZone/migrations
mysql -u root -p FZ < inventory_sample_data.sql
```

---

## ✅ Checklist سريع

قبل الاختبار:
- [ ] MySQL يعمل (`sudo /opt/lampp/lampp status`)
- [ ] قاعدة البيانات FZ موجودة
- [ ] Migration تم تطبيقه
- [ ] البيانات التجريبية موجودة

أثناء الاختبار:
- [ ] السيرفر Backend يعمل (port 3001)
- [ ] السيرفر Frontend يعمل (port 3000)
- [ ] لا توجد أخطاء في console

---

## 📞 ملفات مهمة

```bash
# دليل الاختبار الشامل (كل التفاصيل)
testing/PHASE1_TESTING_GUIDE.md

# اختبار تلقائي (سريع)
testing/test-inventory-phase1.js

# الخطة الكاملة
InventoryModulePlan/README.md

# تقرير Phase 1
InventoryModulePlan/PHASE1_COMPLETION_REPORT.md
```

---

## ✨ نصيحة

**للاختبار السريع جداً (دقيقتان!):**

```bash
# فقط شغل هذا الأمر:
cd /opt/lampp/htdocs/FixZone

# تأكد أن السيرفر يعمل أولاً
cd backend && node server.js &

# ثم اختبر
cd .. && node testing/test-inventory-phase1.js
```

إذا كانت النتيجة:
```
✅ نجح: 9
❌ فشل: 0
📊 نسبة النجاح: 100.0%
🎉 تهانينا! جميع الاختبارات نجحت!
```

**يبقى كل حاجة تمام! 🎉**

---

**تاريخ الإنشاء:** 2 أكتوبر 2025  
**الحالة:** ✅ جاهز للاستخدام

