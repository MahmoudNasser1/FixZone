#  погружение в систему FixZone ERP: анализ бизнес-логики и архитектуры

**Дата:** 14 ноября 2025 г.  
**Аналитик:** ИИ-помощник (инженер по качеству)  
**Цель:** Этот документ предоставляет углубленный анализ основной бизнес-логики, архитектуры и взаимодействия модулей в системе FixZone ERP. Цель — понять «почему» и «как» система работает, чтобы обеспечить основу для комплексного тестирования и будущего развития.

---

##  Core Business Flow: от ремонта до получения дохода

В основе FixZone лежит четко определенный процесс, который превращает запрос на ремонт от клиента в доход. Этот поток является основой всей системы.

1.  **Запрос на ремонт (Repair Request):**
    *   **Цель:** Зафиксировать проблему клиента и информацию об устройстве. Это отправная точка для любого ремонта.
    *   **Компоненты:**
        *   **Frontend:** `NewRepairPage.js`, `RepairDetailsPage.js`.
        *   **Backend:** `repairs.js` (контроллер), таблицы `RepairRequest`, `Customer`, `Device`.

2.  **Диагностика и выполнение (Diagnostics & Fulfillment):**
    *   **Цель:** Технический специалист диагностирует проблему, определяет необходимые услуги и использует запчасти со склада.
    *   **Компоненты:**
        *   **Frontend:** `RepairDetailsPage.js` (добавление услуг, использование запчастей).
        *   **Backend:** Контроллеры `repairs.js`, `inventoryController.js`. Таблицы `RepairRequestService`, `PartsUsed`, `StockMovement`.
        *   **Ключевая логика:** Когда запчасть используется (`PartsUsed`), ее количество должно быть уменьшено на складе (`StockLevel`), и это движение должно быть записано (`StockMovement`).

3.  **Выставление счетов (Invoicing):**
    *   **Цель:** Создать финансовый документ (счет), который объединяет все затраты (услуги и запчасти) для клиента.
    *   **Компоненты:**
        *   **Frontend:** `CreateInvoicePage.js`, `InvoiceDetailsPage.js`.
        *   **Backend:** `invoicesController.js`. Таблицы `Invoice`, `InvoiceItem`.
        *   **Ключевая логика:** Счет может быть создан:
            1.  **Из запроса на ремонт:** Автоматически добавляет все услуги и использованные запчасти в счет.
            2.  **Напрямую:** Позволяет создавать счета для продажи товаров или услуг без предварительного ремонта (например, продажа аксессуаров). Для этого требуется прямая привязка к клиенту.

4.  **Оплата (Payment):**
    *   **Цель:** Зафиксировать платежи от клиента по счету.
    *   **Компоненты:**
        *   **Frontend:** `InvoiceDetailsPage.js` (добавление платежа).
        *   **Backend:** `invoicesController.js` (логика платежей, вероятно, в отдельном контроллере `payments.js`). Таблица `Payment`.
        *   **Ключевая логика:** Статус счета (`draft`, `partially_paid`, `paid`) динамически обновляется в зависимости от общей суммы и полученных платежей.

---

## 📦 Модуль 2: Управление запасами (Inventory Management) — сердце операций

Управление запасами — это не просто список товаров; это сложная система, обеспечивающая наличие нужных запчастей в нужное время.

### 2.1. Основные концепции

*   **Товар (InventoryItem):** Основная единица. Это не физический товар, а его описание (например, «Экран для iPhone 15»). Содержит SKU, закупочную и продажную цены.
*   **Склад (Warehouse):** Физическое местоположение, где хранятся товары. Система поддерживает несколько складов.
*   **Уровень запасов (StockLevel):** Связующее звено. Указывает, какое количество (`quantity`) определенного `InventoryItem` находится в конкретном `Warehouse`.
*   **Движение запасов (StockMovement):** Журнал всех транзакций. Каждое добавление, списание или перемещение запасов должно быть записано здесь. Это обеспечивает полную отслеживаемость.
*   **Перемещение запасов (StockTransfer):** Специализированная операция для перемещения товаров между складами. Она генерирует два `StockMovement` (один «OUT» со склада-источника, один «IN» на склад-получатель).

### 2.2. Бизнес-цели и их реализация

*   **Цель: Знать, что у нас есть и где.**
    *   **Реализация:** `inventoryController.js` (`getAllInventoryItems`) объединяет данные из `InventoryItem` и `StockLevel`, чтобы показать общее количество и распределение по складам.

*   **Цель: Пополнять запасы.**
    *   **Реализация:** Это делается через **счета на закупку**.
        1.  Создается счет с `invoiceType = 'purchase'`, привязанный к поставщику (`Vendor`).
        2.  Товары из этого счета добавляются на склад через операцию `adjustStock` в `inventoryController.js`, которая создает `StockMovement` типа `IN`.

*   **Цель: Использовать запчасти для ремонта.**
    *   **Реализация:**
        1.  В `RepairDetailsPage.js` техник добавляет «использованную запчасть».
        2.  Это создает запись в `PartsUsed`.
        3.  Backend должен инициировать `StockMovement` типа `OUT` со склада техника, уменьшая `StockLevel`.

*   **Цель: Продавать товары напрямую.**
    *   **Реализация:**
        1.  В `CreateInvoicePage.js` пользователь добавляет товар со склада.
        2.  После завершения счета (например, статус `paid`), система должна инициировать `StockMovement` типа `OUT`.

---

## 💰 Модуль 3: Выставление счетов и финансы (Invoicing & Finance) — управление денежными потоками

Этот модуль является финансовым ядром системы, отслеживая все доходы и расходы.

### 3.1. Типы счетов

Анализ `invoicesController.js` и `DETAILED_IMPLEMENTATION_PLAN.md` показывает, что система поддерживает (или будет поддерживать) два ключевых типа счетов:

1.  **Счет на продажу (Sale Invoice):**
    *   **`invoiceType = 'sale'`**
    *   **Назначение:** Выставляется клиенту (`Customer`).
    *   **Источник:** Может быть создан из `RepairRequest` или напрямую для продажи товаров/услуг.
    *   **Влияние:** Увеличивает дебиторскую задолженность.

2.  **Счет на закупку (Purchase Invoice):**
    *   **`invoiceType = 'purchase'`**
    *   **Назначение:** Получается от поставщика (`Vendor`).
    *   **Назначение:** Используется для отслеживания расходов и пополнения запасов.
    *   **Влияние:** Увеличивает кредиторскую задолженность и инициирует пополнение запасов.

### 3.2. Расходы (Expenses)

*   **Цель:** Отслеживать операционные расходы, не связанные напрямую с закупкой товаров (например, аренда, зарплата, маркетинг).
*   **Реализация (из плана):**
    *   Будет создана новая страница `ExpensesPage.js` и соответствующий backend (`expenses.js`).
    *   Расход (`Expense`) может быть связан со счетом на закупку (`Invoice` с `invoiceType = 'purchase'`) или существовать самостоятельно.

---

## 🔗 4. Взаимосвязи и зависимости модулей

Ни один модуль не работает в изоляции. Понимание их взаимосвязей критически важно.

*   **Ремонт -> Запасы:** Запрос на ремонт потребляет запчасти, что уменьшает уровень запасов.
*   **Ремонт -> Счета:** Запрос на ремонт является основанием для создания счета на продажу.
*   **Запасы -> Счета:**
    *   Товары со склада могут быть проданы напрямую через счет на продажу.
    *   Пополнение запасов происходит через счета на закупку.
*   **Клиенты/Поставщики -> Счета:**
    *   Клиент (`Customer`) привязан к счету на продажу.
    *   Поставщик (`Vendor`) привязан к счету на закупку.

![Entity Relationship Diagram](https://i.imgur.com/example.png)  
*(Примечание: это концептуальная диаграмма, реальная схема может отличаться)*

---

## 🎯 5. Заключение и следующие шаги

Этот углубленный анализ показывает, что FixZone ERP — это взаимосвязанная система, построенная вокруг основного бизнес-процесса «ремонт-счет-оплата». Управление запасами и финансовый учет являются ключевыми поддерживающими модулями.

**Ключевые выводы для тестирования:**

1.  **Тестирование сквозных сценариев (End-to-End):** Наиболее важные тесты должны проверять полный цикл:
    *   Создание ремонта -> Использование запчасти -> Уменьшение запасов -> Создание счета -> Регистрация оплаты -> Обновление статуса счета.
    *   Создание счета на закупку -> Оплата поставщику -> Увеличение запасов.

2.  **Тестирование целостности данных (Data Integrity):**
    *   Убедитесь, что `StockMovement` создается для каждой операции с запасами.
    *   Проверьте, что `totalAmount` в счете всегда является точной суммой его `InvoiceItems`.
    *   Статус счета должен всегда точно отражать состояние его платежей.

3.  **Тестирование пограничных случаев (Edge Cases):**
    *   Что произойдет, если попытаться использовать запчасть, которой нет в наличии? (Ожидается ошибка)
    *   Можно ли удалить услугу из счета, который уже частично оплачен? (Требуется бизнес-решение)
    *   Создание счета без клиента или запроса на ремонт. (Ожидается ошибка валидации)

Этот документ служит основой для создания комплексного плана тестирования, который охватывает не только отдельные функции, но и их взаимодействие в рамках всей системы.









